# DeliDev implementation and evidence ledger

Issue #964 is preserved in full in [requirements](cmds-delidev-requirements.md). This ledger distinguishes code, deterministic tests, and actual external/native evidence. It is not a reduction of the requirements.

## Current implementation work
The CLI/server/Worker implementation is in progress. No release or complete real-account harness integration claim is made; bounded native evidence is recorded separately below.

| Boundary | Implementation | Verification |
| --- | --- | --- |
| CLI, typed JSON, explicit startup, server/sidecar lifecycle | Versioned JSON/errors, explicit detached/foreground startup and compatible reuse, status/stop, configuration CRUD, read-only doctor, backup implemented; native services/supervision pending | Native macOS subprocess smoke: no implicit startup, readiness/reuse, CRUD, backup, explicit stop, restart persistence |
| Connect, authentication, origins, pairing, TLS, streams | Owner authentication on every product RPC, exact origins, TLS-required remote listeners, signed scoped cursors, bounded Connect event streaming implemented; single-use device pairing, credential revocation, machine-scoped Worker authorization and outbound job streams implemented | Real loopback Connect: missing authentication, hostile origin/Host, snapshot/replay, deduplication across restart, backup deduplication, pairing replay/single-use, cross-machine denial, claimed-job/report deduplication, active-stream revocation; remote TLS validation guards tested, remote native lifecycle not validated |
| SQLite, IDs/revisions, atomic events/receipts, backup/restore | Exclusive private scope, WAL transactions, global entity identity, optimistic revisions, durable receipts, metadata-only events, coherent bounded snapshots, consistent backup implemented; restore/deletion coordination pending | Real temporary SQLite: concurrent retries/restart, rollback, kind collisions, deletion receipt redaction, cursor bounds, WAL backup, corrupt/newer DB preservation; Go race tests pass |
| Projects, repositories, settings, restrictions, templates | Strict versioned configuration schemas, relationship/revision validation and server-owned health fields implemented; repository saves now revalidate each Worker and atomically commit canonical checkouts with default-on automatic fetch; Project/Agent deletion preserves retained snapshots and final project restrictions while disabling future schedules | CLI/Connect configuration, real Worker saves, multi-machine all-or-nothing validation, missing-remote refusal and concurrent asynchronous revision tests; complete session workflow pending |
| Accounts, provider/models, protected secrets, proxy | Native OS wrapping-key adapters and a private authenticated-encryption vault with immutable retries/deletion markers implemented; API/keyless account connect/disconnect/status and cleanup retries integrated through RPC/CLI; bounded non-inference account validation, nine provider presets, periodic/explicit catalog discovery, search/resolve and deletion suppression integrated; bounded native API JSON/SSE relay and server-side durable execution authority/reference storage implemented; Worker scoped-token delivery and first Codex execution integrated; quota refresh, subscription authentication, native model compatibility remain pending; public first Codex API dispatch and FIFO/Resume continuation are integrated | Temporary macOS keychain and Linux arm64 GNOME Keyring container round trips/locked-state checks; real Connect/SQLite account lifecycle/revocation/restart tests and actual Linux CLI/server/Secret Service composition; Windows native credential test implemented and cross-compiled, not executed |
| Six routing policies, quota evidence, immutable snapshots | Pure six-policy selection/read-only preview and private atomic first-execution snapshot/account/input-claim/routing persistence primitive implemented; public first Codex readiness/immutable job integration implemented | Weighted rotation, sequential traversal/recovery, project/account restrictions, Fixed exclusions, minimum blocking windows, stale evidence and quota ties; real SQLite concurrent claims, rollback, restart immutability and edit/claim races tested with simulated readiness |
| Workers, executables, outbound jobs, processes, updates, services | Durable outbound job API, instance leases and uncertainty on replacement implemented; explicit foreground Worker loop, private pairing and execution journals, capped reconnect and CLI commands implemented; native process ownership with a durable start barrier, separate interactive streams and Git integration implemented; Worker-owned executable selection and version discovery through RPC/CLI implemented; Codex non-inference native handshake implemented; first Codex job execution through cleanup reporting integrated; remaining native profiles/lifecycle, updates and services pending | Real Connect pairing/dispatch/report/revocation tests; CLI pairing to a real Worker and real temporary Git inspection; persisted completion reuse and interrupted-journal uncertainty; discovery isolation/path precedence/output bounds/cancellation and stale-generation publication tests; installed Codex Worker execution and live revocation pass against a scripted loopback provider with seeded readiness, not external inference |
| Codex, Claude Code, OpenCode, Grok Build native adapters | Bounded native JSON-RPC transport and version-specific Codex non-inference app-server handshake integrated with discovery; separate Claude Code `2.1.236` bare-mode stream-json initialization and exact private scope validation integrated with discovery; internal thread create/read/resume, exact settings, Execute/Plan turns, expected-turn Steer, interruption and typed core lifecycle/message events implemented; immutable first-turn Worker execution with scoped account authority, durable events and cleanup reporting integrated; durable questions and public non-secret response acceptance through journaled native delivery and exact live acceptance evidence integrated; public first dispatch, verified FIFO/Resume continuation and selected-input public Steer integrated; original approvals, journaled responses, exact permission output and pinned single-use execution evidence integrated; bounded automatic inspection after uncertain interaction delivery integrated; remaining rich events/approval cases, Claude session/account execution and other native adapters pending | Real macOS Claude Code `2.1.236` private no-credential/no-inference initialization and owned cleanup; real macOS Codex `0.151.0` handshake, thread persistence/resume, Execute/Plan/Steer/interruption against loopback scripted provider responses; temporary native protocol fixtures; real selected accounts, external inference and other OS/harness combinations not validated |
| Workspace preparation, Local protection, forks, snapshots | Worker-local inspection, exact reference resolution, detached multi-repository preparation, primary cwd, Local protection, General Chat isolation, durable preparation/cleanup manifests implemented; multi-repository native Codex dispatch/continuation with exact primary/runtime roots and authenticated Local existing-checkout and unborn-branch execution implemented; inspection and session preparation RPC/CLI integrated with atomic complete-manifest publication and immutable job cancellation; original-journal preparation recovery and explicit partial cleanup, including Local metadata-only cleanup, integrated; native forks and snapshots pending | Real temporary Git: subdirectory/linked inspection, detached commits, retry reuse, partial rollback, dirty Local preservation, remote fetch advancement/failure, disabled fetch, cancellation, explicit missing-default failure; real CLI/Worker General Chat/multi-repository preparation and Archive retention |
| Sessions, queue, Steer, interactions, Plan, archive/recovery | Durable session/initial-input acceptance, ordered bounded queue, revision-checked content edits/removal tombstones, session lists/rename, workspace jobs and never-executed preparation-aware Stop/Archive/Restore integrated through Connect/CLI; accepted first Codex Worker jobs and terminal cleanup reports integrated; first-native Stop/Archive/paused Restore and bounded Worker interruption integrated; public owner non-secret question responses and journaled Worker delivery and exact live native acceptance integrated; public first dispatch/snapshot/routing, FIFO continuation, explicit Resume and selected-input Steer integrated; public approval response delivery, exact live permission acceptance and pinned single-use approval execution evidence integrated; automatic same-connection inspection after uncertain response delivery and retained completion recovery after Worker restart integrated; paired Worker Local origin and existing-checkout native execution integrated; other approval acceptance/history recovery and forks remain pending | Real SQLite/Connect/CLI acceptance/restart/replay, concurrent FIFO order, mode preservation, scoped pages, byte bounds, stale/cross-session edit denial, archive visibility/paused restore and uncertainty refusal; actual private Worker first-turn execution/completion and revocation verified with installed Codex and seeded readiness, and public CLI Execute/Plan first dispatch, FIFO follow-ups, Stop/Resume, selected-input Steer and post-Steer continuation pass with actual discovery and keyless loopback validation without seeded readiness |
| Schedules, overlap/skip/wait, durable occurrences | Calendar/storage, bounded coordinator and owner/client ScheduleService/CLI implemented, including atomic independent sessions, Overlap/Skip/Wait, offline history, cleanup-gated FIFO, Local origin and reference-deletion disabling; Project/Agent deletion preserves retained session execution under final project restrictions; remaining native harness/platform acceptance pending | Calendar/SQLite/coordinator/Connect/CLI fixtures cover timezone/DST, receipts, rollback, capacity/FIFO/history after deletion/restart, role/provenance and cursor gates; installed macOS Codex manual Run now verifies native Worktree Execute and Local Plan; a separate actual cron timer verifies Worktree Execute, independent history after deletion and continuation, all with a keyless scripted provider |
| Terminal/files/diff/reviews/Sidechat/forwarding | Pending | Pending |
| GitHub PAT/query/PR evidence/remediation | Pending | Real GitHub integration not validated |
| Usage/costs/budgets, diagnostics/doctor | Immutable attributed Codex native token observations and redacted notice publication implemented; usage aggregation/reset/fork/subagent reconciliation, dashboard, costs, budgets and full diagnostics pending | Private native Codex/server/Worker-outbox composition and real Connect/SQLite replay/reset/attribution tests; no billable-cost or aggregate-usage claim |
| Search/activity/inbox, config import/export | Native question/approval/terminal inbox persistence and independent read-state RPC/CLI with joined current-source/session inspection and bounded pagination implemented; notification delivery, search/activity and import/export remain pending | Atomic publication, source uniqueness, read-state isolation and migration/reopen fixtures; native evidence recorded below |
| Deletion/managed backups/offline cleanup/storage | Pending | Pending |
| CLI build/distribution and OS lifecycle | Pending | Native Windows/Linux evidence pending |

## Presentation scope
The current user request is the CLI. Desktop windows/tray/widgets/native browser presentation and desktop artifact installation belong to the full product issue and remain visible in the complete requirements; they are not claimed as CLI implementation evidence. Their server-owned data and product operations remain within the CLI/RPC scope.

## Local verification log
- 2026-09-24, macOS arm64, Go 1.26.1: `go test -race ./cmds/delidev-cli/internal/...` and `go vet ./cmds/delidev-cli/internal/...` for the domain, private-files, and SQLite foundation. No harness or provider account was invoked.
- 2026-09-24: `go test -race ./cmds/delidev-cli/...`, package vet, Buf formatting/lint, and generated Go bindings validated. The native macOS executable was exercised against an isolated temporary data directory. Linux arm64 cross-compilation succeeded; this does not establish native Linux behavior.
- 2026-09-24: Windows amd64 cross-compilation succeeded. Native Windows runtime remains unverified. Real temporary Git preparation tests and package race tests/vet pass on macOS arm64.
- A subsequent Git-registration regression exposed the macOS `/var` parent alias during rollback. Worker roots now canonicalize before ownership journals are created; the real Git regression and package race/vet checks were rerun after the fix.

## Native interface discovery (not execution validation)
- Installed read-only version/help checks: Codex CLI `0.151.0`, Claude Code `2.1.236`, OpenCode `1.18.20`. No `grok` executable was found on this machine. No user credential files were read and no inference was invoked by these checks.
- Official adapter references retrieved: [Codex app-server](https://learn.chatgpt.com/docs/app-server) for native expected-turn steering; [Claude Code programmatic execution](https://code.claude.com/docs/en/headless) for structured streams; [OpenCode server](https://opencode.ai/docs/server/) for authenticated native HTTP; [Grok Build headless/ACP](https://docs.x.ai/build/cli/headless-scripting) for native JSON-RPC integration. Installed-version capability validation and real-account evidence remain required before claiming an adapter supported end to end.

- Worker schema v2 migration preserves a consistent private v1 backup before changes; real SQLite tests cover successful upgrade and rollback after a conflicting migration. Revocation tests reject both new mutations and receipt replays from a previously authenticated principal.

- CLI integration tests pair both a Worker and a client, reuse pairing receipts, inspect a real Git subdirectory over outbound Connect, and reject Worker credentials for owner resource queries. Package race tests and vet pass after this integration.

- Native macOS CLI subprocess smoke passed for explicit server startup, private Worker pairing, outbound readiness, real Git inspection, asynchronous repository save, and revocation shutting down the Worker with a typed authentication error. No network fetch, inference or user account access was involved.

- Revocation now atomically cancels undispatched Worker jobs and preserves claimed-job uncertainty; real Connect regressions cover both states. Migration backup names are synchronized before schema commit. Final package race tests/vet and generated-binding freshness checks pass; updated Worker CLI cross-compiles for Windows amd64 and Linux arm64 without implying native runtime validation.

- Native macOS process tests cover start-before-side-effect barriers, separate stdout/stderr, byte-split UTF-8 stdin, blocked-input cancellation, daemonized descendants after natural exit/cancel/disconnect, supervisor-death recovery, cross-owner refusal, unrelated live scopes, missing journals, output-consumer failure and secret-free ownership journals. Worker Git now uses these scopes and retains cleanup uncertainty. Linux/Windows process test binaries cross-compile; native Linux/Windows lifecycle and PTY behavior remain unverified.

- Worker discovery now runs end to end through `machine discover`, outbound jobs and atomic machine publication. Real Connect tests cover authorization, exact request/result retries, stale discovery generations, independent attachment revisions, unverified-capability rejection, safe diagnostic reconstruction and rollback after a wrong job revision. Temporary native process fixtures verify environment isolation, explicit-path precedence, missing/permission/incompatible outcomes, bounded raw output, cancellation and confirmed runtime cleanup. Native launch errors retain safe OS classifications.
- Native macOS arm64 CLI/server/Worker smoke, isolated temporary server and Worker directories: version discovery observed Codex `0.151.0`, Claude Code `2.1.236`, OpenCode `1.18.20`, and missing Grok Build. All observations correctly retained `protocol_verified=false`. No account login, inference, native protocol handshake, or model availability claim follows from this smoke. Probe processes and private runtime directories were cleaned after native ownership reconciliation.

- After discovery integration: complete delidev Go race tests and vet pass, Buf lint passes, and regeneration reproduces all bindings exactly. Windows amd64 and Linux arm64 CLI builds and platform test-binary cross-compilation pass; native Windows/Linux discovery and lifecycle remain unverified. No repository-owned generated `dist` directory remains.

- Native JSON-RPC fixtures cover split Unicode frames, typed/redacted native errors, concurrent interaction replies, pre-send cancellation, uncertain request delivery and late acknowledgments, blocked input, malformed/bounded frames, slow-consumer overflow, and confirmed owned-process cleanup. Protocol-profile tests reject changed native versions, foreign homes/platforms, extra response fields and preexisting loaded threads without invoking inference.
- Native macOS arm64 CLI/server/Worker `machine discover --protocol` smoke successfully verified Codex app-server `0.151.0` initialization and empty bounded loaded-thread state. Actual native testing exposed the optional `emittedAtMs` notification metadata and `nextCursor` response field; both are now represented and covered by fixtures. The final probe left zero private probe directories after native reconciliation. Execution capabilities remain empty, and no account login, thread creation, model refresh or inference was performed. Claude Code/OpenCode protocol profiles and missing Grok remain explicitly unverified.

- After native protocol integration, complete delidev race tests and vet pass; the legacy discovery-receipt regression also passes after closing/reopening real SQLite. Buf lint and exact regeneration checks pass. Updated Windows amd64/Linux arm64 CLI binaries and native protocol test binaries cross-compile, without claiming native Windows/Linux or account execution verification. No repository-owned generated `dist` directory remains.

- Protected credential storage now implements native macOS Security.framework, Windows Credential Manager and Linux Secret Service wrapping-key adapters, with AES-256-GCM private envelopes, domain-separated commitment keys, durable staging/deletion markers and exact immutable retries. It is a storage primitive; account lifecycle RPC/CLI integration, upstream validation, login, proxy authorization and execution revocation remain pending.
- Native macOS arm64 tests on macOS 26.6.2 passed against a newly created temporary keychain, including duplicate refusal, locked-store failure without UI, temporary-keychain unlock, native deletion and larger encrypted payloads. Testing identified that file-keychain operations need the process-wide no-UI setting as well as the query option. No default/login keychain credentials were read.
- Native Linux arm64 credential tests passed in a network-disabled disposable Debian/GNOME Keyring container with a non-root user, private home and private D-Bus session. Real native testing exposed GNOME Keyring's `text/plain` metadata for binary values; the adapter now preserves and validates the binary payload independently of MIME. Actual create/read/duplicate/delete, larger envelopes and locked collection/deletion-marker behavior passed. This proves the Secret Service adapter in that container, not complete Linux desktop/Worker lifecycle acceptance.
- After credential storage changes, the complete delidev Go race suite and vet pass. Windows amd64/arm64, Linux amd64 and macOS amd64 credential test binaries cross-compile; native Windows execution remains unverified. Tests cover cancellation after a native write, uncertain writes before/after acceptance, restart reuse, conflicting concurrent writes, missing/corrupt keys and records, private-file rejection and deletion resurrection prevention. No repository-owned generated `dist` output was produced.

- API/keyless account lifecycle is integrated through authenticated Connect and the CLI. Real temporary SQLite/HTTP tests cover exact and conflicting retries, restart, concurrent connects, in-flight client revocation, uncertain native writes, orphan-intent cleanup, blocked reconnect/deletion, preserved concurrent metadata edits, and old receipt replay after reconnect/deletion. Failure injection uses a test-only secret backend; stored credentials stay unverified and cannot route.
- A separately compiled Linux arm64 CLI/server passed its complete credential lifecycle in a non-root, network-disabled disposable Debian/GNOME Keyring container: API key through stdin, protected native storage, server restart and receipt replay, disconnect/cleanup, account deletion and resurrection prevention. Retained server files and CLI output contained neither raw nor Base64-encoded test credentials. No provider, model or inference request was made; native Windows/macOS account-command composition remains unverified.
- After account lifecycle integration, `go test -race ./cmds/delidev-cli/...`, `go vet ./cmds/delidev-cli/...`, Buf lint and exact generated-binding reproduction pass. The updated CLI and server tests cross-compile for Windows amd64/arm64, Linux amd64 and macOS amd64; cross-compilation does not establish runtime acceptance.

- Bounded provider inspection now backs `account validate` with server-owned key retrieval, exact authority/authentication evidence, model pagination, no redirects/retries, sanitized failure classes and durable observations. Real HTTP/Connect/SQLite fixtures cover restart/replay, failed observation replay, independent accounts during a blocked check and disconnect cancellation. Public/custom catalog success remains explicitly unverified for credential validity. No actual provider-account or inference verification is claimed.
- The rebuilt Linux arm64 CLI/server passed the network-disabled native Secret Service fixture with protected-key retrieval for an in-container model-list endpoint, unsupported credential-evidence output, and exact validation replay without another request. Full delidev Go race tests/vet passed; subsequent URL-validation changes passed the affected domain/provider/server race tests and package vet.
- After provider validation integration, Buf lint and exact Go-binding regeneration pass. Updated CLI binaries cross-compile for Windows amd64/arm64, Linux amd64 and macOS amd64. These builds do not establish native remote TLS, real account authentication or harness execution evidence.

- Provider/catalog integration adds nine editable presets, periodic/explicit account-bound discovery, atomic model publication, advisory metadata provenance, NEW acknowledgement, manual/display preservation, deletion suppression and indexed search/canonical resolution. Real HTTP/Connect/SQLite tests cover OpenRouter pagination and malformed/reflected metadata, accepted failure/restart replay, unchanged-model revisions, user-declared overrides, ambiguous/hidden selectors, cursor filter/tamper/epoch behavior, Worker denial, discovery disablement and automatic shutdown cancellation. Schema v3 migration tests preserve original v2 data/receipts and synchronized backups, and roll back both canonical-ID and alias conflicts without rewriting legacy records.
- The actual Linux arm64 CLI/server passed its expanded native Secret Service fixture in the non-root network-disabled disposable container: native-key automatic discovery after enabling the provider, model search/resolve, explicit discovery, restart without repeating an accepted request, failed discovery output/replay and final disconnect/deletion. No external provider, user account or inference endpoint was contacted. Complete delidev race tests/vet passed; subsequent catalog logging/canonical-resolution changes passed affected server/store race tests. Buf formatting/lint and exact reproduction of all 32 generated files pass; CLI cross-builds for Windows amd64/arm64, Linux amd64 and macOS amd64 pass after the final portable refinements. Native Windows and macOS catalog command composition remains unverified.

- The internal API relay now covers five scoped native operations, exact model/connection binding, direct verified TLS, cancellation, protocol terminal states, redacted native errors, owned-reference callbacks and split-delta protected-value checks. It has no production router/authority integration yet. Real loopback HTTP tests passed on macOS arm64 and in the non-root network-disabled Linux arm64 container; these use fixture authority/provider data, not real account inference. Complete delidev race tests/vet pass; proxy test binaries cross-compile for Windows amd64/arm64, Linux amd64 and macOS amd64. Durable token issuance/revocation, reference storage and real harness acceptance remain pending.

- Session acceptance now atomically persists the session and first queued input without freezing Agent configuration or claiming native execution. Owner/client Connect and CLI support bounded ordered enqueue, content edits/removal with reference-only receipts, lists, rename and never-executed Stop/Archive/Restore. Current blocked state and unsupported Resume are explicit. Real SQLite/Connect/CLI tests cover restart, concurrent acceptance, receipt reuse after edits/removal, immutable mode, cross-session/stale input denial, byte-bounded pages, Worker denial and independent archive/outcome/pause. Schema v4 tests validate v3 backup preservation and rollback on ambiguous queue order. Live native session/Local-origin/first-dispatch authority remain pending.
- After session integration, complete delidev race tests/vet, Buf formatting/lint and exact reproduction of all 32 generated files pass. The Linux arm64 CLI test binary passed create/queue/edit/remove/archive/restore/unsupported-Resume through real temporary Connect/SQLite in the non-root network-disabled container. Windows amd64/arm64, Linux amd64 and macOS amd64 CLI cross-builds pass; this is not live native harness or native Windows acceptance. The additional old-create-receipt-after-removal regression passed under the race detector. No repository-owned `dist` output was generated.

- Worker stream reception now remains active during owned native work, with one unresolved server assignment and a 45-second heartbeat deadline. Native macOS controlled-process tests prove cancellation after permission revocation, unavailable/clean stream termination and silent-connection expiry, with a durable canceled journal and no successful report. Discovery regression verifies a newer generation remains queued while the previous native job is claimed and still rejects the stale result. Complete delidev race tests/vet pass; the final silence/termination regression also passes independently. This fixes a lifecycle prerequisite for workspace/session dispatch, without claiming those integrations complete.
- The rebuilt Linux arm64 Worker test binary passed all four live-process stream-termination/heartbeat scenarios in the non-root network-disabled container. Windows amd64 CLI cross-compilation passed; native Windows cancellation remains unverified. No harness inference, user credential access or repository-owned `dist` generation occurred.

- Session creation now atomically accepts its ordered workspace preparation request and first input. Real macOS CLI/server/Worker/Git tests verify General Chat preparation, detached two-repository commit/primary selection, explicit retry after queued cancellation, receipt reuse and ready-file retention across Archive. Connect/SQLite tests cover claimed-job cancellation without envelope revision changes, independent following jobs, pending Archive until cancellation confirmation, malformed-success uncertainty, Worker denial and restart replay. Remote-manifest fixtures verify identity/digest/reference/commit/path/ownership rejection and Windows path grammar independently of the server OS.
- Schema v5 preserves a private validated v4 backup and durable cancellation across restart. Native controlled-process tests verify both cancellation during work and pre-cancellation before any process starts; the same stream then prepares a different General Chat job despite a delayed old cancellation. Complete delidev Go race tests/vet, Buf formatting/lint and exact reproduction of all 32 generated files pass. This increment prepares workspaces, not native harness conversations; first-dispatch snapshots/routing, uncertain-preparation reconciliation and full harness lifecycle remain pending.
- The Linux arm64 Worker binary passed running/pre-start targeted cancellation and subsequent General Chat preparation in the non-root network-disabled disposable container. The container has no Git, so no new native Linux multi-repository CLI claim is made. Windows amd64 CLI cross-compilation passes; native Windows workspace/cancellation remains unverified. No user credential, provider inference or repository-owned `dist` generation occurred.

- Preparation recovery now has authenticated RPC/CLI, immutable claimed-assignment evidence, original Worker journal matching, bounded native reconciliation and atomic session/original-job publication. Explicit partial cleanup retains a private proof outside the removed tree; ready working files survive recovery and pending Archive. Real macOS replacement-Worker/Connect/SQLite tests cover lost completion, partial cleanup, digest mismatch/refusal and repaired explicit retry, original journal retention, exact receipt replay and targeted cancellation of recovery. Native Git fixtures verify detached commit/common-directory/registration checks, ready-change preservation, partial Git cleanup, post-removal interruption and replacement-link refusal.
- Schema v6 migration preserves the original v5 database backup, recovers a legacy uncertain claim from its durable receipt and retains its exact revision/body after mutable result changes. Job deletion removes the retained assignment. Complete delidev race tests/vet pass; additional assignment-migration and ownership-link tests pass under the race detector. Buf formatting/lint and exact reproduction of all 32 generated files pass. Native harness execution, conversation recovery, snapshot-based inactive workspace cleanup and full session deletion remain pending.
- The rebuilt Linux arm64 server/Worker test binary passed all three replacement-Worker recovery cases (ready, partial General Chat cleanup and mismatched-journal explicit retry) plus targeted recovery cancellation in the non-root network-disabled container. That fixture has no Git, so Linux Git-worktree recovery is not claimed. Windows amd64 CLI cross-compilation passes; native Windows recovery remains unverified.

- Codex internal thread control now separates stable discovery from explicit thread-mode native requests, pins legacy history without model fallback, applies additive instructions and validates effective model/provider/effort/tier/cwd/permissions/approval. Fixtures cover no-send unsupported selections, retained native defaults, root-thread serialization, canceled waiters, foreign resumed identities, changed/malformed replies, redacted errors and late acknowledgments without automatic retry. This is not yet public session dispatch or immutable first-execution snapshot/account routing integration.
- Native macOS arm64 Codex `0.151.0` testing exposed observation fields absent from the stable generated schema, native default paginated history and the native rule that no resumable rollout exists before the first user input. The adapter validates this exact release's full generated schema observations and explicitly selects legacy history. The opt-in test then passed creation, exact model/effort/base-plus-additive instructions, one local scripted Responses exchange, metadata inspection, owned process closure and resume of the same native identity. No external provider or user credential was used; full native turn controls, real account fidelity and other harnesses remain unverified.
- Complete delidev Go race tests and vet pass. The final inspection-authority refinement also passes the affected harness race suite and native Linux arm64 fixture. All native thread-control fixtures passed in the non-root network-disabled Linux container; this does not prove an installed Linux Codex runtime or real account. Windows amd64 CLI cross-compilation passes, with native Windows execution still unverified. No protocol schema or generated bindings changed, and no repository-owned `dist` output was generated.

- Codex internal turns now preserve immutable effective settings and input identity/content/turn bindings, use native Execute/Plan modes, require exact active-turn Steer and keep interruption separate from terminal/cleanup proof. Core message/lifecycle events normalize without raw error bodies; rich/unknown/subagent extensions remain private and JSON-excluded. Controlled native fixtures cover rejected and uncertain sends, late acknowledgments, no fallback/replay, paused Stop/failure, canceled-reader retention, terminal conflict and buffered events after connection failure. Public dispatch, durable transcripts, first-execution snapshots and selected-account routing remain pending.
- Native macOS arm64 Codex `0.151.0` passes the private local-provider suite through the new adapter methods: Execute and persisted-thread resume, native Plan, same-turn Steer with both original input identities, and native interrupted outcome. Interruption alone did not close the pending model HTTP request; owned process closure did, and the test requires that cleanup before completion. These scripted loopback exchanges use no external inference provider or user credentials and do not validate tools, interactions, usage or real account fidelity.
- Complete delidev Go race tests and vet pass; subsequent input-correlation and connection-failure refinements pass the affected harness race suite, the explicit native suite and final package vet. The new content/owning-turn regression also passes under the race detector. All controlled Codex adapter fixtures passed on native Linux arm64 in the non-root network-disabled container, with installed-harness tests skipped; installed Linux Codex and native Windows execution remain unverified. Final Windows amd64 CLI cross-compilation passes. No protocol schema/bindings changed or repository-owned `dist` output was generated.

- The private initial-execution transaction primitive now resolves current exact configuration/templates, selects an eligible account using durable routing state, and commits the immutable configuration/digest, original account/connection, full route, first input/native request identities and routing advancement together. Production sessions remain blocked: no public caller supplies native readiness or dispatches these claims yet. No account secret, native process, proxy credential or inference request is created by the primitive.
- Real temporary SQLite tests pass for concurrent rotation, failed-publication rollback, current project restrictions/revoked accounts, queue order, edit/claim races, creation-to-dispatch template edits, immutable replay after restart and contradictory routing owners. Exact Unicode/whitespace/options and instruction-overflow tests pass. The full delidev Go race suite and vet pass; all new store execution tests also pass in the non-root network-disabled Linux arm64 container. Readiness/accounts/workspaces in these tests are simulated records and are not native execution evidence. No protocol schema/bindings changed or repository-owned `dist` output was generated.

- The server now registers the native relay with digest-only Worker execution grants bound to exact claimed ownership and the server process epoch. Real authenticated Connect/SQLite/loopback HTTP fixtures cover immutable registration/replay, foreign-reference denial, job/session/Worker/device/account revocation, replaced connections/instances, expired heartbeat, server restart denial and account disconnect joining in-flight relay cleanup before key deletion. These tests seed simulated native-ready assignments; public session execution and actual Worker token delivery remain pending, and no installed harness or real account was used.
- Schema v7 adds execution grant/reference indexes, immutable bindings and deletion cascades. New SQLite tests preserve v6 assignments and synchronized pre-migration backups, retain exact reference scopes across restart, reject binding replacement and remove owned authority on deletion. The full delidev race suite passes, final affected server/store race tests and package vet pass, Buf lint passes, and all 32 generated files reproduce exactly. No repository-owned `dist` was generated.

- Codex API configuration now pins the paired-origin proxy route through native session flags, supplies only an execution token in the explicit environment, excludes it from child shells and verifies effective provider authority before binding a thread. Closed validation rejects alternate endpoints/keys/auth/header/query authority while preserving native retry defaults. The pinned config reader serializes all optional provider fields as null; the adapter accounts for that observed behavior without discarding populated authority.
- Opt-in macOS arm64 Codex `0.151.0` tests pass direct private proxy configuration and actual authenticated server registration/relay composition into a scripted loopback provider: exact selected model/input/output, server-only upstream key injection, one request, owned native closure, and no raw execution/upstream token in retained native files. Job/readiness records in the server test are simulated; public CLI/Worker dispatch and external-account inference are not established. Existing native Execute/Plan/Steer/interruption tests also pass. Affected harness/proxy/server race tests and package vet pass; ordinary tests never launch installed harnesses.

- `PublishExecution` now connects typed native core events to atomic server transcript/session state: exact Worker/job/input ownership, monotonic sequence, distinct observed settings, one-time queue-capacity release, unique native message mapping, bounded text and immutable terminal facts. Worker publication synchronizes a locked outbox before RPC and replays only the same event identity after a lost acknowledgment; new events cannot replace pending uncertainty. Core Codex normalization supplies stable product message IDs and explicitly reports unsupported extensions as unhandled. Public dispatch/job execution remains pending.
- Real Connect/SQLite tests cover concurrent receipt replay, restart without duplicated deltas, scope/sequence rejection, native-item duplication rollback, partial-message failure, completed-message gates, bounded accumulation without truncation, late completion after Stop/Archive, and Worker outbox recovery after deliberately lost acknowledgment. Schema v8 migration preserves v7 execution grants and backups. The expanded opt-in macOS arm64 Codex `0.151.0` test passes the actual relay plus Worker core-event outbox plus server transcript, with exact input/model/native identities and no persisted native credential. Readiness is simulated and rich events remain unimplemented; no external provider or user account was used.
- The complete delidev Go race suite passes; package vet passes after replacing test-only protobuf value copies with `proto.Clone`. Buf lint and exact reproduction of all 32 generated files pass. Native Linux arm64 server tests for grants/revocation/core publication/outbox replay and store migration/reference ownership pass in the non-root network-disabled container. Those Linux tests use controlled native-readiness records and no installed harness. No repository-owned `dist` was generated.

- Codex native metadata now validates root identity, exact effective settings and active mode, rejects remote-control activation/model rerouting, and publishes redacted warning kinds. Native token usage retains cumulative versus last-request values, unavailable fields and assignment-derived event-time attribution as immutable observations; it does not compute charges, actual costs or aggregate usage. Populated quota, tools/interactions and other native extensions remain explicitly unhandled. The installed source's context-limit counter filling/reset behavior is documented rather than treated as provider-billed usage.
- New native/Connect/SQLite regressions cover malformed/missing/negative/overflow counters, counter resets, immutable usage IDs and provenance, exact lost-acknowledgment replay, forbidden cross-kind payloads, foreign thread/turn ownership, settings/mode drift, notice redaction and unavailable quota. Complete delidev Go race tests and vet pass. The macOS arm64 Codex `0.151.0` private suite still passes thread persistence/resume, Execute/Plan/Steer/interruption; the actual server-relay/outbox smoke handles every event in its simple scripted turn and stores exact token observations. Native readiness remains seeded in that composition; ordinary CLI/Worker dispatch, external accounts and full event-family acceptance are not established. No protocol schema/generated bindings or repository-owned `dist` output changed.

- A private first-execution workspace lease now validates exact manifest/native Git ownership, preserves uncommitted files, retains the session preparation/recovery lock and synchronizes a separate durable execution claim. Preparation/recovery reject unresolved claims after Worker lock loss, even when workspace files are missing. Owned execution-job cleanup closes the claim; a missing process index retains uncertainty. Temporary real Git/process tests cover concurrent claims, unchanged source checkout, dirty worktree preservation, changed manifest/HEAD/refusal, forged owned-path suffixes, existing owner refusal, cancellation, Worker-loss/missing-workspace behavior, closed-claim non-replay and idempotent native cleanup. This is not ordinary Worker dispatch, Local execution, later-turn/resume or native recovery integration.
- After workspace lease integration, the complete delidev Go race suite and vet pass. The lease tests, including real Git and owned process cleanup, also pass on native Linux arm64 as a non-root user in a network-disabled disposable `node:24-bookworm` container (the existing Secret Service image lacked Git, so its initial Git case could not run). Native usage/notice publication and lost-acknowledgment tests pass separately in the non-root network-disabled Secret Service container. The current Windows amd64 CLI cross-build passes; no native Windows or installed Linux Codex execution is claimed. No protocol schema/generated bindings or repository-owned `dist` output changed.

- The actual outbound Worker loop now executes an immutable first Codex assignment through exact executable selection, retained private runtime, prepared-workspace lease, digest-only grant registration, native thread/input acceptance, ordered typed publication and owned terminal cleanup reporting. The server verifies completion against the exact retained native identities/cursor/outcome, uses reference-only receipts and preserves uncertainty on missing proof. Worker replacement/revocation atomically propagates session/input recovery without rewriting prior terminal observations or freeing unacknowledged input.
- Opt-in macOS arm64 Codex `0.151.0` tests pass under the race detector for the complete real Worker attach/job loop into a scripted loopback provider, exact model/input/transcript, server-only key injection, no bearer in retained runtime, native/process-lease cleanup and completion report. Live revocation during inference also cancels HTTP/native work, joins process cleanup and retains the interrupted journal/server recovery without replay. First-selection/account readiness remains seeded; these tests do not establish public CLI dispatch, real provider-account inference, rich event support or other installed-harness platforms.
- Complete delidev Go race tests and vet pass after Worker integration. Completion proof/replay and Worker-loss state regressions also pass in the non-root network-disabled Linux arm64 Secret Service container. The Windows amd64 CLI cross-build passes, without claiming native Windows execution. No protocol schema/generated bindings or repository-owned `dist` output changed.

- Account disconnect now atomically pauses and requests cancellation of every unfinished native assignment selected on that account, with bounded pagination across queued/claimed/uncertain states. Undispatched jobs become canceled, while claimed revisions/digests remain unchanged for Worker journals and reconnect. Other accounts, candidate-only routing links and terminal history are excluded. Old request replay preserves executions and credentials on a replacement connection. Relay/key cleanup remains distinct from owned Worker cleanup and retained session recovery.
- Real Connect/SQLite regressions pass for more than one cancellation page, immutable claimed envelopes, preserved queue capacity/ownership, excluded historical/other-account jobs and replay after reconnecting the account. An opt-in macOS arm64 installed Codex test passes under the race detector: account disconnect during a scripted loopback inference cancels the actual Worker process, closes the HTTP request, journals/reports the interrupted result and retains server recovery without replay. Readiness is seeded; no external inference or user account was used. The full delidev race suite and vet pass.
- The multi-page account execution cancellation/replacement-receipt regression also passes on native Linux arm64 in the non-root network-disabled Secret Service container. No protocol schema/generated bindings or repository-owned `dist` output changed.

- First-native session Stop/Archive now resolve unique job ownership, atomically pause/request cancellation and retain exact claimed envelopes. The Worker retains one interruption request before native send and allows a 15-second terminal-publication grace period only under the original live stream after durable input binding; stream loss and earlier cancellation still kill immediately. Verified terminal/process cleanup completes pending Archive for the current agent-only resource graph; completed outcomes and paused Restore survive old control receipt replay. Unconfirmed acceptance/cleanup remains recoverable and never authorizes resend.
- Real Connect/SQLite tests pass for running/unaccepted/terminal/cleaned success/failure controls, exact receipt replay, immutable job content, Archive waiting for cleanup, missing-proof recovery and preserved outcome/paused Restore. The complete delidev race suite and vet pass. The control regressions also pass in the non-root network-disabled Linux arm64 Secret Service container, and the Windows amd64 CLI cross-build passes.
- Opt-in macOS arm64 installed Codex `0.151.0` Worker tests pass under the race detector for normal completion, Worker revocation, account disconnect, explicit Stop and Archive during a scripted loopback inference. Stop/Archive publish native terminal facts, prove owned cleanup and retain correct visibility/pause; lost Worker authority retains recovery. Readiness is seeded and no external inference/user account is used. Public creation/dispatch, richer event families, later turns and explicit native recovery remain pending. No protocol schema/generated bindings or repository-owned `dist` output changed.

- The pinned Codex parser now retains closed command/file-change lifecycle observations, streamed command output, native terminal-input observations and patch updates. It preserves nullable counters, advisory source/action/plugin metadata and aggregate output separately from deltas; tool failure/decline is not whole-turn failure. Cross-kind payloads, unknown variants and mismatched lifecycle ownership are rejected. Durable product publication remains pending, so production execution still treats these typed events as unhandled rather than dropping them.
- Native-wire fixtures pass under the race detector for nullable/default fields, invalid unions, foreign-thread privacy, lifecycle mismatch and distinct update families. The complete delidev Go race suite and vet pass; the final source-null refinement passes the targeted race tests. The same final parser fixtures pass in the non-root network-disabled Linux arm64 Secret Service container. These parser fixtures do not invoke an installed harness or prove native tool execution. No protocol schema/generated bindings or repository-owned `dist` output changed.

- Codex command/patch observations now publish through the durable Worker outbox into dedicated tool transcript records. Shared native-item uniqueness and completion gates prevent identity replacement, partial-tool success and late writes. Native start/completion snapshots, nullable streamed output, final aggregate output, input observations and patch revisions remain distinct; tool failure/decline never implies whole-turn failure. Complete publication/document/history bounds reject updates atomically without truncating retained content or consuming sequence.
- Real Connect/SQLite regressions pass for native-to-product mapping, advisory metadata, nullable counters, patch revisions, whole-turn outcome independence, duplicate/cross-family identity, wrong scope/phase/payload, immutable command/source, completion gates, output/document limits and exact outbox restart replay after a lost acknowledgment. The full delidev race suite and vet pass. These regressions also pass in the non-root network-disabled Linux arm64 Secret Service container; the Windows amd64 CLI cross-build passes.
- An opt-in macOS arm64 installed Codex `0.151.0` Worker test now verifies a real private POSIX `printf` command, its result returned to the same scripted loopback provider, server-retained native command/exit/output and verified owned cleanup. The observed short command emits final aggregate output without output deltas, so the product retains the stream as unavailable rather than manufacturing an empty or copied stream. Existing Worker completion/revocation/account-disconnect/Stop/Archive native tests also pass under the race detector. Account readiness/first selection remain seeded; no external provider or user account was used. Native file-patch execution, interactive command streaming, other installed-harness platforms and public dispatch remain unverified. No protocol schema/generated bindings or repository-owned `dist` output changed.

- The pinned Codex parser now distinguishes native plan/reasoning lifecycle, plan text deltas, indexed reasoning summary/content updates, structured turn-plan steps and aggregated turn diffs. It preserves authoritative completed plan replacement, native part indices/defaults and absent data without inferring turn success or filesystem/review authority. Unknown/malformed/cross-kind payloads fail; foreign observations remain private and late events retain their classification. Dedicated durable publication for these new families remains pending.
- Artifact parser fixtures pass under the race detector and in the non-root network-disabled Linux arm64 Secret Service container; package vet passes. The concurrent full race run hit an existing Worker subprocess-start five-second deadline while unrelated host build activity was present. The isolated Worker termination regression passed without code changes, followed by the complete race suite with package execution serialized (`-p 1`). These fixtures do not invoke an installed harness or establish native artifact acceptance. No protocol schema/generated bindings or repository-owned `dist` output changed.

- Native plan/reasoning artifacts now publish through the durable outbox and shared item-identity/completion index. Start snapshots, indexed/ordered stream deltas and authoritative completion remain separate, including final plan replacement and explicit empty versus missing reasoning content. Turn-plan/diff updates create immutable progress transcript records without native item identities; latest pointers never rewrite history or grant review/filesystem/terminal authority. Publication/document/history bounds fail atomically without truncating content.
- Real Connect/SQLite tests pass for artifact/progress mapping, exact plan replacement, reasoning index preservation, immutable plan/diff history, empty diff observations, null-content refusal, cross-family identity/scope/kind/payload rejection, incomplete-item success gates, size bounds and exact outbox reopen/replay after a lost delta acknowledgment. The full delidev race suite passes with package execution serialized (`-p 1`), and vet passes. Artifact/progress server regressions pass on native Linux arm64 in the non-root network-disabled Secret Service container; the Windows amd64 CLI cross-build passes.
- An opt-in macOS arm64 installed Codex `0.151.0` Worker test now invokes native `update_plan` against the scripted loopback provider, retains exact plan explanation/step/status and verifies native terminal publication plus owned cleanup. The complete installed Worker suite also passes under the race detector for normal completion, command execution, revocation, account disconnect, Stop and Archive. Account readiness/initial selection remain seeded; no external inference/user account is used. Final Plan artifact, reasoning and turn-diff native acceptance remain fixture-only, and public dispatch/native recovery are still pending. No protocol schema/generated bindings or repository-owned `dist` output changed.

- The pinned Codex parser now normalizes root `request_user_input` server requests with distinct numeric/text native identities, per-arrival UUIDs, exact questions/options and blocking/freeform/secret semantics. Deprecated auto-resolution timing is retained without creating timers or answers. Original raw pipe-reply authority remains private. Malformed/ambiguous/cross-kind data and foreign-turn ownership are rejected; foreign-thread requests remain private. Response delivery, resolution, protected secret answers, durable interaction/inbox storage and public RPC/CLI remain pending; production execution still stops on this unhandled typed family.
- Question fixtures pass under the race detector for identity namespaces, arrival binding, nullable options, default/null flags, exact content, secret/freeform metadata, wrong-kind fields and malformed requests. They also pass in the non-root network-disabled Linux arm64 Secret Service container. The complete delidev race suite passes with package execution serialized (`-p 1`), and vet passes. These are adapter/schema fixtures only, without installed-harness question or approval execution. No protocol schema/generated bindings or repository-owned `dist` output changed.

- The native wire now has exact-arrival request retirement for adapter-observed resolution/cancellation. It sends no answer, rejects replacement tokens and in-flight response ownership, and distinguishes an already absent request without claiming semantic acceptance. Real owned-process fixtures verify that retired requests cannot reply, numeric-ID reuse retains a new arrival, stale retirement cannot delete it and the only transmitted response belongs to the newer request. In-flight ownership tests pass under the race detector.
- Affected harness/Worker/server race suites and delidev vet pass. The retirement fixtures also pass in the non-root network-disabled Linux arm64 Secret Service container. Pinned source inspection confirms Codex resolves a question request before parsing/submitting its answer and also resolves failure/cancellation; docs now forbid treating that notification as acceptance. Codex response/resolution integration remains pending. No protocol schema/generated bindings or repository-owned `dist` output changed.

- The private Codex adapter now retains immutable question ownership, validates typed non-secret replies, serializes concurrent responses and normalizes exact native closure separately from transmission. Stop/turn completion/late arrivals cannot reply; request-ID reuse is rejected because native resolution has no arrival token. Original question payloads are bounded and released at closure. Memory-only answers are excluded from ordinary serialization, and secret-marked requests refuse the ordinary response path before claiming/sending. Durable Worker/server interaction publication, protected secret responses and public inbox/CLI remain pending.
- Owned-process regressions pass for concurrent replies, caller-mutated options/secret flags, wrong turn, numeric/text ID separation, foreign/unknown resolution, closure without answers, Stop/terminal/late refusal, native-ID reuse, pre-send cancellation, response shape/size limits and retained-identity/question-memory bounds. A deliberately blocked native pipe verifies retained response identity and recovery after uncertain delivery, with no replay. The full delidev race suite passes with package execution serialized (`-p 1`), and vet passes. The question regressions also pass on native Linux arm64 in the non-root network-disabled Secret Service container.
- An opt-in macOS arm64 installed Codex `0.151.0` test receives a native Plan-mode question, transmits one answer, observes request closure, confirms the exact answer in the next scripted loopback model request and proves owned cleanup. No external account/provider is used. The native profile emits no question `functionCallOutput` item in this test; closure is therefore retained as transmission/closure evidence without falsely clearing the adapter's acceptance gate. Native acceptance/history reconciliation and other installed platforms remain unverified. No protocol schema/generated bindings or repository-owned `dist` output changed.

- Codex question requests, native waiting changes and unanswered closure now publish through the synchronized Worker outbox into server interaction records. Original question content survives native/terminal closure, and waiting-clear never answers or resolves a request. Schema v9 adds execution-scoped unique native request ownership and bounded open-request count/bytes, with synchronized backups preserving v8 message history/indexes and earlier migrations. Native transmitted response publication is explicitly rejected until its dedicated durable owner claim/control path exists. Existing generic authenticated resource reads expose retained interactions; response RPC/CLI, protected answers and inbox read state remain pending.
- Real Connect/SQLite regressions pass for original question order/flags/content, numeric/text identity separation, independent waiting/closure/outcome, duplicate/replaced/foreign requests, cross-kind answer/approval payload rejection, null/size validation, atomic open-request bounds and terminal closure, and exact outbox reopen/replay after lost acknowledgment. Additional targeted race tests verify rollback of question closure when terminal publication fails and refusal to publish a native response without an owner claim. The complete delidev race suite passes with package execution serialized (`-p 1`), and vet passes. Native Linux arm64 publication/migration fixtures pass in the non-root network-disabled Secret Service container. Windows amd64 cross-build passes without claiming native Windows execution.
- An opt-in macOS arm64 installed Codex `0.151.0` Worker test now retains a real Plan-mode question and waiting state on the server, then verifies explicit Stop preserves the question, closes it without an answer, publishes interrupted terminal state and proves owned cleanup. The initial run exposed a fixture that stored its queued input as Execute regardless of configured Plan mode; the fixture now preserves its selected input mode, and startup failures report the typed job state promptly. The final question test and complete eight-test installed Worker suite pass under the race detector. Account readiness/initial selection remain seeded, and all provider traffic uses a private scripted loopback endpoint; public dispatch, semantic response acceptance, approvals and other harness/platform acceptance remain unverified. No protocol schema/generated bindings or repository-owned `dist` output changed.

- A private server transaction now accepts one non-secret question response against its exact original revision and live execution/account/Worker grant scope. It retains exact answer arrays and explicit empty answers, rejects secret/cross-kind/malformed/oversized input before ordinary persistence, and distinguishes queued acceptance from native delivery. Native/terminal closure cancels an unsent response while preserving its original content; reference-only receipt replay cannot requeue it. Grant lookup by immutable job returns scope metadata without its token digest. Public response APIs and active-Worker claim/delivery remain pending.
- Domain and real Connect/SQLite-backed publication fixtures cover concurrent distinct responses, original content retention, canceled response replay, current-revision duplicate refusal, current pause/archive/recovery/native ownership/cleanup/job cancellation, replaced/stale Worker, revoked device/account, replaced connection/server epoch, and rejected-response atomicity. The complete delidev race suite passes with package execution serialized (`-p 1`), and vet passes. This increment establishes private transaction behavior only; no installed-harness response integration or new platform acceptance is claimed. No protocol schema/generated bindings or repository-owned `dist` output changed.

- The server work stream now carries bounded, deduplicated metadata-only response controls for its active job, independently of later queued assignments. The new owning-Worker `ClaimQuestionResponse` RPC records exact original response/interaction revision and claim/job/device/instance ownership; reference-only retries recheck current authority before returning non-secret answers. Native closure after a claim retains uncertainty and paused recovery independently of terminal outcome. Worker loss cancels queued responses, marks claimed replies uncertain and preserves unknown native closure. The production Worker explicitly refuses response controls until its send-intent/delivery controller is integrated; owner response acceptance remains private.
- Real Connect/SQLite tests verify concurrent same-request claim replay, refusal of a replacement claim or foreign/stale control, answer-free receipts, denied content retrieval after cancellation/replacement/stale heartbeat/revocation, original question/answer retention, uncertain native/terminal closure, queued/claimed Worker loss, and response controls bypassing a blocked execution while later jobs remain unclaimed and targeted cancellation remains live. The complete delidev race suite (`-p 1`) and vet pass. Buf formatting/lint and breaking-change checks pass, and a second generation reproduces every generated protocol file byte-for-byte. No installed-harness response delivery, new platform acceptance or repository-owned `dist` output is claimed.

- The actual Worker now receives bounded response controls during native execution, retains an immutable metadata-only claim journal before RPC, validates returned original scope, synchronizes send intent before one native response and records delivery before ordered outbox publication. Its lifecycle-joined controller preserves the separate Stop interruption grace period; stream loss cancels owned work. Existing journals, replaced claims, duplicate controls and lost acknowledgments cannot resend an answer. A dedicated delivery event binds original claim/device/job/interaction/thread/turn/item and records `not-sent`, `transmitted` or `uncertain` without answer content. Pipe transmission, native closure, semantic acceptance and terminal/cleanup outcome remain separate facts.
- Server/Worker regressions pass for exact delivery and response accounting, receipt replay, unclaimed/mismatched/cross-kind observations, answer-bearing event refusal, original answer preservation, closure/terminal acceptance gates, metadata journals preceding claim/send/publication, lost claim/publication acknowledgments, foreign native identity, preexisting journals, uncertain writes, proven pre-send refusal, and controller joining during Stop without canceling native grace. The complete delidev race suite (`-p 1`) and vet pass. All question server/controller fixtures also pass in the non-root network-disabled Linux arm64 Secret Service container; the Windows amd64 CLI cross-build passes without claiming native Windows execution.
- An opt-in macOS arm64 installed Codex `0.151.0` test now drives private server response acceptance through the actual active-job stream, Worker claim/journal, native question reply, exact answer in the next scripted loopback model request, durable delivery/closure and verified terminal cleanup. Product state retains required semantic-acceptance recovery despite observed successful turn completion; no acceptance proof is fabricated from closure or fixture-side model receipt. The complete nine-test installed Worker suite passes under the race detector. Account readiness/first selection remain seeded; public owner response RPC/CLI, protected answers and native acceptance/history reconciliation remain pending. No protocol schema/generated bindings or repository-owned `dist` output changed in this increment.

- Public owner/paired-client `InteractionService.RespondQuestion` and `interaction respond` now accept non-secret answers against the exact original question revision and current execution authority. The mutation request UUID is the response UUID; actor-bound canonical receipt identity and reference-only results preserve current-state replay after closure without requeuing. The CLI reads and validates original questions before sending, accepts answer documents through file/stdin, and keeps credential stdin separate. Server acceptance additionally checks the native encoded wrapper size before persistence.
- Real Connect/SQLite tests pass for competing owner/client responses, changed actor/content refusal, canonical replay after terminal closure, original question/answer retention, answer-free receipts, Worker/anonymous/direct-call denial, revocation before receipt replay and commit, invalid input atomicity, and a domain-sized answer exceeding the native wire bound. CLI transport tests pass for authenticated file/stdin input, exact ordered/freeform answers, old-revision receipt replay after closure, and malformed/secret/wrong-kind refusal before the response RPC. The complete delidev race suite (`-p 1`) and vet pass; Buf formatting/lint, compatibility and byte-for-byte reproduction of all 32 generated protocol files pass.
- The opt-in macOS arm64 Codex `0.151.0` Worker response test now uses the public owner RPC through native reply, scripted loopback model receipt, durable delivery/closure and verified cleanup, and passes under the race detector. It retains required semantic-acceptance recovery separately from successful native outcome. Account readiness/first selection remain seeded; this does not establish public first dispatch, protected answers, inbox read state, native acceptance/history reconciliation or other platform/harness acceptance. No repository-owned `dist` output was generated.

- Native question and terminal publications now atomically create unique unread inbox references. Entries have independent IDs/revisions and preserve bounded native completion/failure/interruption evidence separately from session outcome and owned cleanup. A private read-state primitive changes only the inbox entry; reads, answers, closure and source receipt replay never implicitly mark it read or replace its source. Existing authenticated resource list/get/snapshot/event paths expose these records. Schema v10 backs up and backfills retained questions plus the available terminal progress from v1–v9 without rewriting original content or inventing missing history.
- Domain/store/server race regressions pass for closed metadata unions, source uniqueness, original answer retention, stale/no-op read revisions, response controls remaining claimable after mark-read, independent native/product outcomes, terminal receipt replay preserving read state, and publication rollback leaving no orphan inbox entries. Migration tests cross the 200-record page boundary, preserve v9 backups and native indexes, retain stable IDs on reopen and roll back generated records/indexes/schema when source evidence is malformed. The complete delidev race suite (`-p 1`) and vet pass; targeted migration tests also pass after refining cancellation/I/O error preservation.
- All nine opt-in macOS arm64 Codex `0.151.0` Worker tests pass under the race detector, with successful native runs retaining unread terminal evidence and the public question-response run retaining its separate unread original question. New inbox migration/publication fixtures pass on native Linux arm64 in the non-root network-disabled test container; Windows amd64 cross-build passes without claiming native Windows execution. Public joined inspection/read-state RPC/CLI, desktop notification delivery, protected answers and semantic acceptance/history reconciliation remain pending. No protocol schema/generated bindings or repository-owned `dist` output changed in this increment.

- Owner/paired-client `InboxService` and `inbox list/get/inspect/mark-read/mark-unread` now expose joined current inbox/session/original-question state. Read-state mutations bind the actor, inbox revision and explicit target state into reference-only receipts, validate source scope before commit and return current views on replay without reapplying earlier marks. Filtered cursor pages bind the inbox event epoch and preserve complete original content under a 3 MiB joined-view bound; source changes are read in the same page transaction without changing response authority.
- Real Connect/SQLite tests pass for concurrent exact receipt replay, actor/state substitution refusal, current unread/answer read-back after old mark-read replay, shared client observations, session/source/read-state filters, cursor filter mismatch and membership invalidation, Worker/anonymous/revoked client denial, stale-auth transaction checks, invalid enum/page limits, complete large-document pagination and rollback when the original source is missing. CLI/server tests retain archived/closed sources, use independent inbox revisions, generate/reuse request identities, filter unread entries and reject malformed/stale operations. The full delidev race suite (`-p 1`) and vet pass; Buf formatting/lint, compatibility and byte-for-byte reproduction of all 32 generated protocol files pass.
- The installed macOS arm64 Codex `0.151.0` question-response Worker test now lists its question through the public inbox RPC, explicitly marks it read and answers through the original interaction revision. Native completion retains that read state alongside independent unread terminal evidence and the existing semantic-acceptance recovery gate. This test passes under the race detector with a private scripted loopback provider and seeded first execution/readiness. All new inbox RPC and CLI fixtures pass on native Linux arm64 in the non-root network-disabled container; Windows amd64 cross-build passes without native Windows acceptance. Desktop notification delivery/deduplication, protected answers and semantic native acceptance/history reconciliation remain pending. Issue #964 remains unchanged at `2026-09-23T07:35:34Z`; no repository-owned `dist` output was generated.

- 2026-09-25, macOS arm64, installed Codex `0.151.0`: opt-in `TestManualNativeQuestionHistory` passed against private legacy and paginated threads using only a scripted loopback provider. Both emitted one exact question-answer raw output after raw-event opt-in and zero canonical function-output completions; legacy pagination returned native unsupported, and paginated history contained two user/assistant items with no question output. This is protocol evidence only: production raw-event acceptance and historical recovery are not yet implemented. The existing native question-response smoke also passed. No user account or external inference was used.

- 2026-09-25: enabled the pinned Codex legacy profile's raw supplement feed for exact live question acceptance. Adapter fixtures verify immutable answer digests, ordered/explicit-empty answers, original call/turn/response binding, duplicate suppression, mismatched/null/unknown/extra-field refusal, call-ID reuse rejection, content-free observations and preservation of unrelated recovery. Canonical usage is not duplicated from raw response supplements.
- Real Connect/SQLite tests verify claim-bound acceptance before/after native closure, one-time outstanding-response decrement, original content/transport preservation, atomic rejection of foreign or content-bearing evidence, retained uncertainty after prior transport failure and exact durable Worker-outbox replay after a lost acceptance acknowledgment. A matching native observation does not imply disk synchronization, general history recovery or process cleanup.
- Native macOS arm64 Codex `0.151.0` adapter tests passed with private scripted providers, including raw live acceptance, legacy/paginated investigation, thread persistence/resume, Plan/Steer/interruption and proxy isolation. Real Worker completion/command/plan/revocation/account-disconnect/Stop/Archive checks passed; raw cancellation output initially exposed a Stop regression, which was corrected to retain it as non-acceptance evidence. Native unanswered-question Stop and public inbox/read/respond through native acceptance/cleanup were rerun successfully. No user account or external inference was used; native readiness/public first dispatch remain seeded/pending.

- After live acceptance integration, `go test -race -p 1 ./cmds/delidev-cli/...` and package vet passed on macOS arm64. The native Linux arm64 disposable-container adapter/Connect acceptance fixtures passed with networking disabled. Windows amd64 CLI cross-compilation passed; this is not native Windows evidence. No generated protocol files or repository-owned `dist` output changed.

- 2026-09-25: Public first Codex API dispatch now checks current Worker heartbeat, exact installed protocol profile, original ready manifest, matching validated account connection and supported immutable settings, then atomically commits first snapshot/FIFO claim/routing/native identities/job. Automatic dispatch excludes paused/restored/recovered and previously claimed sessions. Explicit first Resume uses the same transaction; later-turn Resume remains unsupported. Failed checks preserve input/routing/files and publish stable blocking diagnostics without recurring revisions. Creation/control receipt replay joins the additive current execution job and cannot repeat selection or old cancellation.
- Real Connect/SQLite regressions cover unsupported-option rollback, current configuration/input capture, unchanged blocking diagnostics, stale Worker/unverified installation/changed validation refusal, Stop/Archive claim races, failed Resume preserving pause, removal/FIFO order, current-state control replay and bounded lifecycle-filtered candidate pages. Remote wire-selection validation is separated from the Worker's native filesystem checks.
- Opt-in macOS arm64 Codex `0.151.0` public CLI tests pass for both Execute and Plan: private pairing, actual executable/protocol discovery, keyless loopback account validation, session creation/workspace preparation, automatic first assignment, exact selected account/model/input, native transcript, one-time input accounting, owned process cleanup and original creation receipt replay. No readiness, snapshot, routing or execution record is seeded; no user login, external provider inference or harness installation is used. The first attempt exposed a test polling object retaining an omitted field from its previous JSON decode; polling now decodes fresh state. This does not establish external account/model fidelity, other harnesses/platforms, Local/multi-repository execution or later turns.

- The complete delidev race suite (`go test -race -p 1 ./cmds/delidev-cli/...`) and vet pass, followed by targeted race checks for the added rollback/lifecycle/remote-path cases. Buf formatting/lint/breaking checks pass and all 32 generated protocol files reproduce byte-for-byte. First-dispatch/Resume server fixtures also pass natively in the non-root network-disabled Linux arm64 container. Windows amd64 cross-build passes; installed Windows/Linux harness behavior remains unverified. No repository-owned `dist` output was generated.

- The additional public CLI single-repository Worktree scenario passes with installed Codex under the race detector: real temporary Git initialization/commit, asynchronous validated repository save, project selection, detached preparation, automatic native execution and owned cleanup. General Chat Execute/Plan and one-repository Worktree Execute were covered at that stage; the later multi-root entry records multiple-repository execution.

- 2026-09-25: The private Worker continuation lease now requires the exact closed predecessor and independently rechecks owned process cleanup, unchanged preparation provenance and canonical/Git administrative identity. New version-2 first claims capture a metadata-only identity digest. Continuation permits actual agent commits/branches/dirty files, synchronizes immutable closed predecessor history before new ownership and refuses old execution IDs, existing process scopes, ambiguous/missing ownership and unproven legacy claims. Losing the latest claim cannot erase retained execution history. Native continuation/server dispatch remain separate pending work.
- Real temporary Git/process/filesystem tests cover preserved advanced HEAD/branch/untracked content and original checkout, predecessor history across reconstructed Workers, conflicting claims, mismatched/missing/active/legacy/invalid evidence, corrupt history, changed accepted manifest, cancellation, existing scopes, changed worktree administration, retired identity reuse and missing latest ownership. Public macOS arm64 Codex first-dispatch regression tests pass under the race detector for General Chat Execute/Plan and Worktree Execute after the version-2 lease change, using private scripted loopback providers only. No later-turn native or external-account inference claim is made by those first-turn tests.

- The complete delidev race suite and vet pass for the continuation lease change, with targeted race regressions after adding the missing-latest-claim guard. Linux arm64 native workspace tests pass in a non-root network-disabled disposable container. Its original Secret Service image lacked Git, so a temporary derived test image installed Git before rerunning the real repository cases; the initial environment failure was not treated as passing evidence. Windows amd64 cross-build passes without claiming native Windows execution. No protocol schema/generated binding or repository-owned `dist` output changed.

- 2026-09-25: The private Codex adapter now leaves resumed bindings input-blocked until the latest full terminal turn matches retained root/session/turn identity, outcome, ordered original text input IDs/digests and complete effective settings. The pinned legacy profile's `thread/turns/list` supports this bounded check; its separate unsupported item API is not substituted. Automatic continuation requires success, while failed/interrupted outcomes require explicit Resume intent. Malformed/foreign/partial history cannot authorize input or clear prior recovery/pause, and historical content is not replayed as new product events or semantic question acceptance.
- Real owned subprocess fixtures cover native default/permission drift, changed/reordered/missing/duplicate/rich input history, earlier input replay, unsupported/late reads, changed native activity, cancellation, concurrent verification, explicit failure/interruption intent, retained pause/recovery and invalid checkpoint rejection before native requests. The opt-in macOS arm64 installed Codex `0.151.0` tests pass under the race detector for process-replacement continuation after Execute, Plan, same-turn Steer and interruption. They verify each original input identity, retained conversation and exactly one new model request for the continuation, with private scripted loopback providers and no user account or external inference. The native interrupted case rejects automatic continuation before accepting explicit Resume.
- The complete delidev Go race suite (`go test -race -p 1 ./cmds/delidev-cli/...`) and vet pass. New continuation fixtures also pass natively in the non-root network-disabled Linux arm64 container; this does not establish installed Linux Codex support. Windows amd64 CLI cross-compilation passes without native Windows acceptance. Public later-turn coordinator/Worker integration, fresh relay/runtime ownership and general historical recovery remain pending. No protocol schema/generated binding or repository-owned `dist` output changed.

- 2026-09-25: The actual Worker now synchronizes immutable native continuation checkpoints after terminal publication/native/process-lease cleanup and before journaling/reporting completion. Version-2 completion binds the exact file's lowercase SHA-256 digest; the server preserves it with the original job and current-state reference receipt. Version-1 reports remain historical terminal proof without continuation authority. Checkpoints retain exact assignment-input/configuration/account/connection/runtime ownership, native terminal/input identity and effective defaults/permissions, with no prompt/instruction/answer/tool-output/credential contents in the checkpoint. The bounded private reader refuses missing, replaced, linked, altered or mismatched evidence without creating state.
- Real temporary filesystem tests cover exact durable replay/reopen, private content exclusion, complete predecessor mismatch, altered native defaults pinned by the server report's file digest, missing/oversized/corrupt/foreign evidence and history roots, symlinks, changed assignments, conflicting overwrite refusal and independent failed/interrupted outcomes. Real Connect/SQLite tests preserve exact version-2 digest/profile on completion/replay and retain recovery for missing/noncanonical/version-conflicting digests. An initial fixture used the test framework's shared-permission directory as a private runtime; it was corrected to create its own private child before verification. That fixture setup failure was not treated as native evidence.
- All nine opt-in macOS arm64 installed Codex `0.151.0` Worker tests pass under the race detector, now reading completed/interrupted checkpoints back against the accepted server report. Public CLI first dispatch also passes for General Chat Execute/Plan and Worktree Execute with actual discovery and local keyless provider validation, without seeded readiness. These tests use private scripted loopback providers only. Complete Go race tests and vet pass; checkpoint/receipt fixtures pass natively in the non-root network-disabled Linux arm64 container, and the Windows amd64 CLI cross-build passes without native Windows acceptance. Public later-turn FIFO/Resume integration and fresh continuation relay credentials remain pending. No protobuf schema/generated binding or repository-owned `dist` output changed.

- 2026-09-25: Public Codex FIFO continuation and explicit Resume now span the existing CLI/Connect controls, shared bounded dispatcher, immutable version-2 successor assignment, workspace predecessor lease, digest-bound native checkpoint, original native history and fresh per-turn relay authority. Initial configuration/route/account stay immutable; current ownership advances independently and retains preceding progress. Successful cleanup permits automatic FIFO intent; native failure/interruption and explicit controls remain paused. Empty explicit Resume retains checked intent for the next input. Current account/project/model/Worker readiness is rechecked, and legacy/missing/uncertain evidence cannot be upgraded into execution.
- Real Connect/SQLite fixtures cover concurrent FIFO claims, queued edits, changed Agent settings without snapshot replacement, exact predecessor digests/history, fresh authority and old credential/completion denial, receipt replay without successor mutation, root/default drift, reuse of native turns older than the latest predecessor, explicit Resume for successful/failed/interrupted outcomes with queued/future inputs, and rejection of legacy completion, missing cleanup, unresolved responses/waiting, changed connections/readiness, exhausted/disabled accounts, incompatible models and offline Workers. Private filesystem fixtures verify chained checkpoints keep the original history root and leave their predecessor immutable.
- Actual macOS arm64 Codex `0.151.0` public CLI tests pass for General Chat Execute/Plan and single-repository Worktree Execute: real pairing/discovery, keyless loopback validation, original first dispatch, two FIFO follow-ups, Stop with retained queued input, explicit Resume and empty Resume followed by new input. The Execute General Chat scenario additionally interrupts an active native turn through public Stop, resumes its original history, and corrupts the final retained checkpoint to prove the next provider request never starts and its input remains uncertain. No readiness or execution state is seeded, user account accessed or external inference invoked. The initially unsupported native `thread/goal/cleared` resume snapshot was checked against the pinned schema/source and added as root-validated content-free absence metadata; populated/foreign goals remain unsupported. A test-only revision race during active Stop and a fixture cleanup wait were corrected before rerunning; neither failed attempt was counted as passing evidence.
- Validation for this integration: the complete `go test -race -p 1 ./cmds/delidev-cli/...` suite and `go vet ./cmds/delidev-cli/...` pass, with targeted race reruns after final terminal/provenance gates and current-job Archive/paused Restore coverage. All nine opt-in native Worker regressions pass under race, and the three public CLI continuation scenarios pass under race, including Worktree workspace-write permissions and exact once-only ordered user inputs in provider history. New server continuation/completion and Worker checkpoint fixtures pass in the non-root network-disabled Linux arm64 container. Windows amd64 CLI cross-build passes; this is not native Windows or installed Linux Codex acceptance. No protobuf schema/generated binding or repository-owned `dist` output changed. Public Steer, explicit account changes, native recovery and the other harnesses remain pending.

- 2026-09-25: The private Codex adapter now retains original Steer attempt identities/digests and explicitly inspects complete latest-turn history after uncertain delivery. Exact ordered input proof establishes acceptance without replay; absence or changed/partial/foreign history retains uncertainty. Native acknowledgment and history evidence remain distinct, late responses keep original ownership, and reconciliation preserves unrelated protocol failure and Stop/pause. Controlled subprocess race tests cover lost/mismatched/late acknowledgments, definite stale/unsupported rejection, delayed history, changed scope, concurrent/canceled inspection, input identity/content/order mismatch and subsequent resend refusal. The installed macOS arm64 Codex `0.151.0` Plan/Steer/interruption suite passes, including explicit native Steer history inspection and continuation after process replacement, using private scripted loopback responses only.
- Server progress, Worker publication state and private completion checkpoints now retain the entire ordered accepted-input set. Continuation checks every retained queue binding, preserves earlier ordinary FIFO entries when a later input was selected for the same turn, and rejects reused predecessor input identities. Checkpoints preserve all 4,095 supported input bindings under a 1 MiB bound without prompt content or truncation; legacy absent lists authorize only one original input. Temporary filesystem/Connect tests cover missing/reordered/changed/extra/duplicate evidence, queue mode/execution/request mismatch, immutable replay and complete maximum-size retention. The initial server fixture selected Execute input for a Plan predecessor and incorrectly expected no guarded blocking-diagnostic revision; both fixture expectations were corrected before passing, with no failed result counted as evidence.
- The complete delidev Go race suite and vet pass, followed by focused race tests for the final native inspection and capacity guards. Actual macOS arm64 Codex public CLI regression tests pass for General Chat Execute/Plan and Worktree Execute across first dispatch, FIFO, interrupted and queued/future Resume, original history and corrupt-checkpoint refusal. The native Worker inbox/question-response/acceptance/cleanup regression also passes. Linux arm64 adapter, checkpoint and multi-input continuation fixtures pass in the non-root network-disabled container; Windows amd64 cross-compilation passes without native Windows or installed Linux Codex acceptance. No protobuf schema/generated binding or repository-owned `dist` output changed. Public Steer RPC/CLI, durable claims, Worker controls and delivery publication remain pending; these private prerequisites do not expose an end-to-end Steer operation.

- 2026-09-25: Public selected-input Steer now spans `session steer`, dedicated Connect acceptance, a retained `steer` resource, metadata-only active-job control, current-Worker claim, content-free claim/send/observation journals, native expected-turn delivery, automatic uncertainty inspection and atomic typed publication. Original queue sequence/content/mode, configuration/account/permissions and unrelated FIFO entries remain unchanged. Only confirmed acceptance frees capacity and joins ordered checkpoint inputs; definitive rejection preserves the queue, while uncertainty pauses recovery without resend. Late native proof preserves the original uncertain observation separately and cannot clear recovery.
- Real Connect/SQLite and Worker fixtures cover exact concurrent acceptance, duplicate/changed claims, current ownership, stale execution/turn/revision, mode mismatch, waiting/unconfirmed interactions, capacity, Worker denial, explicit selection of a later queue entry, claimed edit exclusion, immutable receipt replay, lost claim/publication acknowledgments, automatic history inspection, independent inspection failure, late acceptance/rejection, foreign/mixed publication rollback and Stop/Archive/account-disconnect/terminal races. Ordinary tests remain independent of installed harnesses and user credentials.
- Installed macOS arm64 Codex `0.151.0` public CLI tests pass under the race detector for General Chat Execute/Plan and Worktree Execute. Each scenario uses actual executable discovery/protocol verification, public keyless account validation and preparation, then queues and Steers an exact same-mode input during an active inference request, retains both same-turn user messages and native acceptance, proves owned cleanup, and continues the original conversation in a fresh process. The scripted loopback provider observes every accepted input exactly once; old Steer receipt replay sends nothing. No readiness was seeded, user account accessed or external inference invoked.
- Validation for public Steer: the complete `go test -race -p 1 ./cmds/delidev-cli/...` suite and package vet pass, with focused race reruns after foreign-publication rollback and preservation of earlier independent diagnostics were added. All existing opt-in macOS native Worker regressions pass, including question response, revocation, disconnect, Stop and Archive. Steer server/Worker fixtures pass in the non-root network-disabled Linux arm64 container, and the final server diagnostic-preservation fixtures also pass there. Windows amd64 cross-build passes; installed Linux Codex and native Windows behavior remain unverified. Buf formatting/lint, compatibility against the preceding committed schema and exact reproduction of all 32 generated files pass. No repository-owned `dist` output was generated. Explicit account changes, approvals, native recovery, other harnesses and the remaining issue #964 surfaces are still required.

- 2026-09-25: Private Codex command/file/additional-permission approval adapters now retain exact native callback/arrival ownership and original request scopes, deep-copy response authority, validate closed decision/permission unions and transmit one caller-journaled response. Command decisions must match original offered choices/amendments; native permission grants cannot widen descriptors or discard requested deny restrictions. Tracking shares question bounds while permitting separate same-item approval callbacks. Unknown requests stay private; approval output cannot become question-answer evidence. Concurrent response, stale turn, Stop/closure, wrong-kind, absent choices, malformed fields, bounds and uncertain blocked-pipe tests pass.
- Installed macOS arm64 Codex `0.151.0` opt-in tests pass for command approval, command cancellation, native apply-patch interception into a private workspace file and additional filesystem permission response. They use private homes and scripted loopback providers, no user accounts or external inference. The request-permissions feature is enabled only in its temporary opt-in fixture, matching pinned upstream coverage. Native closure and tool completion remain observable separately from unconfirmed semantic response acceptance; private success does not establish an end-to-end public approval workflow.
- Approval validation: the complete Go race suite and package vet pass, plus focused race tests for final native permission/approval and production-Worker refusal boundaries. Installed macOS native approval, question-response and Execute/Plan/Steer/interruption regressions pass under the race detector. Approval/permission subprocess fixtures pass in a non-root, network-disabled Linux arm64 container; Windows amd64 cross-build passes, without installed Linux Codex or native Windows approval evidence. No protobuf/schema/generated bindings or repository-owned `dist` output changed. Dedicated public approval/inbox persistence, CLI/Connect responses, durable Worker claims/control/journals and exact acceptance/recovery inspection remain pending, as do the other issue #964 requirements.

- 2026-09-25: Native approval request publication now spans the Worker typed conversion/outbox and server interaction/index/unread-inbox transaction. The retained product graph binds exact Codex harness/version and original command/file/permission scope, preserves nullable collections and distinct same-item callbacks, and uses existing retention limits without a SQLite/protobuf schema change. Metadata-only native/terminal closure retains original approval content. Question response acceptance rejects approval records; inbox read state stays independent. Fixture tests cover all three native request kinds, exact policy/permission conversion, lost-ack outbox reopening with original content, cross-kind response refusal and invalid-publication rollback.
- Installed macOS arm64 Codex `0.151.0` Worker evidence now verifies command approval waiting, exact persisted choices, unread inbox, public Stop, unanswered closure and owned process/checkpoint cleanup with one loopback inference request. The fixture seeds account readiness and uses no user account or external provider. This proves native approval observation/Stop integration, not owner approval response delivery. The dedicated approval RPC/CLI, Worker response claims/journals and semantic acceptance/recovery boundary remain pending.
- Durable approval validation: the complete `go test -race -p 1 ./cmds/delidev-cli/...` suite and package vet pass. A focused race test additionally verifies shared question/approval payload-byte exhaustion, atomic overflow refusal and terminal closure without consuming the rejected event sequence. All approval publication/replay/rollback/byte-bound fixtures pass in the non-root network-disabled Linux arm64 container; Windows amd64 cross-build passes. Native Windows and installed Linux Codex remain unverified. Existing protobuf and SQLite versions remain unchanged; no generated binding or repository-owned `dist` output changed.

- 2026-09-25: Dedicated `RespondApproval` owner/client RPC and `interaction approve` now validate original decisions/restricted grants, current scope and response size before immutable queued acceptance. Actor-bound reference-only receipts preserve exact retries after closure. Metadata-only active-job approval controls, owning-Worker claims, claim/send/observation journals and ordered delivery publications preserve once-only native transmission independently of semantic acceptance. Concurrent acceptance/claim, cross-client/revoked receipt, authority drift, exact amendment/grant, cancellation/closure/loss, journal-before-side-effect, lost acknowledgment and CLI file/credential-stdin fixtures pass.
- Installed macOS arm64 Codex `0.151.0` Worker response evidence now runs public inbox/read/approval-response RPCs through the real active-job stream, Worker claim/journal and native command execution to retained actual tool output, terminal facts, checkpoint and owned process cleanup. The fixture exposed Codex's early approval `agent` source versus canonical `unifiedExecStartup` completion; compatibility is limited to a pinned matching ordinary approval with unchanged command/cwd, preserving both observations. Response transmission and tool completion still retain an unconfirmed-acceptance recovery gate. Approval Stop and question-response/acceptance regressions pass under race. These tests use private homes/scripted loopback providers and seeded first-execution/account readiness, not user credentials or external inference.

- Public approval validation: the complete `go test -race -p 1 ./cmds/delidev-cli/...` suite and package vet pass. Final focused race fixtures also verify exact native decision/permission serialization, explicit empty arguments/collections, private response exclusion and oversized-response refusal. Server/domain/Worker/CLI approval fixtures pass in the non-root network-disabled Linux arm64 container, including closed-approval command-source provenance. Windows amd64 CLI cross-compilation passes; native Windows and installed Linux Codex remain unverified. Buf format/lint, compatibility against the previous commit and exact reproduction of all 32 generated files pass. No repository-owned `dist` output was generated. Exact semantic approval acceptance/recovery, other harnesses and the remaining issue #964 requirements remain open.


- 2026-09-25: Exact live Codex additional-permission acceptance now compares the immutable sent grant with its uniquely owned native core tool output, including effective filesystem descriptors, network selection, scope and strict-auto-review meaning. Content-free `approval-accepted` publication binds the original durable response/claim and permissions request, atomically retains evidence and decrements outstanding acceptance once, while preserving transport, closure, cleanup and unrelated recovery. Same-item ambiguity, malformed/changed/cross-kind evidence, absent or not-sent responses and duplicate publication cannot confirm another response. Open/closed request outbox fixtures verify exact lost-ack reopening without grant content or native resend.
- Installed macOS arm64 Codex `0.151.0` adapter tests pass under race for turn-scope, session-scope, strict turn and empty permission grants, plus command accept/cancel and private file-patch regressions. The native run exposed app-server read/write mirrors alongside authoritative entries; both native and public validators now use pinned entries precedence, including an explicitly empty collection, while retaining the complete original request. Regression tests prevent ignored mirrors from introducing access and preserve order/deny restrictions. A later publication guard initially relied on a discarded original approval body; the Worker now retains only the closed approval kind through closure, and both open/closed durable publication tests pass. Failed intermediate runs are not counted as acceptance evidence. Permission tools are still enabled only in private opt-in fixtures; no user account or external inference participates, and installed public permission-flow or historical acceptance recovery is not claimed.
- Permission acceptance validation: the final complete `go test -race -p 1 ./cmds/delidev-cli/...` suite and `go vet ./cmds/delidev-cli/...` pass after the immutable-byte digest and metadata-only Worker kind guards. Final installed macOS Worker approval response, approval Stop and question response/acceptance regressions also pass under race with seeded readiness and private scripted loopback providers. Domain, native adapter and server approval/question-acceptance fixtures pass in the non-root network-disabled Linux arm64 container. Windows amd64 CLI cross-build passes; native Windows and installed Linux Codex remain unverified. No protobuf/generated bindings, SQLite schema or repository-owned `dist` output changed. Command/file approval acceptance, lost-native-event/history recovery, other harnesses and the remaining issue #964 requirements remain open.


- 2026-09-25: The pinned single-use approval profile now correlates one original plain `accept`, possible delivery and native closure with successful original command/file execution. Command evidence requires exact command/cwd, ordinary approval without callback/network scope, canonical startup provenance and observed zero exit; patch evidence requires successful nonempty changes. Same-item ambiguity, other decisions, foreign/changed identity, absent delivery/closure and unsuccessful results remain unconfirmed. The server verifies the original earlier tool start and durable claim, then atomically stores tool completion and `native-approved-command`/`native-approved-patch` acceptance under the same event sequence. It decrements one outstanding response without clearing independent recovery or fabricating cleanup.
- Source inspection confirms that the pinned orchestrator waits for approving resolution before runtime execution, while its emitter distinguishes zero-exit successful output from rejection/setup failures. Remembered-policy branches are deliberately not inferred: execpolicy persistence can warn and still release execution, and some patch approval paths skip the session cache. The new profile therefore proves only the selected single-use action. An initial callback-source negative fixture correctly failed the preexisting provenance guard; the test now asserts that rejection and unchanged acceptance rather than expecting an otherwise valid completion. No failed run is counted as passing evidence.
- Installed macOS arm64 Codex `0.151.0` approval adapter tests pass under race for command accept/cancel, private file patch and all four permission-response variants. The real outbound Worker command-response fixture now passes from public response acceptance through exact native execution, atomic server acceptance, unchanged inbox read state, terminal retention, checkpoint and owned cleanup, with no remaining unconfirmed-response gate. Tests use private scripted loopback providers and seeded first-execution/account readiness, without user accounts or external inference. The file path's Worker/server composition is separately fixture-backed, not claimed as an installed end-to-end file approval test.
- Single-use approval validation: the complete `go test -race -p 1 ./cmds/delidev-cli/...` suite and package vet pass. Focused final race cases cover session/execpolicy refusal, exact and ambiguous native scope, open/closed delivery, prior uncertainty, atomic rollback of the tool when acceptance fails, and lost-ack outbox reopening that replays both facts exactly once. Native adapter and Connect/SQLite server fixtures pass in the non-root network-disabled Linux arm64 container; Windows amd64 CLI cross-build passes. Native Windows and installed Linux Codex remain unverified. No protobuf/generated binding, SQLite schema or repository-owned `dist` output changed. Other approval decisions, unsuccessful/ambiguous execution, historical recovery and the remaining issue #964 scope are still required.


- 2026-09-25: Uncertain question/approval responses now trigger automatic bounded native thread/turn/input inspection under their original response/arrival authority. The Worker journals and attempts publication of the immutable delivery observation before releasing its publication lock for the inspection. The native adapter performs two matching root reads around complete latest-turn/input validation, then releases native control while waiting within the same five-second budget for exact original live evidence. It never consumes events, synthesizes acceptance, polls provider history, resends a response or clears prior pause/recovery. Missing, foreign or failed evidence remains uncertainty.
- A review of the first implementation exposed publication-lock starvation: an inspector holding that lock could prevent the normal reader from publishing the closure preceding acceptance. The final path releases it and lets exact question/permission/tool evidence use the existing normal outbox and server transaction. A once-written acceptance snapshot survives immediate native cleanup. Race fixtures verify evidence arriving during inspection, cleanup racing the waiter, original publication while the controller waits, failed/foreign proof, repeated controls, immutable transport observations and lost-ack replay using the same acceptance request identity. Shared Steer scope validation remains covered by its complete existing inspection tests.
- Installed macOS arm64 Codex `0.151.0` tests pass for inspection of accepted question, command/file and turn/session/strict/empty permission responses, plus cancellation remaining unconfirmed. A separate native test intentionally leaves the live question output queued after the scripted provider has received the exact answer: canonical history/closure remains insufficient; the original queued event later confirms acceptance once, preserving pause. Actual Worker question/approval response, Stop and Archive regressions pass with private homes, loopback scripted providers and seeded readiness. Native uncertain-write fault injection through the complete public Worker is not claimed; automatic controller concurrency is fixture-backed, and these tests use no user account or external inference.
- Response-inspection validation: the final complete `go test -race -p 1 ./cmds/delidev-cli/...` and `go vet ./cmds/delidev-cli/...` pass after the concurrency fix. Native adapter/Steer/approval/question fixtures and Worker controller/outbox tests pass in the non-root network-disabled Linux arm64 container. Final installed macOS adapter and Worker regressions pass under race; Windows amd64 CLI cross-build passes, without claiming native Windows or installed Linux Codex. No protobuf/generated bindings, SQLite schema or repository-owned `dist` output changed. Historical acceptance reconstruction, process replacement, other approval decisions/harnesses and the remaining issue #964 requirements are still open.


- 2026-09-25: Private completed-execution inspection now joins the original finished/reported Worker operation, exact server/device/instance/assignment identity, fully acknowledged event outbox, version-2 checkpoint and current closed workspace/process claim. It retains the original report/terminal identity without an RPC replay, native launch, input send or replacement claim. Two-minute bounded inspection acquires publisher then session locks, verifies original configuration/account/history/input and workspace identity, and rereads durable operation/outbox evidence before returning. Its result contains no prompt, answer, path, credential or native settings.
- Worker fixtures cover successful/failed/stopped outcomes, finished/reported journal states, repeated inspection from a replacement manager, live publication exclusion, cancellation, missing/corrupt/foreign/pending records, account/history/assignment mismatch, active claims and missing process indexes. Real temporary Git tests preserve agent commits, branches and dirty files, exclude concurrent continuation and reject retired predecessors. Final review added an original-index check before Git inspection so a read-only Git subprocess cannot recreate a missing preparation index and substitute new cleanup evidence; its regression test passes.
- Installed macOS arm64 Codex `0.151.0` Worker tests independently inspect actual successful, question-response, approval-response, Stop and Archive completion/checkpoint records. They use private homes, scripted loopback providers and seeded readiness, with no user account or external inference. This validates the evidence reader against actual completed native records, not public recovery after a real Worker restart. The execution-recovery RPC/CLI/job and atomic server reconciliation are still required; missing native acceptance/history and safe surviving-process reattachment remain separate requirements.
- Completed-execution inspection validation: the complete `go test -race -p 1 ./cmds/delidev-cli/...` passed, followed by final full Worker/workspace race suites, focused ownership regressions, package vet and installed native Worker reruns after the missing-index guard. Final Worker/workspace recovery fixtures pass in the non-root network-disabled Linux arm64 container. Windows amd64 CLI cross-build passes; native Windows and installed Linux Codex are unverified. No protobuf/generated bindings, SQLite schema or repository-owned `dist` output changed. The full issue #964 goal remains incomplete.


### Public retained completion recovery
- 2026-09-25: `RecoverSessionExecution`, CLI `session recover-execution --id ID --revision N --execution-id ID [--wait]`, the dedicated `recover-execution` Worker job and atomic coordinator reconciliation now connect retained private completion inspection. Acceptance binds actor/revision/execution; duplicate active recovery and exact receipt retries cannot queue duplicate work. The job retains metadata/digests only, and the result preserves the original report identity and checkpoint-bound terminal result. Original/current scope and complete accepted input/Steer/interaction facts are rechecked at result commit. Every successful recovery stays paused; pending Archive completes, original terminal outcome and non-recovery diagnostics remain independent, and account disconnection cannot grant execution authority.
- Connect/SQLite fixtures exercise successful/failed/stopped outcomes, original job restoration, accepted/report retries, stale revisions/foreign actors, mismatched or incomplete proof, pending input/Steer/response, changed cursor/request identity, extra accepted input, Worker replacement during inspection, archive and disconnected-account behavior. Worker fixtures check private credentials and all original journal/checkpoint scope, cancellation, metadata-only results and recovery-report replay without a second inspection. CLI fixtures verify exact mutation/execution identity, current joined resources, bounded wait output and typed failure.
- Installed macOS arm64 Codex `0.151.0`: a scripted HTTP fault rejects the original completion report after the actual Worker has retained native terminal events, checkpoint and closed workspace/process proof. The original Worker stops, a new Worker instance attaches, and public recovery queues/executes/reports through the real outbound Worker loop. The coordinator restores the original completion while leaving the session paused; the scripted provider receives exactly one native request. This uses a private temporary runtime, seeded readiness and a loopback scripted provider, with no user credentials or external inference. It does not prove missing-event reconstruction, surviving-process reattachment or other harness/platform/account acceptance.
- Final review independently checks original interaction/response states as well as progress counters: a claimed response can become uncertain before any delivery observation increments the outstanding counter. Regressions with a zero counter and unresolved original question/approval claims or open interaction fail closed. An unsuccessful recovery inspection also preserves the original non-recovery failure diagnostic through an explicit retry; continuation clears only the current recovery reference and retains historical recovery records.
- Validation: final complete `go test -race -p 1 ./cmds/delidev-cli/...` and `go vet ./cmds/delidev-cli/...` pass after the original-response guard. Installed macOS lost-report/restarted-Worker recovery passes under race; existing actual Worker completion, question/approval response, Stop and Archive regressions also pass. Worker and CLI recovery fixtures plus the final coordinator/continuation regressions pass in the non-root network-disabled Linux arm64 container. Windows amd64 CLI cross-build passes; installed Linux Codex and native Windows remain unverified. Buf format/lint and compatibility against the preceding commit pass, and full generation reproduces all 32 Go/TypeScript generated files without drift. No SQLite schema or repository-owned `dist` output changed. Issue #964 remains incomplete; the complete remaining requirements are retained in the normative snapshot and ledger.


### Multiple-repository native execution
- 2026-09-25: The coordinator and Worker now permit complete managed multi-repository Worktree assignments. Native thread start/resume receives all original prepared paths in repository order while cwd remains the designated primary. Each root is canonical/owned; effective runtime roots must match exactly, and workspace-write must cover every additional root without foreign or duplicate explicit writable authority. Selected native permission/approval/network/tmp behavior remains intact. Optional private checkpoint root evidence is retained and compared on subsequent native processes, preserving compatibility with old single-cwd records. Checkpoint writes, continuation reads and completed-execution inspection derive the expected list independently from the original accepted manifest; missing, foreign or reordered lists fail closed.
- Adapter fixtures cover all four permission selections, wire order/primary, caller slice isolation, missing/foreign/duplicate runtime and writable roots, absent primary, missing/file/relative/symlink paths, bounds, and changed continuation roots. Real temporary multi-repository Git tests preserve a secondary branch/dirty file and reject missing non-primary ownership before accepting a later verified claim.
- Installed macOS arm64 Codex `0.151.0`: non-inference binding passes with the primary second in the list under default/read-only/workspace-write/full-access. A scripted native `exec_command` successfully writes files in both selected roots without requesting broader approval. Public CLI tests pass for two-repository Worktree Execute and Plan through actual pairing, discovery, keyless validation, repository saves, preparation, first dispatch, FIFO turns and queued/future Resume. The Execute scenario additionally covers native interruption/Resume and retained-checkpoint failure. Every completed continuation checks exact roots and primary from the original manifest; no readiness is seeded and no user account or external inference is used.
- Validation: final complete `go test -race -p 1 ./cmds/delidev-cli/...` and `go vet ./cmds/delidev-cli/...` pass. Final installed macOS multi-root binding/tool writes, public CLI interruption/continuation and existing restarted-Worker completion recovery pass under race. Adapter, checkpoint and secondary-repository ownership fixtures pass in the non-root network-disabled Linux arm64 container with explicit test-binary entrypoints. The initial container command ran its default credential suite instead; that run is excluded from multi-root evidence. A macOS fixture originally supplied uncanonicalized temporary checkout paths; its corrected canonical-path regression and complete suite pass. Windows amd64 CLI cross-build passes; native Windows and installed Linux Codex remain unverified. No protobuf/generated bindings, SQLite schema or repository-owned `dist` output changed. Local origin, other harness/account modes and all remaining issue #964 requirements remain open.


### Authenticated Local checkout execution
- 2026-09-25: Explicit Local creation now composes owner/paired-client authentication with a locally loaded paired Worker credential in a dedicated write-only RPC field. The CLI checks private scope, machine, exact endpoint and authenticated server ID/protocol before delivery. Server acceptance independently authenticates and transactionally rechecks the Worker, retaining only immutable origin machine/device IDs in actor-bound receipt identity and session state. Other workspace modes reject the extra authority; later controls/views preserve origin, and current dispatch checks reject revoked or mismatched provenance.
- Local preparation captures each repository's canonical Git common/admin identity digest and clears fetch/starting selection. First execution, subsequent native processes and closed-execution inspection preserve current user branches, commits and dirty files, including changes after preparation. Independent explicitly selected Local sessions can share checkouts; deleting managed metadata does not delete source files. Missing legacy proof, missing/replaced Git metadata, redirected linked-worktree registration and foreign origin cannot acquire native authority.
- Installed macOS arm64 Codex `0.151.0`: public CLI Local Execute with two repositories and Plan with one repository pass through actual pairing, discovery, keyless loopback validation, creation, preparation, native first dispatch, FIFO continuation and queued/future Resume. Execute additionally interrupts an active turn, resumes original history and rejects a corrupted checkpoint before provider work. Every turn verifies exact original cwd/roots, preparation origin, user branch/HEAD and dirty tracked bytes. No readiness is seeded, user credential accessed or external inference invoked. Private fixtures separately exercise origin failures/revocation/receipt isolation and absence of raw Worker tokens in SQLite; actual Git fixtures cover post-preparation commits, explicit sharing, cleanup preservation and identity replacement. Uncertain Local preparation recovery, real provider accounts and other OS/harness acceptance remain open.
- At the authenticated-Local increment, preparation still required an existing HEAD commit; unborn-checkout handling and uncertain Local preparation recovery were required follow-up work. The later unborn/recovery increment below closes those two boundaries without claiming complete workspace/recovery acceptance.
- Validation: final complete `go test -race -p 1 ./cmds/delidev-cli/...` and `go vet ./cmds/delidev-cli/...` pass after the linked-registration guard, followed by the focused origin suite after moving fixture filesystem setup outside its transaction. Final installed macOS CLI Local and existing General Chat/Worktree dispatch regressions pass under race. Non-root network-disabled Linux arm64 origin/CLI/workspace fixtures pass, including the final linked-registration refusal/recovery; Windows amd64 CLI cross-build passes. Buf format/lint and compatibility against the preceding commit pass, and all 32 generated Go/TypeScript files reproduce without drift. SQLite schema and repository-owned `dist` output are unchanged. Native Windows, installed Linux Codex, real selected accounts and the complete remaining issue #964 scope are not claimed as verified.


### Unborn Local execution and preparation recovery
- Local preparation now records an explicit optional unborn HEAD state with empty commit IDs/starting reference. Native Git must prove a valid missing symbolic branch; unrelated failures and corrupt refs do not become empty-checkout success. Existing committed manifest/checkpoint encoding is unchanged. Current user commits or orphan-branch choices remain compatible with captured Git identity; validation does not modify the index, staged/untracked files or branches.
- The public original-journal preparation recovery path now supports Local. Ready inspection rechecks captured Git identity/current HEAD and preserves all files. Explicit partial recovery validates an exact non-owned repository prefix, deletes managed metadata only and retains synchronized per-original-job cleanup proof across post-removal interruption. Server acceptance/result publication recheck the original Local origin and preparation identity; invalid evidence leaves uncertainty and pause intact.
- Real temporary Git fixtures cover unborn first execution/closed inspection, transition to the first user commit, unchanged historical manifests, malformed/missing/detached/non-branch refs, launch/cancellation failures, ready Local recovery, empty/partial/full recorded prefixes, interrupted cleanup publication, foreign proof refusal and original-file preservation. Real SQLite/Connect and replacement-Worker tests cover Local ready/partial/mismatched-journal recovery across server and Worker restart, exact report retry, retained Archive/paused state and original journal preservation. Separate RPC fixtures reject origin changes before acceptance and before result publication.
- Installed macOS arm64 Codex `0.151.0` passes public CLI Local unborn Execute with two repositories and Plan with one, first dispatch, FIFO continuation, queued/future Resume, active interruption, resumed original history and corrupted-checkpoint refusal. Every turn checks exact original roots/cwd, unborn metadata, absent branch commit and retained dirty files. Public pairing/discovery and keyless loopback validation are used without seeded readiness, user credentials or external inference.
- Validation: complete `go test -race -p 1 ./cmds/delidev-cli/...` and `go vet ./cmds/delidev-cli/...` pass. Installed macOS CLI unborn profiles and existing General Chat/Worktree/committed-Local regressions pass under race. Non-root network-disabled Linux arm64 Local workspace and server/replacement-Worker recovery fixtures pass; Windows amd64 CLI cross-build passes. No Rust, frontend, protocol generation, SQLite schema or repository-owned `dist` output changes were required. Native Windows, installed Linux Codex, real provider accounts and the remaining issue #964 capabilities remain unverified or pending as recorded above.


### Schedule calendar and durable occurrence foundation
- Added typed editable schedule selections and server-owned configuration/timer/origin state, immutable accepted occurrences, exact derived session provenance and a closed overlap/state/reason model. The existing pinned cron dependency now supplies explicit five-field/IANA-zone calculations; bundled timezone data and a bounded long-horizon adapter cover DST and a valid leap-day run beyond the parser's five-year search cutoff. Caller-selected scheduled provenance remains rejected by ordinary session creation.
- Schema 11 atomically retains timer/sequence advancement and unique ordered/cron-due occurrences. Wait/history pages survive schedule deletion and restart; immutable selection and session-binding checks refuse foreign adoption. Capacity refusal preserves all 1,000 previously accepted waiting occurrences without partial timer advancement. Migration backs up first, preserves existing v10 inbox records and rolls back on unknown legacy schedule ownership.
- Worker leases now retain the start of their continuous observed interval, reset on replacement/gap/clock rollback and migrate from the last observed heartbeat. A connection established after a due instant cannot prove that earlier availability. These primitives do not yet start a scheduling loop, expose ScheduleService/CLI mutations, implement overlap dispatch or create native scheduled executions; those required integrations are the next step.
- Validation: complete `go test -race -p 1 ./cmds/delidev-cli/...` and `go vet ./cmds/delidev-cli/...` pass. Calendar, occurrence, migration, lease and capacity fixtures also pass in the non-root network-disabled Linux arm64 container; the Windows amd64 CLI cross-build passes. No protocol generation, Rust, frontend or repository-owned `dist` output changed. These results verify the scheduling foundation, not native scheduled-session acceptance or the remaining issue #964 scope.


### Schedule server coordinator
- The server now owns a bounded scheduling loop joined before storage/vault closure. Cron and internal Run now use one actor/revision-bound receipt path, with time sampled after acquiring the transaction. Timer/occurrence/session/input/preparation writes commit together, and duplicate cron rejection rolls back every newly created resource. No native/provider work or premature account/template snapshot is performed by the scheduler.
- Overlap creates independent source-linked sessions; Skip and Wait inspect current sessions, including explicitly resumed/re-prepared historical runs. Wait preserves immutable accepted selections through edits/pause/deletion and restart, releases only its FIFO head, and remains queued across Worker disconnection. Invalid current references/Local origin become typed failed history before side effects. Offline due instants and wait-capacity overflow produce explicit skipped history without evicting prior acceptance or creating execution catch-up work.
- Completion reconciliation preserves native question/approval waiting, recovery, archive/preparation cleanup and pending automatic follow-up. Terminal native events alone cannot release successors. Exact completed job/input identity and owned cleanup are required, and original input/Steer/interaction records are independently checked so an uncertain response cannot hide behind a zero summary counter. Completed occurrence history stays immutable after later explicit session activity.
- Private SQLite/coordinator fixtures cover session/preparation/input ownership, manual receipt concurrency, independent Worktree/Local selections, Local revocation, offline skipping, FIFO/restart/deletion, concurrent Wait release, full-capacity preservation, duplicate-cron transaction rollback and loop cancellation. Existing native-publication Connect fixtures verify success/failure/Stop cleanup gates and unsettled-original-response refusal. Public lifecycle RPC/CLI, referenced-configuration deletion integration and actual scheduled harness execution remain pending.
- Validation: final complete `go test -race -p 1 ./cmds/delidev-cli/...` and `go vet ./cmds/delidev-cli/...` pass after the settled-delivery guard and bounded missed-history drain. All schedule coordinator tests also pass in the non-root network-disabled Linux arm64 container, and the Windows amd64 CLI cross-build passes. No real installed harness, user account or external inference was invoked by these fixtures. This increment changes no protobuf/generated sources, SQLite schema, Rust, frontend or repository-owned `dist` output; the full remaining issue #964 scope remains open.

## Public schedule lifecycle and manual native execution

- Added owner/paired-client ScheduleService and CLI create/list/get/inspect/edit/delete/pause/resume/next-run/history/occurrence/Run now. Strict definition writes exclude timer/origin/configuration state, creation can allocate its ID, existing mutations require current resource revisions and actor-bound reference-only receipts join current records. Old requests cannot resurrect deleted configuration or initiate duplicate work. Worker credentials cannot invoke the public boundary.
- Local creation/relocation uses write-only secondary paired Worker authentication loaded from a private CLI scope; unchanged Local edits retain and revalidate the original machine/device provenance. Schedule list/history pages bind exact filters and event epochs, retain numeric occurrence order and remain bounded. Pause/delete preserve accepted work; Run now shares configured overlap and may run a valid paused schedule.
- Project/Agent configuration deletion now atomically disables matching schedules with a retained actionable problem. Invalid/stale deletion rolls the disablement back, and Resume/Run now cannot clear it without valid reconfiguration. Existing retained-session reference deletion guards remain; implementing that broader lifecycle is still required. Accepted occurrence/session selections and history are never rewritten by this change.
- Public Connect/CLI fixtures verify concurrent exact retries, current-state joins, role separation, forged/stale definition refusal, Local revocation, cursor scope/epoch rejection, deletion/history retention and real background FIFO release after configuration deletion. They use private SQLite, pairing credentials and HTTP only, without installed harnesses or user accounts.
- The opt-in installed Codex 0.151.0 macOS `TestManualNativeCLIScheduledWorkspaces` passes with actual private Worker pairing/discovery, real Git and a keyless scripted loopback provider. Public Run now creates `SCHEDULED` Worktree Execute (two repositories) and Local Plan sessions, reaches native terminal cleanup and succeeded occurrence history, then verifies ordinary FIFO continuation, queued/future Resume, immutable roots/settings/history and receipt joins. Worktree also exercises interrupted-turn Resume and corrupt-checkpoint refusal. This is manual initiation evidence, not an actual cron timer, hosted-account or other-platform native claim.
- Validation: complete `go test -race -p 1 ./cmds/delidev-cli/...`, `go vet ./cmds/delidev-cli/...`, Buf format/lint/breaking against the prior HEAD and deterministic regeneration of all 32 generated files pass. Schedule Connect/CLI fixtures pass in the non-root network-disabled Linux arm64 container; Windows amd64 CLI cross-build passes. No Rust, frontend, database schema or repository-owned `dist` output changed. Full issue #964 implementation remains in progress.

## Actual native cron timer acceptance

- Added opt-in `TestManualNativeCLICronWorkspace`, which configures an enabled every-minute schedule through public CLI/Connect, waits for the actual published future minute and verifies a single cron-triggered occurrence at exactly that due instant. No injected clock/state, seeded execution readiness or Run now creates this session.
- Installed macOS Codex 0.151.0 with the private paired Worker, real Git and a keyless scripted loopback provider completes native Worktree Execute and the server records succeeded history after owned cleanup. The test pauses future scheduling, deletes configuration, inspects independent retained history/session state and completes normal FIFO follow-ups plus queued/future Resume on the original native history.
- The installed-harness test passes, as do the complete required `go test -race -p 1 ./cmds/delidev-cli/...` and `go vet ./cmds/delidev-cli/...`. This test/evidence increment changes no product protocol, schema, Rust, frontend or repository-owned generated `dist` output. Other-platform/harness/native-account evidence and the remaining issue #964 scope remain open.

## Project/Agent deletion with independent retained execution

- Removed the retained-session prohibition from Project/Agent configuration deletion. The existing revision-checked RPC/CLI atomically removes selectable configuration, creates the original deletion tombstone and disables matching schedules with a reconfiguration problem. Session/input/occurrence/snapshot/workspace records remain unchanged, and deletion does not imply Stop, Archive or permanent session erasure. New selections and unstarted first dispatch cannot resolve deleted settings.
- Schema 12 privately retains only a deleted project's final Agent/account restrictions and source revision when established execution snapshots need them. Active relay authorization and continuation/Resume prefer live policy, then require valid tombstone-bound retained evidence. A policy tightened before deletion stays tightened; missing fields, invalid/unknown fields, absent policy or missing Agent deletion ownership never create unrestricted access. Existing immutable Agent snapshots remain usable after explicit Agent deletion without re-routing, and current account/connection/Worker/native ownership checks still apply.
- Real SQLite/Connect fixtures cover transaction rollback, final-policy retention across restart, unchanged session bytes/revisions, deletion receipt replay, no premature snapshot/queue consumption, continued native assignment ownership, account disablement after Agent deletion, and allowed/Agent-denied/account-denied relay authority before and after project deletion. Migration backs up schema 11, preserves accepted schedule/occurrence bytes and rolls back on conflicting schema without modifying the backup or original version. Earlier migration tests still cover preserved inbox and refusal of unrecognized pre-schema-11 schedules.
- The opt-in installed macOS Codex 0.151.0 cron fixture now deletes both a restricted project and its Agent through the public CLI while the first native provider response is live. The actual timer-created Worktree session completes with its original snapshot/account, retains successful occurrence history, then continues FIFO turns and queued/future Resume after schedule deletion. All setup uses actual private pairing/discovery, real Git and a keyless scripted loopback provider; no user account, hosted inference or other-platform native execution is claimed.
- Final validation passes: complete `go test -race -p 1 ./cmds/delidev-cli/...`, `go vet ./cmds/delidev-cli/...`, the installed native cron/configuration-deletion test, and the relevant store/migration/Connect tests in the non-root network-disabled Linux arm64 container. Windows amd64 CLI cross-compilation passes. No generated protocol, Rust, frontend or repository-owned `dist` output changed. Permanent session/resource/managed-backup deletion, remaining native harness/platform/account evidence and the rest of issue #964 remain required.


### Claude Code isolated protocol discovery
- The Worker now probes the installed Claude Code `2.1.236` stream-json initialization protocol under a fresh private bare runtime. Only a correlated control response with no account token, fixed probe permission mode and explicitly disabled remote-control gates can publish verified. Native agent/model advertisements remain private; protocol success grants neither execution capabilities nor selected-account readiness.
- Temporary native-process fixtures cover fragmented Unicode, foreign/duplicate/malformed/trailing frames, unsolicited interactions, account/scope changes, timeouts, early exit, independent output limits, inherited credential/config exclusion, fresh private runtime checks and owned cleanup. Domain and Connect publication tests keep this profile generation-bound and capabilities empty.
- An explicitly selected installed macOS arm64 Claude Code `2.1.236` passed actual version detection, initialization, strict response validation and owned cleanup through the production discovery path. No user login, keychain credentials, external provider or inference request was used. Native Claude execution, API/session authority, history/resume, interactions, subscription login and other operating systems remain unverified and required.
- After the final schema checks, the complete `go test -race -p 1 ./cmds/delidev-cli/...` suite and package vet pass. Compiled Linux arm64 adapter and production discovery fixtures pass as a non-root user in a network-disabled disposable container; these are protocol fixtures, not an installed Linux Claude acceptance claim. Windows amd64 adapter tests and the complete CLI cross-compile. No Rust, frontend or generated protocol sources changed, and no repository-owned `dist` was generated.

### Claude Code execution transport and native resume evidence
- A separate owned stream-json transport now preserves exact outgoing control/input identities and original native callback arrivals. It serializes writes, rejects reused or foreign identities and canceled replies, retains late control acknowledgments without replay, discards raw error text, and keeps unvalidated native bodies JSON-excluded. Frame, aggregate event, pending request and retained identity bounds fail explicitly; blocked input joins its writer and owned process before return.
- Ordinary subprocess fixtures exercise fragmented Unicode, exact input/session preservation, late acknowledgment after timeout, canceled reads, one concurrent callback reply, cancellation before/after reply, raw error/log exclusion, malformed/duplicate/ambiguous/foreign responses, invalid UTF-8, partial exit, event exhaustion and blocked writes. Successful transport delivery is never published as native semantic acceptance.
- An explicitly selected installed macOS arm64 Claude Code `2.1.236` passed two real stream-json turns through the new transport and a private scripted loopback provider. After owned cleanup, a second process resumed the same native session and sent its original user/assistant history plus the new input exactly once. The test checks native queued/started/replayed/completed input UUIDs, exact replies, effective version/model/permission/cwd, requested high effort and additive native base instructions. Fresh private bare state excludes user logins and external inference; the optional native health request remains local and unsupported. This does not establish public dispatch, production API authority, child credential isolation, subscription login, typed rich events/interaction acceptance, general history recovery or other-platform Claude execution.
- Final validation passes: full `go test -race -p 1 ./cmds/delidev-cli/...`, package `go vet`, the opt-in installed native transport/resume test, and compiled Claude discovery/stream fixtures as a non-root user in a network-disabled Linux arm64 container. Windows amd64 Claude tests and the complete CLI cross-compile. These platform fixtures/builds do not establish installed Linux/Windows Claude acceptance. No protocol bindings, Rust, frontend or repository-owned `dist` output changed.

### Claude Code private API launch, callback echoes and native relay
- Added a fresh private API stream initializer with exact installed-version/native permission/account-source checks, canonical empty account runtime and workspace validation, explicit model/effort, exclusive synchronized additive-instruction file and a Worker execution token delivered only through native environment to the paired server's fixed API relay. It initializes without sending a prompt and grants no public session capability. Ordinary subprocess fixtures verify environment reconstruction, closed native authority, mismatches/timeouts, cleanup, private file permissions, invalid-config rejection before launch and JSON/log/argv exclusion of credentials/instructions.
- Actual native tool execution exposed Claude's callback-response echo under replay mode. The transport now binds one echo to the original claimed native callback and exact canonical reply digest, retaining it separately from tool completion. Fixtures reject altered/unclaimed/duplicate echoes and collisions with outgoing operation identities, and prove digest comparison preserves large integer values and array order.
- Installed macOS arm64 Claude Code `2.1.236` executed one explicit Bash environment check through the production initializer and real server API relay into a scripted loopback Messages provider. The test verifies exact scoped model, high effort, additive base instructions, server-only upstream key injection, one original native permission response and its echo, credential absence in the child, exact original input/session completion and owned cleanup. Both the execution token and upstream fixture key are absent from retained private native state and process logs. The relay lease is controlled test authority; this is not durable Worker dispatch/account readiness or external inference acceptance.
- The native client uses the exact `beta=true` query. The relay now preserves it only for authorized Anthropic Messages/count-tokens operations, rejecting encoded/case-changed/duplicate/additional/empty queries and queries on other protocols before provider authority. Fixtures verify the unchanged upstream query and beta header with the immutable scoped operation.
- Separate installed initialization checks retain all five explicit native permission selectors: default, plan, acceptEdits, dontAsk and bypassPermissions. Research showed that `CLAUDE_CODE_SUBPROCESS_ENV_SCRUB=1` instead forces non-default selectors to default; the initializer uses host-managed provider state, which preserves those selectors while excluding credentials from the exercised tool child. The API profile remains bare. Full native tools and Plan artifacts/questions/transitions, typed event/history/usage/interaction acceptance, production Claude registration/public dispatch, subscription authentication and the remaining platform/account matrix remain required, with no substituted or inferred capabilities.
- Final validation passes: complete `go test -race -p 1 ./cmds/delidev-cli/...`, package `go vet`, all opt-in installed Claude stream/API tests, and compiled Linux arm64 discovery/API/transport plus exact beta-query fixtures in non-root network-disabled containers. Windows amd64 Claude adapter tests and the complete CLI cross-compile. No generated protocol, Rust, frontend or repository-owned `dist` output changed; the installed Linux/Windows native acceptance matrix remains open.

### Claude Code full native API tools and Plan flow
- Superseding the preceding bare API launch boundary, private API execution now uses full native mode while retaining fresh private configuration, strict empty MCP, host-managed provider state and the exact requested permission/effort. Both explicit native authentication sources carry the same scoped execution token; the secure-store namespace remains private. Bounded typed native command descriptors reject unknown fields, ambiguous names/aliases and missing native commands. Discovery retains its separate no-input bare profile.
- The pinned native HTTP client sends both Bearer and API-key headers. The relay accepts the exact equal valid credential pair only for Anthropic Messages/count-tokens, acquires one lease and injects only the server-owned upstream key. Ordinary HTTP fixtures reject differing/malformed tokens, duplicate or combined headers and dual credentials on other protocols before authority acquisition or secret reads.
- Installed macOS arm64 Claude Code `2.1.236` passes full-mode initialization for all five native permission selectors and actual Bash execution with both credential variables absent from the tool child. A separate actual native Plan test runs AskUserQuestion, returns the original selected answer, lets Write create the native-selected private plan file, compares its exact contents/path with the ExitPlanMode callback, returns the original approval and observes the native approved tool result followed by original-input completion. Native system messages remain part of the unchanged provider history. Tests use the real server relay and a scripted loopback provider, with no user login or external inference.
- This is private native API/Plan evidence. Typed session state/events/history, durable interaction acceptance, production Claude Worker registration/public dispatch, subscription authentication and the remaining platform/account matrix are still required. A successful native callback or tool result cannot independently authorize a public session transition.
- Validation passes: complete `go test -race -p 1 ./cmds/delidev-cli/...`, package `go vet`, all opt-in installed Claude native tests, and Linux arm64 adapter plus Anthropic query/authentication fixtures as a non-root user in network-disabled containers. Windows amd64 adapter tests and the complete CLI cross-compile. No generated protocol, Rust, frontend or repository-owned `dist` output changed.

### Claude Code original-input lifecycle and native API failure
- Added a private immutable execution binding that validates full native system initialization, exact original queued/started/replayed input identities and original-content digest, independent command closure and correlated results. It retains bounded unique native event identities, enforces exact core JSON field names and latches recovery after contradictory or foreign observations. Result subtype, error bit and native loop reason remain distinct; deferred/aborted/error reasons never become successful completion. Native output, diagnostics, usage/costs and remaining families stay private and cannot be discarded or published by this core observer.
- Ordinary fixtures exercise changed model/permission/account/workspace/plugin/memory authority, omitted required initialization state, field aliases/duplicates, out-of-order or repeated lifecycle facts, original text/input/session changes, child/injected replay, missing/foreign/repeated results, native failure/deferral reasons, cancellation without acceptance, contradictory command/result closure in either order, caller mutation of an observation, null final text, and preservation of uncertainty after an uncorrelated error. Native raw content remains excluded from serialized lifecycle observations.
- The installed macOS arm64 Claude Code `2.1.236` full Plan flow now passes through this observer and confirms exact original-input acceptance plus successful native completion. A separate actual native 401 test through the real server relay and scripted provider exposed assistant diagnostic `request_id` metadata and a result with `subtype: success`, `is_error: true`, `terminal_reason: api_error` but no input UUID. The transport now retains a bounded provider request ID as private assistant data without acknowledging any control; fixtures prove even an ID equal to an outstanding control cannot resolve it. The lifecycle retains the session failure with no input-completion identity, preserves earlier acceptance, and requires recovery. No external inference, user login or raw diagnostic logging is involved.
- Public Claude Worker dispatch, typed rich messages/tools/usage/interaction acceptance, native history/resume and subscription/account integration remain required. This binder observes one immutable attempt; it sends no input and cannot prove publication, process liveness or cleanup.
- Final validation passes: complete `go test -race -p 1 ./cmds/delidev-cli/...`, package `go vet`, actual native Plan/API-error lifecycle tests and the complete compiled Claude fixture suite as a non-root Linux arm64 user with networking disabled. Windows amd64 Claude tests and the CLI cross-compile. This is not installed Linux/Windows native acceptance. No generated protocol, Rust, frontend or repository-owned `dist` output changed.

### Claude Code typed content, tool results and usage
- The private original-input observer now validates ordered provider message/block streams, exact text/thinking completions, original tool identity and complete tool-result frames. Native result text cannot substitute for the transcript. Thinking signatures and raw diagnostics remain private; completed content buffers are released after comparison. Per-block, aggregate in-flight, message/tool identity and concurrent-tool bounds are tested, including interleaved completions and unchanged ownership after a failed result frame.
- Tool observations retain both streamed proposal input and the native completed object. Installed macOS arm64 Claude Code `2.1.236` enriches `ExitPlanMode` from `{}` with the actual plan text/path; the strengthened Plan test matches that completed input to the original callback, native-written artifact and original tool result. Structured tool output and ordered result text blocks remain original data, never execution or permission authority.
- An additional installed native test runs two multi-block inputs in one owned process through the actual scoped API relay and scripted loopback Messages provider. Each block keeps its provider message ID, index and distinct native envelope UUID. First/second main-loop input usage is 3/13; native cumulative model input is 3/16, output is 11/22, cache-write is 5/10 and cache-read is 7/14. The adapter retains those scopes separately, preserves missing/zero and decimal estimate spelling, and does not aggregate charges. Original acceptance/result and command closure are checked before the next test input. No raw frame dump, user login or external inference is part of the committed test.
- The official provider usage unions are pinned to Anthropic TypeScript SDK commit `1926adb4d292090975e6b5d19ebafe2274d2469e`. Native iteration/credit metadata is observational and does not initiate routing, retries or redemption. The relay now rejects the native `fallbacks` request selector, including null/empty chains and case aliases, before upstream key access or transmission; real HTTP tests cover Messages and token-count operations.
- Validation passes: the complete delidev Go race suite, package vet, installed native multi-block/two-turn usage plus Plan and API-error tests, and compiled Claude/relay fixtures under a non-root Linux arm64 container with networking disabled. Windows amd64 adapter tests and the complete CLI cross-compile. These are private adapter/controlled-provider observations, not public Claude Worker execution, durable publication, billing aggregates, real-account inference, or installed Linux/Windows acceptance. Remaining rich/subagent families, interaction acceptance, native history/resume, subscription/account integration and the other harness profiles remain required. No generated protocol, Rust, frontend or repository-owned `dist` output changed.

### PR #983 repair pass (2026-09-25)
- Addressed 16 connector findings from the initial and final inventories with isolated commits and focused regressions: backup directory durability and atomic migration publication; failed fresh SQLite and initial vault-pin recovery; completed process-scope retirement; retained account-reference deletion guards; unsupported authentication observations; Git remote-HEAD failure propagation; protected SSE metadata/error codes; nonzero bounded-wait failures; current-process Worker schedule availability; encoded-size resource pagination; keyless cleanup without credential storage; stopped/archived schedule outcomes after late native success; and failed pre-launch execution-claim publication.
- The real Connect pagination fixture retrieves all 41 maximum-size templates through both binary and JSON encodings, checking stable resource replay and strict cursor progress. Account lifecycle fixtures verify keyless disconnect/reconnect/delete while the injected credential service is unavailable, retained credential-bearing cleanup uncertainty, and old receipt isolation across restart. CLI fixtures attach after server startup and refresh related recovery state before returning an observed job problem.
- Non-root Linux arm64 containers with networking disabled pass the store, credential-vault, API-relay and process fixture suites, plus focused server account/pagination/schedule and Git inspection fixtures. These use private temporary state and no user credentials, hosted provider, installed harness or external inference.
- The pre-repair Windows CI log had three independent causes: a drive letter serialized as a SQLite URI authority, a byte scan while a mandatory vault lock was held, and incomplete incompatible-image launch classification. The malformed drive URI reproduces the same SQLite error locally; URI tests now preserve local drive paths and escaped filenames. The byte scan closes the fixture vault first, and the loader classifier covers documented incompatible-image/machine/subsystem statuses. Windows amd64 tests cross-compile; a new native Windows CI result is still required before claiming these runtime failures are cleared.
- Final local verification passes: `go test -race ./cmds/delidev-cli/...`, `go vet ./cmds/delidev-cli/...`, focused Linux fixture execution above, Windows amd64 compilation of every DeliDev test package, and `git diff --check`. The complete native Windows runtime suite remains for CI.
- Pre-launch publication fixtures inject owner-directory sync, pre-rename and post-rename failures for both first execution and continuation, then retry the exact identities. They verify predecessor restoration, retained history, and refusal to discard nonempty or foreign ownership. The focused race suite and non-root Linux fixtures pass; published leases and crash-recovered active claims retain their existing fail-closed recovery boundary.
- These repairs preserve the remaining issue #964 requirements and do not grant complete product or harness/platform acceptance. No Rust, frontend, generated protocol or repository-owned `dist` output changed.

### PR #983 Windows CI follow-up (2026-09-25)
- Native CI run `36092236276` passes Linux/macOS Go tests and the previously failing Windows credential-vault suite. Remaining Windows failures are tracked here separately; they do not establish full Windows acceptance.
- The process stream privacy fixture now closes its owned Handle after Stop before scanning all retained bytes. This releases the mandatory Windows controller lock without skipping it or weakening production locking. The focused macOS race test passes; native Windows execution remains for CI.
- Process retirement fixtures now create their root with the production private-directory primitive, so intermediate owner directories and recovery locks inherit the Windows owner-only ACL. Incomplete/invalid journal cases reach ownership validation instead of failing early on a shared temporary-root ACL. Focused macOS race tests cover interrupted retirement and preserved incomplete evidence.
- The Windows server no longer rejects Unix absolute checkout paths belonging to remote Workers. Domain regressions retain Unix/Windows path bytes and reject relative forms; the existing two-Worker repository transaction test covers failure rollback and stale-revision protection. Focused macOS race tests pass; native Windows confirmation remains for CI.
- SQLite migration and orphaned-sidecar fixtures now create their state roots with the production private-directory primitive rather than POSIX-only mode bits. Their database/recovery files inherit the Windows owner-only ACL, allowing the existing backup, rollback and orphan-preservation assertions to exercise storage behavior. Both focused macOS race regressions pass; production permission validation remains unchanged.
- Final follow-up validation passes: complete `go test -race ./cmds/delidev-cli/...`, `go vet ./cmds/delidev-cli/...`, Windows amd64 test-package compilation with `go test -exec=true`, and `git diff --check`. Cross-compilation does not execute Windows tests; a post-push native CI result is still required. No generated `dist`, Rust, frontend or protocol output changed.
- The final review inventory added a field-name reflection finding. A real HTTP regression reproduced leaked prefixes in LF/CRLF, unknown and colonless SSE fields; a separate bounded name matcher now rejects them before delivery and retains benign original metadata. Focused relay race tests pass.
- A second real HTTP regression reproduced protected machine-code reflection in HTTP-200 error envelopes. Both HTTP error branches now share the guarded native/local-body fallback, retaining failure status when no body is safe. Focused race tests verify raw-key exclusion from replies and logs for four colliding allowlisted codes.
- Numeric-context regressions reproduced raw-key reflection in all three provider protocol profiles. Retained decimal context limits now pass the existing raw/Base64 key guard before assignment; exact/embedded/maximum integer reflections return no partial catalog, while unrelated and keyless limits remain unchanged. Provider race tests pass.
- Snapshot regression fixtures contain 10, 30 and 40 maximum-size templates; the middle case fits binary transport but overflows JSON transport. Both codecs now receive an explicit correlated narrower-scope error before oversized serialization, without partial state/cursor. Smaller snapshots retain exact resources and a coherent cursor; existing byte-paginated lists retain every identity. Focused server race tests pass.
- After all four late review fixes, the complete DeliDev race suite, package vet and Windows amd64 test-package compilation pass again. The one-shot repair contains four Windows CI root-cause repairs and four independently reproduced review repairs; native post-push CI remains separate evidence.

### PR #983 streamed-key and pre-launch recovery follow-up (2026-09-25)
- Real HTTP regressions reproduce split JSON field-name reflection for upstream keys, Base64 keys and execution tokens, at root/nested paths with literal/escaped spellings. Decoded names now pass global and original-parent matchers before JSON-pointer escaping and frame delivery, under the existing memory bounds. Focused relay race tests pass.
- Controller-preparation fixtures inject pre-launch creation failure, verify synchronized empty-scope removal, and run a real subsequent native process under the same owner. Separate cases preserve partial lock/journal/unknown files, existing or replaced scope identities, and recovery on parent synchronization failure. No native-started cleanup proof is bypassed.
