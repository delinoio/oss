# DeliDev implementation and evidence ledger

Issue #964 is preserved in full in [requirements](cmds-delidev-requirements.md). This ledger distinguishes code, deterministic tests, and actual external/native evidence. It is not a reduction of the requirements.

## Current implementation work
The CLI/server/Worker implementation is in progress. No release or complete real-account harness integration claim is made; bounded native evidence is recorded separately below.

| Boundary | Implementation | Verification |
| --- | --- | --- |
| CLI, typed JSON, explicit startup, server/sidecar lifecycle | Versioned JSON/errors, explicit detached/foreground startup and compatible reuse, status/stop, configuration CRUD, read-only doctor, backup implemented; native services/supervision pending | Native macOS subprocess smoke: no implicit startup, readiness/reuse, CRUD, backup, explicit stop, restart persistence |
| Connect, authentication, origins, pairing, TLS, streams | Owner authentication on every product RPC, exact origins, TLS-required remote listeners, signed scoped cursors, bounded Connect event streaming implemented; single-use device pairing, credential revocation, machine-scoped Worker authorization and outbound job streams implemented | Real loopback Connect: missing authentication, hostile origin/Host, snapshot/replay, deduplication across restart, backup deduplication, pairing replay/single-use, cross-machine denial, claimed-job/report deduplication, active-stream revocation; remote TLS validation guards tested, remote native lifecycle not validated |
| SQLite, IDs/revisions, atomic events/receipts, backup/restore | Exclusive private scope, WAL transactions, global entity identity, optimistic revisions, durable receipts, metadata-only events, coherent bounded snapshots, consistent backup implemented; restore/deletion coordination pending | Real temporary SQLite: concurrent retries/restart, rollback, kind collisions, deletion receipt redaction, cursor bounds, WAL backup, corrupt/newer DB preservation; Go race tests pass |
| Projects, repositories, settings, restrictions, templates | Strict versioned configuration schemas, relationship/revision validation and server-owned health fields implemented; repository saves now revalidate each Worker and atomically commit canonical checkouts with default-on automatic fetch | CLI/Connect configuration, real Worker saves, multi-machine all-or-nothing validation, missing-remote refusal and concurrent asynchronous revision tests; complete session workflow pending |
| Accounts, provider/models, protected secrets, proxy | Native OS wrapping-key adapters and a private authenticated-encryption vault with immutable retries/deletion markers implemented; API/keyless account connect/disconnect/status and cleanup retries integrated through RPC/CLI; bounded non-inference account validation, nine provider presets, periodic/explicit catalog discovery, search/resolve and deletion suppression integrated; bounded native API JSON/SSE relay and server-side durable execution authority/reference storage implemented; Worker scoped-token delivery and first Codex execution integrated; quota refresh, subscription authentication, native model compatibility and public dispatch remain pending | Temporary macOS keychain and Linux arm64 GNOME Keyring container round trips/locked-state checks; real Connect/SQLite account lifecycle/revocation/restart tests and actual Linux CLI/server/Secret Service composition; Windows native credential test implemented and cross-compiled, not executed |
| Six routing policies, quota evidence, immutable snapshots | Pure six-policy selection/read-only preview and private atomic first-execution snapshot/account/input-claim/routing persistence primitive implemented; native readiness/dispatch integration pending | Weighted rotation, sequential traversal/recovery, project/account restrictions, Fixed exclusions, minimum blocking windows, stale evidence and quota ties; real SQLite concurrent claims, rollback, restart immutability and edit/claim races tested with simulated readiness |
| Workers, executables, outbound jobs, processes, updates, services | Durable outbound job API, instance leases and uncertainty on replacement implemented; explicit foreground Worker loop, private pairing and execution journals, capped reconnect and CLI commands implemented; native process ownership with a durable start barrier, separate interactive streams and Git integration implemented; Worker-owned executable selection and version discovery through RPC/CLI implemented; Codex non-inference native handshake implemented; first Codex job execution through cleanup reporting integrated; remaining native profiles/lifecycle, updates and services pending | Real Connect pairing/dispatch/report/revocation tests; CLI pairing to a real Worker and real temporary Git inspection; persisted completion reuse and interrupted-journal uncertainty; discovery isolation/path precedence/output bounds/cancellation and stale-generation publication tests; installed Codex Worker execution and live revocation pass against a scripted loopback provider with seeded readiness, not external inference |
| Codex, Claude Code, OpenCode, Grok Build native adapters | Bounded native JSON-RPC transport and a version-specific Codex non-inference app-server handshake integrated with discovery; internal thread create/read/resume, exact settings, Execute/Plan turns, expected-turn Steer, interruption and typed core lifecycle/message events implemented; immutable first-turn Worker execution with scoped account authority, durable events and cleanup reporting integrated; durable questions and public non-secret response acceptance through journaled native delivery and exact live acceptance evidence integrated; public dispatch, remaining rich events/approvals and other native adapters pending | Real macOS Codex `0.151.0` handshake, thread persistence/resume, Execute/Plan/Steer/interruption against loopback scripted provider responses; temporary native protocol fixtures; real selected accounts, external inference and other OS/harness combinations not validated |
| Workspace preparation, Local protection, forks, snapshots | Worker-local inspection, exact reference resolution, detached multi-repository preparation, primary cwd, Local protection, General Chat isolation, durable preparation/cleanup manifests implemented; inspection and session preparation RPC/CLI integrated with atomic complete-manifest publication and immutable job cancellation; original-journal preparation recovery and explicit partial cleanup integrated; native forks and snapshots pending | Real temporary Git: subdirectory/linked inspection, detached commits, retry reuse, partial rollback, dirty Local preservation, remote fetch advancement/failure, disabled fetch, cancellation, explicit missing-default failure; real CLI/Worker General Chat/multi-repository preparation and Archive retention |
| Sessions, queue, Steer, interactions, Plan, archive/recovery | Durable session/initial-input acceptance, ordered bounded queue, revision-checked content edits/removal tombstones, session lists/rename, workspace jobs and never-executed preparation-aware Stop/Archive/Restore integrated through Connect/CLI; accepted first Codex Worker jobs and terminal cleanup reports integrated; first-native Stop/Archive/paused Restore and bounded Worker interruption integrated; public owner non-secret question responses and journaled Worker delivery and exact live native acceptance integrated; public first dispatch/snapshot/routing, Local origin proof, Resume, Steer/approvals/Plan/forks remain pending | Real SQLite/Connect/CLI acceptance/restart/replay, concurrent FIFO order, mode preservation, scoped pages, byte bounds, stale/cross-session edit denial, archive visibility/paused restore and uncertainty refusal; actual private Worker first-turn execution/completion and revocation verified with installed Codex and seeded readiness, while public live session dispatch remains pending |
| Schedules, overlap/skip/wait, durable occurrences | Pending | Pending |
| Terminal/files/diff/reviews/Sidechat/forwarding | Pending | Pending |
| GitHub PAT/query/PR evidence/remediation | Pending | Real GitHub integration not validated |
| Usage/costs/budgets, diagnostics/doctor | Immutable attributed Codex native token observations and redacted notice publication implemented; usage aggregation/reset/fork/subagent reconciliation, dashboard, costs, budgets and full diagnostics pending | Private native Codex/server/Worker-outbox composition and real Connect/SQLite replay/reset/attribution tests; no billable-cost or aggregate-usage claim |
| Search/activity/inbox, config import/export | Native question/terminal inbox persistence and independent read-state RPC/CLI with joined current-source/session inspection and bounded pagination implemented; notification delivery, search/activity and import/export remain pending | Atomic publication, source uniqueness, read-state isolation and migration/reopen fixtures; native evidence recorded below |
| Deletion/managed backups/offline cleanup/storage | Pending | Pending |
| CLI build/distribution and OS lifecycle | Pending | Native Windows/Linux evidence pending |

## Presentation scope
The current user request is the CLI. Desktop windows/tray/widgets/native browser presentation and desktop artifact installation belong to the full product issue and remain visible in the complete requirements; they are not claimed as CLI implementation evidence. Their server-owned data and product operations remain within the CLI/RPC scope.

## Local verification log
- 2026-09-24, macOS arm64, Go 1.26.1: `go test -race ./cmds/delidev-cli/internal/...` and `go vet ./cmds/delidev-cli/internal/...` for the domain, private-files, and SQLite foundation. No harness or provider account was invoked.
- 2026-09-24: `go test -race ./cmds/delidev-cli/...`, package vet, Buf formatting/lint, and generated Go bindings validated. The native macOS executable was exercised against an isolated temporary data directory. Linux arm64 cross-compilation succeeded; this does not establish native Linux behavior.
- 2026-09-24: Windows amd64 cross-compilation succeeded. Native Windows runtime remains unverified. Real temporary Git preparation tests and package race tests/vet pass on macOS arm64.
- A subsequent Git-registration regression exposed the macOS `/var` parent alias during rollback. Worker roots now canonicalize before ownership journals are created; the real Git regression and package race/vet checks were rerun after the fix.

## Native interface discovery (not execution validation)
- Installed read-only version/help checks: Codex CLI `0.151.0`, Claude Code `2.1.236`, OpenCode `1.18.20`. No `grok` executable was found on this machine. No user credential files were read and no inference was invoked by these checks.
- Official adapter references retrieved: [Codex app-server](https://learn.chatgpt.com/docs/app-server) for native expected-turn steering; [Claude Code programmatic execution](https://code.claude.com/docs/en/headless) for structured streams; [OpenCode server](https://opencode.ai/docs/server/) for authenticated native HTTP; [Grok Build headless/ACP](https://docs.x.ai/build/cli/headless-scripting) for native JSON-RPC integration. Installed-version capability validation and real-account evidence remain required before claiming an adapter supported end to end.

- Worker schema v2 migration preserves a consistent private v1 backup before changes; real SQLite tests cover successful upgrade and rollback after a conflicting migration. Revocation tests reject both new mutations and receipt replays from a previously authenticated principal.

- CLI integration tests pair both a Worker and a client, reuse pairing receipts, inspect a real Git subdirectory over outbound Connect, and reject Worker credentials for owner resource queries. Package race tests and vet pass after this integration.

- Native macOS CLI subprocess smoke passed for explicit server startup, private Worker pairing, outbound readiness, real Git inspection, asynchronous repository save, and revocation shutting down the Worker with a typed authentication error. No network fetch, inference or user account access was involved.

- Revocation now atomically cancels undispatched Worker jobs and preserves claimed-job uncertainty; real Connect regressions cover both states. Migration backup names are synchronized before schema commit. Final package race tests/vet and generated-binding freshness checks pass; updated Worker CLI cross-compiles for Windows amd64 and Linux arm64 without implying native runtime validation.

- Native macOS process tests cover start-before-side-effect barriers, separate stdout/stderr, byte-split UTF-8 stdin, blocked-input cancellation, daemonized descendants after natural exit/cancel/disconnect, supervisor-death recovery, cross-owner refusal, unrelated live scopes, missing journals, output-consumer failure and secret-free ownership journals. Worker Git now uses these scopes and retains cleanup uncertainty. Linux/Windows process test binaries cross-compile; native Linux/Windows lifecycle and PTY behavior remain unverified.

- Worker discovery now runs end to end through `machine discover`, outbound jobs and atomic machine publication. Real Connect tests cover authorization, exact request/result retries, stale discovery generations, independent attachment revisions, unverified-capability rejection, safe diagnostic reconstruction and rollback after a wrong job revision. Temporary native process fixtures verify environment isolation, explicit-path precedence, missing/permission/incompatible outcomes, bounded raw output, cancellation and confirmed runtime cleanup. Native launch errors retain safe OS classifications.
- Native macOS arm64 CLI/server/Worker smoke, isolated temporary server and Worker directories: version discovery observed Codex `0.151.0`, Claude Code `2.1.236`, OpenCode `1.18.20`, and missing Grok Build. All observations correctly retained `protocol_verified=false`. No account login, inference, native protocol handshake, or model availability claim follows from this smoke. Probe processes and private runtime directories were cleaned after native ownership reconciliation.

- After discovery integration: complete delidev Go race tests and vet pass, Buf lint passes, and regeneration reproduces all bindings exactly. Windows amd64 and Linux arm64 CLI builds and platform test-binary cross-compilation pass; native Windows/Linux discovery and lifecycle remain unverified. No repository-owned generated `dist` directory remains.

- Native JSON-RPC fixtures cover split Unicode frames, typed/redacted native errors, concurrent interaction replies, pre-send cancellation, uncertain request delivery and late acknowledgments, blocked input, malformed/bounded frames, slow-consumer overflow, and confirmed owned-process cleanup. Protocol-profile tests reject changed native versions, foreign homes/platforms, extra response fields and preexisting loaded threads without invoking inference.
- Native macOS arm64 CLI/server/Worker `machine discover --protocol` smoke successfully verified Codex app-server `0.151.0` initialization and empty bounded loaded-thread state. Actual native testing exposed the optional `emittedAtMs` notification metadata and `nextCursor` response field; both are now represented and covered by fixtures. The final probe left zero private probe directories after native reconciliation. Execution capabilities remain empty, and no account login, thread creation, model refresh or inference was performed. Claude Code/OpenCode protocol profiles and missing Grok remain explicitly unverified.

- After native protocol integration, complete delidev race tests and vet pass; the legacy discovery-receipt regression also passes after closing/reopening real SQLite. Buf lint and exact regeneration checks pass. Updated Windows amd64/Linux arm64 CLI binaries and native protocol test binaries cross-compile, without claiming native Windows/Linux or account execution verification. No repository-owned generated `dist` directory remains.

- Protected credential storage now implements native macOS Security.framework, Windows Credential Manager and Linux Secret Service wrapping-key adapters, with AES-256-GCM private envelopes, domain-separated commitment keys, durable staging/deletion markers and exact immutable retries. It is a storage primitive; account lifecycle RPC/CLI integration, upstream validation, login, proxy authorization and execution revocation remain pending.
- Native macOS arm64 tests on macOS 26.6.2 passed against a newly created temporary keychain, including duplicate refusal, locked-store failure without UI, temporary-keychain unlock, native deletion and larger encrypted payloads. Testing identified that file-keychain operations need the process-wide no-UI setting as well as the query option. No default/login keychain credentials were read.
- Native Linux arm64 credential tests passed in a network-disabled disposable Debian/GNOME Keyring container with a non-root user, private home and private D-Bus session. Real native testing exposed GNOME Keyring's `text/plain` metadata for binary values; the adapter now preserves and validates the binary payload independently of MIME. Actual create/read/duplicate/delete, larger envelopes and locked collection/deletion-marker behavior passed. This proves the Secret Service adapter in that container, not complete Linux desktop/Worker lifecycle acceptance.
- After credential storage changes, the complete delidev Go race suite and vet pass. Windows amd64/arm64, Linux amd64 and macOS amd64 credential test binaries cross-compile; native Windows execution remains unverified. Tests cover cancellation after a native write, uncertain writes before/after acceptance, restart reuse, conflicting concurrent writes, missing/corrupt keys and records, private-file rejection and deletion resurrection prevention. No repository-owned generated `dist` output was produced.

- API/keyless account lifecycle is integrated through authenticated Connect and the CLI. Real temporary SQLite/HTTP tests cover exact and conflicting retries, restart, concurrent connects, in-flight client revocation, uncertain native writes, orphan-intent cleanup, blocked reconnect/deletion, preserved concurrent metadata edits, and old receipt replay after reconnect/deletion. Failure injection uses a test-only secret backend; stored credentials stay unverified and cannot route.
- A separately compiled Linux arm64 CLI/server passed its complete credential lifecycle in a non-root, network-disabled disposable Debian/GNOME Keyring container: API key through stdin, protected native storage, server restart and receipt replay, disconnect/cleanup, account deletion and resurrection prevention. Retained server files and CLI output contained neither raw nor Base64-encoded test credentials. No provider, model or inference request was made; native Windows/macOS account-command composition remains unverified.
- After account lifecycle integration, `go test -race ./cmds/delidev-cli/...`, `go vet ./cmds/delidev-cli/...`, Buf lint and exact generated-binding reproduction pass. The updated CLI and server tests cross-compile for Windows amd64/arm64, Linux amd64 and macOS amd64; cross-compilation does not establish runtime acceptance.

- Bounded provider inspection now backs `account validate` with server-owned key retrieval, exact authority/authentication evidence, model pagination, no redirects/retries, sanitized failure classes and durable observations. Real HTTP/Connect/SQLite fixtures cover restart/replay, failed observation replay, independent accounts during a blocked check and disconnect cancellation. Public/custom catalog success remains explicitly unverified for credential validity. No actual provider-account or inference verification is claimed.
- The rebuilt Linux arm64 CLI/server passed the network-disabled native Secret Service fixture with protected-key retrieval for an in-container model-list endpoint, unsupported credential-evidence output, and exact validation replay without another request. Full delidev Go race tests/vet passed; subsequent URL-validation changes passed the affected domain/provider/server race tests and package vet.
- After provider validation integration, Buf lint and exact Go-binding regeneration pass. Updated CLI binaries cross-compile for Windows amd64/arm64, Linux amd64 and macOS amd64. These builds do not establish native remote TLS, real account authentication or harness execution evidence.

- Provider/catalog integration adds nine editable presets, periodic/explicit account-bound discovery, atomic model publication, advisory metadata provenance, NEW acknowledgement, manual/display preservation, deletion suppression and indexed search/canonical resolution. Real HTTP/Connect/SQLite tests cover OpenRouter pagination and malformed/reflected metadata, accepted failure/restart replay, unchanged-model revisions, user-declared overrides, ambiguous/hidden selectors, cursor filter/tamper/epoch behavior, Worker denial, discovery disablement and automatic shutdown cancellation. Schema v3 migration tests preserve original v2 data/receipts and synchronized backups, and roll back both canonical-ID and alias conflicts without rewriting legacy records.
- The actual Linux arm64 CLI/server passed its expanded native Secret Service fixture in the non-root network-disabled disposable container: native-key automatic discovery after enabling the provider, model search/resolve, explicit discovery, restart without repeating an accepted request, failed discovery output/replay and final disconnect/deletion. No external provider, user account or inference endpoint was contacted. Complete delidev race tests/vet passed; subsequent catalog logging/canonical-resolution changes passed affected server/store race tests. Buf formatting/lint and exact reproduction of all 32 generated files pass; CLI cross-builds for Windows amd64/arm64, Linux amd64 and macOS amd64 pass after the final portable refinements. Native Windows and macOS catalog command composition remains unverified.

- The internal API relay now covers five scoped native operations, exact model/connection binding, direct verified TLS, cancellation, protocol terminal states, redacted native errors, owned-reference callbacks and split-delta protected-value checks. It has no production router/authority integration yet. Real loopback HTTP tests passed on macOS arm64 and in the non-root network-disabled Linux arm64 container; these use fixture authority/provider data, not real account inference. Complete delidev race tests/vet pass; proxy test binaries cross-compile for Windows amd64/arm64, Linux amd64 and macOS amd64. Durable token issuance/revocation, reference storage and real harness acceptance remain pending.

- Session acceptance now atomically persists the session and first queued input without freezing Agent configuration or claiming native execution. Owner/client Connect and CLI support bounded ordered enqueue, content edits/removal with reference-only receipts, lists, rename and never-executed Stop/Archive/Restore. Current blocked state and unsupported Resume are explicit. Real SQLite/Connect/CLI tests cover restart, concurrent acceptance, receipt reuse after edits/removal, immutable mode, cross-session/stale input denial, byte-bounded pages, Worker denial and independent archive/outcome/pause. Schema v4 tests validate v3 backup preservation and rollback on ambiguous queue order. Live native session/Local-origin/first-dispatch authority remain pending.
- After session integration, complete delidev race tests/vet, Buf formatting/lint and exact reproduction of all 32 generated files pass. The Linux arm64 CLI test binary passed create/queue/edit/remove/archive/restore/unsupported-Resume through real temporary Connect/SQLite in the non-root network-disabled container. Windows amd64/arm64, Linux amd64 and macOS amd64 CLI cross-builds pass; this is not live native harness or native Windows acceptance. The additional old-create-receipt-after-removal regression passed under the race detector. No repository-owned `dist` output was generated.

- Worker stream reception now remains active during owned native work, with one unresolved server assignment and a 45-second heartbeat deadline. Native macOS controlled-process tests prove cancellation after permission revocation, unavailable/clean stream termination and silent-connection expiry, with a durable canceled journal and no successful report. Discovery regression verifies a newer generation remains queued while the previous native job is claimed and still rejects the stale result. Complete delidev race tests/vet pass; the final silence/termination regression also passes independently. This fixes a lifecycle prerequisite for workspace/session dispatch, without claiming those integrations complete.
- The rebuilt Linux arm64 Worker test binary passed all four live-process stream-termination/heartbeat scenarios in the non-root network-disabled container. Windows amd64 CLI cross-compilation passed; native Windows cancellation remains unverified. No harness inference, user credential access or repository-owned `dist` generation occurred.

- Session creation now atomically accepts its ordered workspace preparation request and first input. Real macOS CLI/server/Worker/Git tests verify General Chat preparation, detached two-repository commit/primary selection, explicit retry after queued cancellation, receipt reuse and ready-file retention across Archive. Connect/SQLite tests cover claimed-job cancellation without envelope revision changes, independent following jobs, pending Archive until cancellation confirmation, malformed-success uncertainty, Worker denial and restart replay. Remote-manifest fixtures verify identity/digest/reference/commit/path/ownership rejection and Windows path grammar independently of the server OS.
- Schema v5 preserves a private validated v4 backup and durable cancellation across restart. Native controlled-process tests verify both cancellation during work and pre-cancellation before any process starts; the same stream then prepares a different General Chat job despite a delayed old cancellation. Complete delidev Go race tests/vet, Buf formatting/lint and exact reproduction of all 32 generated files pass. This increment prepares workspaces, not native harness conversations; first-dispatch snapshots/routing, uncertain-preparation reconciliation and full harness lifecycle remain pending.
- The Linux arm64 Worker binary passed running/pre-start targeted cancellation and subsequent General Chat preparation in the non-root network-disabled disposable container. The container has no Git, so no new native Linux multi-repository CLI claim is made. Windows amd64 CLI cross-compilation passes; native Windows workspace/cancellation remains unverified. No user credential, provider inference or repository-owned `dist` generation occurred.

- Preparation recovery now has authenticated RPC/CLI, immutable claimed-assignment evidence, original Worker journal matching, bounded native reconciliation and atomic session/original-job publication. Explicit partial cleanup retains a private proof outside the removed tree; ready working files survive recovery and pending Archive. Real macOS replacement-Worker/Connect/SQLite tests cover lost completion, partial cleanup, digest mismatch/refusal and repaired explicit retry, original journal retention, exact receipt replay and targeted cancellation of recovery. Native Git fixtures verify detached commit/common-directory/registration checks, ready-change preservation, partial Git cleanup, post-removal interruption and replacement-link refusal.
- Schema v6 migration preserves the original v5 database backup, recovers a legacy uncertain claim from its durable receipt and retains its exact revision/body after mutable result changes. Job deletion removes the retained assignment. Complete delidev race tests/vet pass; additional assignment-migration and ownership-link tests pass under the race detector. Buf formatting/lint and exact reproduction of all 32 generated files pass. Native harness execution, conversation recovery, snapshot-based inactive workspace cleanup and full session deletion remain pending.
- The rebuilt Linux arm64 server/Worker test binary passed all three replacement-Worker recovery cases (ready, partial General Chat cleanup and mismatched-journal explicit retry) plus targeted recovery cancellation in the non-root network-disabled container. That fixture has no Git, so Linux Git-worktree recovery is not claimed. Windows amd64 CLI cross-compilation passes; native Windows recovery remains unverified.

- Codex internal thread control now separates stable discovery from explicit thread-mode native requests, pins legacy history without model fallback, applies additive instructions and validates effective model/provider/effort/tier/cwd/permissions/approval. Fixtures cover no-send unsupported selections, retained native defaults, root-thread serialization, canceled waiters, foreign resumed identities, changed/malformed replies, redacted errors and late acknowledgments without automatic retry. This is not yet public session dispatch or immutable first-execution snapshot/account routing integration.
- Native macOS arm64 Codex `0.151.0` testing exposed observation fields absent from the stable generated schema, native default paginated history and the native rule that no resumable rollout exists before the first user input. The adapter validates this exact release's full generated schema observations and explicitly selects legacy history. The opt-in test then passed creation, exact model/effort/base-plus-additive instructions, one local scripted Responses exchange, metadata inspection, owned process closure and resume of the same native identity. No external provider or user credential was used; full native turn controls, real account fidelity and other harnesses remain unverified.
- Complete delidev Go race tests and vet pass. The final inspection-authority refinement also passes the affected harness race suite and native Linux arm64 fixture. All native thread-control fixtures passed in the non-root network-disabled Linux container; this does not prove an installed Linux Codex runtime or real account. Windows amd64 CLI cross-compilation passes, with native Windows execution still unverified. No protocol schema or generated bindings changed, and no repository-owned `dist` output was generated.

- Codex internal turns now preserve immutable effective settings and input identity/content/turn bindings, use native Execute/Plan modes, require exact active-turn Steer and keep interruption separate from terminal/cleanup proof. Core message/lifecycle events normalize without raw error bodies; rich/unknown/subagent extensions remain private and JSON-excluded. Controlled native fixtures cover rejected and uncertain sends, late acknowledgments, no fallback/replay, paused Stop/failure, canceled-reader retention, terminal conflict and buffered events after connection failure. Public dispatch, durable transcripts, first-execution snapshots and selected-account routing remain pending.
- Native macOS arm64 Codex `0.151.0` passes the private local-provider suite through the new adapter methods: Execute and persisted-thread resume, native Plan, same-turn Steer with both original input identities, and native interrupted outcome. Interruption alone did not close the pending model HTTP request; owned process closure did, and the test requires that cleanup before completion. These scripted loopback exchanges use no external inference provider or user credentials and do not validate tools, interactions, usage or real account fidelity.
- Complete delidev Go race tests and vet pass; subsequent input-correlation and connection-failure refinements pass the affected harness race suite, the explicit native suite and final package vet. The new content/owning-turn regression also passes under the race detector. All controlled Codex adapter fixtures passed on native Linux arm64 in the non-root network-disabled container, with installed-harness tests skipped; installed Linux Codex and native Windows execution remain unverified. Final Windows amd64 CLI cross-compilation passes. No protocol schema/bindings changed or repository-owned `dist` output was generated.

- The private initial-execution transaction primitive now resolves current exact configuration/templates, selects an eligible account using durable routing state, and commits the immutable configuration/digest, original account/connection, full route, first input/native request identities and routing advancement together. Production sessions remain blocked: no public caller supplies native readiness or dispatches these claims yet. No account secret, native process, proxy credential or inference request is created by the primitive.
- Real temporary SQLite tests pass for concurrent rotation, failed-publication rollback, current project restrictions/revoked accounts, queue order, edit/claim races, creation-to-dispatch template edits, immutable replay after restart and contradictory routing owners. Exact Unicode/whitespace/options and instruction-overflow tests pass. The full delidev Go race suite and vet pass; all new store execution tests also pass in the non-root network-disabled Linux arm64 container. Readiness/accounts/workspaces in these tests are simulated records and are not native execution evidence. No protocol schema/bindings changed or repository-owned `dist` output was generated.

- The server now registers the native relay with digest-only Worker execution grants bound to exact claimed ownership and the server process epoch. Real authenticated Connect/SQLite/loopback HTTP fixtures cover immutable registration/replay, foreign-reference denial, job/session/Worker/device/account revocation, replaced connections/instances, expired heartbeat, server restart denial and account disconnect joining in-flight relay cleanup before key deletion. These tests seed simulated native-ready assignments; public session execution and actual Worker token delivery remain pending, and no installed harness or real account was used.
- Schema v7 adds execution grant/reference indexes, immutable bindings and deletion cascades. New SQLite tests preserve v6 assignments and synchronized pre-migration backups, retain exact reference scopes across restart, reject binding replacement and remove owned authority on deletion. The full delidev race suite passes, final affected server/store race tests and package vet pass, Buf lint passes, and all 32 generated files reproduce exactly. No repository-owned `dist` was generated.

- Codex API configuration now pins the paired-origin proxy route through native session flags, supplies only an execution token in the explicit environment, excludes it from child shells and verifies effective provider authority before binding a thread. Closed validation rejects alternate endpoints/keys/auth/header/query authority while preserving native retry defaults. The pinned config reader serializes all optional provider fields as null; the adapter accounts for that observed behavior without discarding populated authority.
- Opt-in macOS arm64 Codex `0.151.0` tests pass direct private proxy configuration and actual authenticated server registration/relay composition into a scripted loopback provider: exact selected model/input/output, server-only upstream key injection, one request, owned native closure, and no raw execution/upstream token in retained native files. Job/readiness records in the server test are simulated; public CLI/Worker dispatch and external-account inference are not established. Existing native Execute/Plan/Steer/interruption tests also pass. Affected harness/proxy/server race tests and package vet pass; ordinary tests never launch installed harnesses.

- `PublishExecution` now connects typed native core events to atomic server transcript/session state: exact Worker/job/input ownership, monotonic sequence, distinct observed settings, one-time queue-capacity release, unique native message mapping, bounded text and immutable terminal facts. Worker publication synchronizes a locked outbox before RPC and replays only the same event identity after a lost acknowledgment; new events cannot replace pending uncertainty. Core Codex normalization supplies stable product message IDs and explicitly reports unsupported extensions as unhandled. Public dispatch/job execution remains pending.
- Real Connect/SQLite tests cover concurrent receipt replay, restart without duplicated deltas, scope/sequence rejection, native-item duplication rollback, partial-message failure, completed-message gates, bounded accumulation without truncation, late completion after Stop/Archive, and Worker outbox recovery after deliberately lost acknowledgment. Schema v8 migration preserves v7 execution grants and backups. The expanded opt-in macOS arm64 Codex `0.151.0` test passes the actual relay plus Worker core-event outbox plus server transcript, with exact input/model/native identities and no persisted native credential. Readiness is simulated and rich events remain unimplemented; no external provider or user account was used.
- The complete delidev Go race suite passes; package vet passes after replacing test-only protobuf value copies with `proto.Clone`. Buf lint and exact reproduction of all 32 generated files pass. Native Linux arm64 server tests for grants/revocation/core publication/outbox replay and store migration/reference ownership pass in the non-root network-disabled container. Those Linux tests use controlled native-readiness records and no installed harness. No repository-owned `dist` was generated.

- Codex native metadata now validates root identity, exact effective settings and active mode, rejects remote-control activation/model rerouting, and publishes redacted warning kinds. Native token usage retains cumulative versus last-request values, unavailable fields and assignment-derived event-time attribution as immutable observations; it does not compute charges, actual costs or aggregate usage. Populated quota, tools/interactions and other native extensions remain explicitly unhandled. The installed source's context-limit counter filling/reset behavior is documented rather than treated as provider-billed usage.
- New native/Connect/SQLite regressions cover malformed/missing/negative/overflow counters, counter resets, immutable usage IDs and provenance, exact lost-acknowledgment replay, forbidden cross-kind payloads, foreign thread/turn ownership, settings/mode drift, notice redaction and unavailable quota. Complete delidev Go race tests and vet pass. The macOS arm64 Codex `0.151.0` private suite still passes thread persistence/resume, Execute/Plan/Steer/interruption; the actual server-relay/outbox smoke handles every event in its simple scripted turn and stores exact token observations. Native readiness remains seeded in that composition; ordinary CLI/Worker dispatch, external accounts and full event-family acceptance are not established. No protocol schema/generated bindings or repository-owned `dist` output changed.

- A private first-execution workspace lease now validates exact manifest/native Git ownership, preserves uncommitted files, retains the session preparation/recovery lock and synchronizes a separate durable execution claim. Preparation/recovery reject unresolved claims after Worker lock loss, even when workspace files are missing. Owned execution-job cleanup closes the claim; a missing process index retains uncertainty. Temporary real Git/process tests cover concurrent claims, unchanged source checkout, dirty worktree preservation, changed manifest/HEAD/refusal, forged owned-path suffixes, existing owner refusal, cancellation, Worker-loss/missing-workspace behavior, closed-claim non-replay and idempotent native cleanup. This is not ordinary Worker dispatch, Local execution, later-turn/resume or native recovery integration.
- After workspace lease integration, the complete delidev Go race suite and vet pass. The lease tests, including real Git and owned process cleanup, also pass on native Linux arm64 as a non-root user in a network-disabled disposable `node:24-bookworm` container (the existing Secret Service image lacked Git, so its initial Git case could not run). Native usage/notice publication and lost-acknowledgment tests pass separately in the non-root network-disabled Secret Service container. The current Windows amd64 CLI cross-build passes; no native Windows or installed Linux Codex execution is claimed. No protocol schema/generated bindings or repository-owned `dist` output changed.

- The actual outbound Worker loop now executes an immutable first Codex assignment through exact executable selection, retained private runtime, prepared-workspace lease, digest-only grant registration, native thread/input acceptance, ordered typed publication and owned terminal cleanup reporting. The server verifies completion against the exact retained native identities/cursor/outcome, uses reference-only receipts and preserves uncertainty on missing proof. Worker replacement/revocation atomically propagates session/input recovery without rewriting prior terminal observations or freeing unacknowledged input.
- Opt-in macOS arm64 Codex `0.151.0` tests pass under the race detector for the complete real Worker attach/job loop into a scripted loopback provider, exact model/input/transcript, server-only key injection, no bearer in retained runtime, native/process-lease cleanup and completion report. Live revocation during inference also cancels HTTP/native work, joins process cleanup and retains the interrupted journal/server recovery without replay. First-selection/account readiness remains seeded; these tests do not establish public CLI dispatch, real provider-account inference, rich event support or other installed-harness platforms.
- Complete delidev Go race tests and vet pass after Worker integration. Completion proof/replay and Worker-loss state regressions also pass in the non-root network-disabled Linux arm64 Secret Service container. The Windows amd64 CLI cross-build passes, without claiming native Windows execution. No protocol schema/generated bindings or repository-owned `dist` output changed.

- Account disconnect now atomically pauses and requests cancellation of every unfinished native assignment selected on that account, with bounded pagination across queued/claimed/uncertain states. Undispatched jobs become canceled, while claimed revisions/digests remain unchanged for Worker journals and reconnect. Other accounts, candidate-only routing links and terminal history are excluded. Old request replay preserves executions and credentials on a replacement connection. Relay/key cleanup remains distinct from owned Worker cleanup and retained session recovery.
- Real Connect/SQLite regressions pass for more than one cancellation page, immutable claimed envelopes, preserved queue capacity/ownership, excluded historical/other-account jobs and replay after reconnecting the account. An opt-in macOS arm64 installed Codex test passes under the race detector: account disconnect during a scripted loopback inference cancels the actual Worker process, closes the HTTP request, journals/reports the interrupted result and retains server recovery without replay. Readiness is seeded; no external inference or user account was used. The full delidev race suite and vet pass.
- The multi-page account execution cancellation/replacement-receipt regression also passes on native Linux arm64 in the non-root network-disabled Secret Service container. No protocol schema/generated bindings or repository-owned `dist` output changed.

- First-native session Stop/Archive now resolve unique job ownership, atomically pause/request cancellation and retain exact claimed envelopes. The Worker retains one interruption request before native send and allows a 15-second terminal-publication grace period only under the original live stream after durable input binding; stream loss and earlier cancellation still kill immediately. Verified terminal/process cleanup completes pending Archive for the current agent-only resource graph; completed outcomes and paused Restore survive old control receipt replay. Unconfirmed acceptance/cleanup remains recoverable and never authorizes resend.
- Real Connect/SQLite tests pass for running/unaccepted/terminal/cleaned success/failure controls, exact receipt replay, immutable job content, Archive waiting for cleanup, missing-proof recovery and preserved outcome/paused Restore. The complete delidev race suite and vet pass. The control regressions also pass in the non-root network-disabled Linux arm64 Secret Service container, and the Windows amd64 CLI cross-build passes.
- Opt-in macOS arm64 installed Codex `0.151.0` Worker tests pass under the race detector for normal completion, Worker revocation, account disconnect, explicit Stop and Archive during a scripted loopback inference. Stop/Archive publish native terminal facts, prove owned cleanup and retain correct visibility/pause; lost Worker authority retains recovery. Readiness is seeded and no external inference/user account is used. Public creation/dispatch, richer event families, later turns and explicit native recovery remain pending. No protocol schema/generated bindings or repository-owned `dist` output changed.

- The pinned Codex parser now retains closed command/file-change lifecycle observations, streamed command output, native terminal-input observations and patch updates. It preserves nullable counters, advisory source/action/plugin metadata and aggregate output separately from deltas; tool failure/decline is not whole-turn failure. Cross-kind payloads, unknown variants and mismatched lifecycle ownership are rejected. Durable product publication remains pending, so production execution still treats these typed events as unhandled rather than dropping them.
- Native-wire fixtures pass under the race detector for nullable/default fields, invalid unions, foreign-thread privacy, lifecycle mismatch and distinct update families. The complete delidev Go race suite and vet pass; the final source-null refinement passes the targeted race tests. The same final parser fixtures pass in the non-root network-disabled Linux arm64 Secret Service container. These parser fixtures do not invoke an installed harness or prove native tool execution. No protocol schema/generated bindings or repository-owned `dist` output changed.

- Codex command/patch observations now publish through the durable Worker outbox into dedicated tool transcript records. Shared native-item uniqueness and completion gates prevent identity replacement, partial-tool success and late writes. Native start/completion snapshots, nullable streamed output, final aggregate output, input observations and patch revisions remain distinct; tool failure/decline never implies whole-turn failure. Complete publication/document/history bounds reject updates atomically without truncating retained content or consuming sequence.
- Real Connect/SQLite regressions pass for native-to-product mapping, advisory metadata, nullable counters, patch revisions, whole-turn outcome independence, duplicate/cross-family identity, wrong scope/phase/payload, immutable command/source, completion gates, output/document limits and exact outbox restart replay after a lost acknowledgment. The full delidev race suite and vet pass. These regressions also pass in the non-root network-disabled Linux arm64 Secret Service container; the Windows amd64 CLI cross-build passes.
- An opt-in macOS arm64 installed Codex `0.151.0` Worker test now verifies a real private POSIX `printf` command, its result returned to the same scripted loopback provider, server-retained native command/exit/output and verified owned cleanup. The observed short command emits final aggregate output without output deltas, so the product retains the stream as unavailable rather than manufacturing an empty or copied stream. Existing Worker completion/revocation/account-disconnect/Stop/Archive native tests also pass under the race detector. Account readiness/first selection remain seeded; no external provider or user account was used. Native file-patch execution, interactive command streaming, other installed-harness platforms and public dispatch remain unverified. No protocol schema/generated bindings or repository-owned `dist` output changed.

- The pinned Codex parser now distinguishes native plan/reasoning lifecycle, plan text deltas, indexed reasoning summary/content updates, structured turn-plan steps and aggregated turn diffs. It preserves authoritative completed plan replacement, native part indices/defaults and absent data without inferring turn success or filesystem/review authority. Unknown/malformed/cross-kind payloads fail; foreign observations remain private and late events retain their classification. Dedicated durable publication for these new families remains pending.
- Artifact parser fixtures pass under the race detector and in the non-root network-disabled Linux arm64 Secret Service container; package vet passes. The concurrent full race run hit an existing Worker subprocess-start five-second deadline while unrelated host build activity was present. The isolated Worker termination regression passed without code changes, followed by the complete race suite with package execution serialized (`-p 1`). These fixtures do not invoke an installed harness or establish native artifact acceptance. No protocol schema/generated bindings or repository-owned `dist` output changed.

- Native plan/reasoning artifacts now publish through the durable outbox and shared item-identity/completion index. Start snapshots, indexed/ordered stream deltas and authoritative completion remain separate, including final plan replacement and explicit empty versus missing reasoning content. Turn-plan/diff updates create immutable progress transcript records without native item identities; latest pointers never rewrite history or grant review/filesystem/terminal authority. Publication/document/history bounds fail atomically without truncating content.
- Real Connect/SQLite tests pass for artifact/progress mapping, exact plan replacement, reasoning index preservation, immutable plan/diff history, empty diff observations, null-content refusal, cross-family identity/scope/kind/payload rejection, incomplete-item success gates, size bounds and exact outbox reopen/replay after a lost delta acknowledgment. The full delidev race suite passes with package execution serialized (`-p 1`), and vet passes. Artifact/progress server regressions pass on native Linux arm64 in the non-root network-disabled Secret Service container; the Windows amd64 CLI cross-build passes.
- An opt-in macOS arm64 installed Codex `0.151.0` Worker test now invokes native `update_plan` against the scripted loopback provider, retains exact plan explanation/step/status and verifies native terminal publication plus owned cleanup. The complete installed Worker suite also passes under the race detector for normal completion, command execution, revocation, account disconnect, Stop and Archive. Account readiness/initial selection remain seeded; no external inference/user account is used. Final Plan artifact, reasoning and turn-diff native acceptance remain fixture-only, and public dispatch/native recovery are still pending. No protocol schema/generated bindings or repository-owned `dist` output changed.

- The pinned Codex parser now normalizes root `request_user_input` server requests with distinct numeric/text native identities, per-arrival UUIDs, exact questions/options and blocking/freeform/secret semantics. Deprecated auto-resolution timing is retained without creating timers or answers. Original raw pipe-reply authority remains private. Malformed/ambiguous/cross-kind data and foreign-turn ownership are rejected; foreign-thread requests remain private. Response delivery, resolution, protected secret answers, durable interaction/inbox storage and public RPC/CLI remain pending; production execution still stops on this unhandled typed family.
- Question fixtures pass under the race detector for identity namespaces, arrival binding, nullable options, default/null flags, exact content, secret/freeform metadata, wrong-kind fields and malformed requests. They also pass in the non-root network-disabled Linux arm64 Secret Service container. The complete delidev race suite passes with package execution serialized (`-p 1`), and vet passes. These are adapter/schema fixtures only, without installed-harness question or approval execution. No protocol schema/generated bindings or repository-owned `dist` output changed.

- The native wire now has exact-arrival request retirement for adapter-observed resolution/cancellation. It sends no answer, rejects replacement tokens and in-flight response ownership, and distinguishes an already absent request without claiming semantic acceptance. Real owned-process fixtures verify that retired requests cannot reply, numeric-ID reuse retains a new arrival, stale retirement cannot delete it and the only transmitted response belongs to the newer request. In-flight ownership tests pass under the race detector.
- Affected harness/Worker/server race suites and delidev vet pass. The retirement fixtures also pass in the non-root network-disabled Linux arm64 Secret Service container. Pinned source inspection confirms Codex resolves a question request before parsing/submitting its answer and also resolves failure/cancellation; docs now forbid treating that notification as acceptance. Codex response/resolution integration remains pending. No protocol schema/generated bindings or repository-owned `dist` output changed.

- The private Codex adapter now retains immutable question ownership, validates typed non-secret replies, serializes concurrent responses and normalizes exact native closure separately from transmission. Stop/turn completion/late arrivals cannot reply; request-ID reuse is rejected because native resolution has no arrival token. Original question payloads are bounded and released at closure. Memory-only answers are excluded from ordinary serialization, and secret-marked requests refuse the ordinary response path before claiming/sending. Durable Worker/server interaction publication, protected secret responses and public inbox/CLI remain pending.
- Owned-process regressions pass for concurrent replies, caller-mutated options/secret flags, wrong turn, numeric/text ID separation, foreign/unknown resolution, closure without answers, Stop/terminal/late refusal, native-ID reuse, pre-send cancellation, response shape/size limits and retained-identity/question-memory bounds. A deliberately blocked native pipe verifies retained response identity and recovery after uncertain delivery, with no replay. The full delidev race suite passes with package execution serialized (`-p 1`), and vet passes. The question regressions also pass on native Linux arm64 in the non-root network-disabled Secret Service container.
- An opt-in macOS arm64 installed Codex `0.151.0` test receives a native Plan-mode question, transmits one answer, observes request closure, confirms the exact answer in the next scripted loopback model request and proves owned cleanup. No external account/provider is used. The native profile emits no question `functionCallOutput` item in this test; closure is therefore retained as transmission/closure evidence without falsely clearing the adapter's acceptance gate. Native acceptance/history reconciliation and other installed platforms remain unverified. No protocol schema/generated bindings or repository-owned `dist` output changed.

- Codex question requests, native waiting changes and unanswered closure now publish through the synchronized Worker outbox into server interaction records. Original question content survives native/terminal closure, and waiting-clear never answers or resolves a request. Schema v9 adds execution-scoped unique native request ownership and bounded open-request count/bytes, with synchronized backups preserving v8 message history/indexes and earlier migrations. Native transmitted response publication is explicitly rejected until its dedicated durable owner claim/control path exists. Existing generic authenticated resource reads expose retained interactions; response RPC/CLI, protected answers and inbox read state remain pending.
- Real Connect/SQLite regressions pass for original question order/flags/content, numeric/text identity separation, independent waiting/closure/outcome, duplicate/replaced/foreign requests, cross-kind answer/approval payload rejection, null/size validation, atomic open-request bounds and terminal closure, and exact outbox reopen/replay after lost acknowledgment. Additional targeted race tests verify rollback of question closure when terminal publication fails and refusal to publish a native response without an owner claim. The complete delidev race suite passes with package execution serialized (`-p 1`), and vet passes. Native Linux arm64 publication/migration fixtures pass in the non-root network-disabled Secret Service container. Windows amd64 cross-build passes without claiming native Windows execution.
- An opt-in macOS arm64 installed Codex `0.151.0` Worker test now retains a real Plan-mode question and waiting state on the server, then verifies explicit Stop preserves the question, closes it without an answer, publishes interrupted terminal state and proves owned cleanup. The initial run exposed a fixture that stored its queued input as Execute regardless of configured Plan mode; the fixture now preserves its selected input mode, and startup failures report the typed job state promptly. The final question test and complete eight-test installed Worker suite pass under the race detector. Account readiness/initial selection remain seeded, and all provider traffic uses a private scripted loopback endpoint; public dispatch, semantic response acceptance, approvals and other harness/platform acceptance remain unverified. No protocol schema/generated bindings or repository-owned `dist` output changed.

- A private server transaction now accepts one non-secret question response against its exact original revision and live execution/account/Worker grant scope. It retains exact answer arrays and explicit empty answers, rejects secret/cross-kind/malformed/oversized input before ordinary persistence, and distinguishes queued acceptance from native delivery. Native/terminal closure cancels an unsent response while preserving its original content; reference-only receipt replay cannot requeue it. Grant lookup by immutable job returns scope metadata without its token digest. Public response APIs and active-Worker claim/delivery remain pending.
- Domain and real Connect/SQLite-backed publication fixtures cover concurrent distinct responses, original content retention, canceled response replay, current-revision duplicate refusal, current pause/archive/recovery/native ownership/cleanup/job cancellation, replaced/stale Worker, revoked device/account, replaced connection/server epoch, and rejected-response atomicity. The complete delidev race suite passes with package execution serialized (`-p 1`), and vet passes. This increment establishes private transaction behavior only; no installed-harness response integration or new platform acceptance is claimed. No protocol schema/generated bindings or repository-owned `dist` output changed.

- The server work stream now carries bounded, deduplicated metadata-only response controls for its active job, independently of later queued assignments. The new owning-Worker `ClaimQuestionResponse` RPC records exact original response/interaction revision and claim/job/device/instance ownership; reference-only retries recheck current authority before returning non-secret answers. Native closure after a claim retains uncertainty and paused recovery independently of terminal outcome. Worker loss cancels queued responses, marks claimed replies uncertain and preserves unknown native closure. The production Worker explicitly refuses response controls until its send-intent/delivery controller is integrated; owner response acceptance remains private.
- Real Connect/SQLite tests verify concurrent same-request claim replay, refusal of a replacement claim or foreign/stale control, answer-free receipts, denied content retrieval after cancellation/replacement/stale heartbeat/revocation, original question/answer retention, uncertain native/terminal closure, queued/claimed Worker loss, and response controls bypassing a blocked execution while later jobs remain unclaimed and targeted cancellation remains live. The complete delidev race suite (`-p 1`) and vet pass. Buf formatting/lint and breaking-change checks pass, and a second generation reproduces every generated protocol file byte-for-byte. No installed-harness response delivery, new platform acceptance or repository-owned `dist` output is claimed.

- The actual Worker now receives bounded response controls during native execution, retains an immutable metadata-only claim journal before RPC, validates returned original scope, synchronizes send intent before one native response and records delivery before ordered outbox publication. Its lifecycle-joined controller preserves the separate Stop interruption grace period; stream loss cancels owned work. Existing journals, replaced claims, duplicate controls and lost acknowledgments cannot resend an answer. A dedicated delivery event binds original claim/device/job/interaction/thread/turn/item and records `not-sent`, `transmitted` or `uncertain` without answer content. Pipe transmission, native closure, semantic acceptance and terminal/cleanup outcome remain separate facts.
- Server/Worker regressions pass for exact delivery and response accounting, receipt replay, unclaimed/mismatched/cross-kind observations, answer-bearing event refusal, original answer preservation, closure/terminal acceptance gates, metadata journals preceding claim/send/publication, lost claim/publication acknowledgments, foreign native identity, preexisting journals, uncertain writes, proven pre-send refusal, and controller joining during Stop without canceling native grace. The complete delidev race suite (`-p 1`) and vet pass. All question server/controller fixtures also pass in the non-root network-disabled Linux arm64 Secret Service container; the Windows amd64 CLI cross-build passes without claiming native Windows execution.
- An opt-in macOS arm64 installed Codex `0.151.0` test now drives private server response acceptance through the actual active-job stream, Worker claim/journal, native question reply, exact answer in the next scripted loopback model request, durable delivery/closure and verified terminal cleanup. Product state retains required semantic-acceptance recovery despite observed successful turn completion; no acceptance proof is fabricated from closure or fixture-side model receipt. The complete nine-test installed Worker suite passes under the race detector. Account readiness/first selection remain seeded; public owner response RPC/CLI, protected answers and native acceptance/history reconciliation remain pending. No protocol schema/generated bindings or repository-owned `dist` output changed in this increment.

- Public owner/paired-client `InteractionService.RespondQuestion` and `interaction respond` now accept non-secret answers against the exact original question revision and current execution authority. The mutation request UUID is the response UUID; actor-bound canonical receipt identity and reference-only results preserve current-state replay after closure without requeuing. The CLI reads and validates original questions before sending, accepts answer documents through file/stdin, and keeps credential stdin separate. Server acceptance additionally checks the native encoded wrapper size before persistence.
- Real Connect/SQLite tests pass for competing owner/client responses, changed actor/content refusal, canonical replay after terminal closure, original question/answer retention, answer-free receipts, Worker/anonymous/direct-call denial, revocation before receipt replay and commit, invalid input atomicity, and a domain-sized answer exceeding the native wire bound. CLI transport tests pass for authenticated file/stdin input, exact ordered/freeform answers, old-revision receipt replay after closure, and malformed/secret/wrong-kind refusal before the response RPC. The complete delidev race suite (`-p 1`) and vet pass; Buf formatting/lint, compatibility and byte-for-byte reproduction of all 32 generated protocol files pass.
- The opt-in macOS arm64 Codex `0.151.0` Worker response test now uses the public owner RPC through native reply, scripted loopback model receipt, durable delivery/closure and verified cleanup, and passes under the race detector. It retains required semantic-acceptance recovery separately from successful native outcome. Account readiness/first selection remain seeded; this does not establish public first dispatch, protected answers, inbox read state, native acceptance/history reconciliation or other platform/harness acceptance. No repository-owned `dist` output was generated.

- Native question and terminal publications now atomically create unique unread inbox references. Entries have independent IDs/revisions and preserve bounded native completion/failure/interruption evidence separately from session outcome and owned cleanup. A private read-state primitive changes only the inbox entry; reads, answers, closure and source receipt replay never implicitly mark it read or replace its source. Existing authenticated resource list/get/snapshot/event paths expose these records. Schema v10 backs up and backfills retained questions plus the available terminal progress from v1–v9 without rewriting original content or inventing missing history.
- Domain/store/server race regressions pass for closed metadata unions, source uniqueness, original answer retention, stale/no-op read revisions, response controls remaining claimable after mark-read, independent native/product outcomes, terminal receipt replay preserving read state, and publication rollback leaving no orphan inbox entries. Migration tests cross the 200-record page boundary, preserve v9 backups and native indexes, retain stable IDs on reopen and roll back generated records/indexes/schema when source evidence is malformed. The complete delidev race suite (`-p 1`) and vet pass; targeted migration tests also pass after refining cancellation/I/O error preservation.
- All nine opt-in macOS arm64 Codex `0.151.0` Worker tests pass under the race detector, with successful native runs retaining unread terminal evidence and the public question-response run retaining its separate unread original question. New inbox migration/publication fixtures pass on native Linux arm64 in the non-root network-disabled test container; Windows amd64 cross-build passes without claiming native Windows execution. Public joined inspection/read-state RPC/CLI, desktop notification delivery, protected answers and semantic acceptance/history reconciliation remain pending. No protocol schema/generated bindings or repository-owned `dist` output changed in this increment.

- Owner/paired-client `InboxService` and `inbox list/get/inspect/mark-read/mark-unread` now expose joined current inbox/session/original-question state. Read-state mutations bind the actor, inbox revision and explicit target state into reference-only receipts, validate source scope before commit and return current views on replay without reapplying earlier marks. Filtered cursor pages bind the inbox event epoch and preserve complete original content under a 3 MiB joined-view bound; source changes are read in the same page transaction without changing response authority.
- Real Connect/SQLite tests pass for concurrent exact receipt replay, actor/state substitution refusal, current unread/answer read-back after old mark-read replay, shared client observations, session/source/read-state filters, cursor filter mismatch and membership invalidation, Worker/anonymous/revoked client denial, stale-auth transaction checks, invalid enum/page limits, complete large-document pagination and rollback when the original source is missing. CLI/server tests retain archived/closed sources, use independent inbox revisions, generate/reuse request identities, filter unread entries and reject malformed/stale operations. The full delidev race suite (`-p 1`) and vet pass; Buf formatting/lint, compatibility and byte-for-byte reproduction of all 32 generated protocol files pass.
- The installed macOS arm64 Codex `0.151.0` question-response Worker test now lists its question through the public inbox RPC, explicitly marks it read and answers through the original interaction revision. Native completion retains that read state alongside independent unread terminal evidence and the existing semantic-acceptance recovery gate. This test passes under the race detector with a private scripted loopback provider and seeded first execution/readiness. All new inbox RPC and CLI fixtures pass on native Linux arm64 in the non-root network-disabled container; Windows amd64 cross-build passes without native Windows acceptance. Desktop notification delivery/deduplication, protected answers and semantic native acceptance/history reconciliation remain pending. Issue #964 remains unchanged at `2026-09-23T07:35:34Z`; no repository-owned `dist` output was generated.

- 2026-09-25, macOS arm64, installed Codex `0.151.0`: opt-in `TestManualNativeQuestionHistory` passed against private legacy and paginated threads using only a scripted loopback provider. Both emitted one exact question-answer raw output after raw-event opt-in and zero canonical function-output completions; legacy pagination returned native unsupported, and paginated history contained two user/assistant items with no question output. This is protocol evidence only: production raw-event acceptance and historical recovery are not yet implemented. The existing native question-response smoke also passed. No user account or external inference was used.

- 2026-09-25: enabled the pinned Codex legacy profile's raw supplement feed for exact live question acceptance. Adapter fixtures verify immutable answer digests, ordered/explicit-empty answers, original call/turn/response binding, duplicate suppression, mismatched/null/unknown/extra-field refusal, call-ID reuse rejection, content-free observations and preservation of unrelated recovery. Canonical usage is not duplicated from raw response supplements.
- Real Connect/SQLite tests verify claim-bound acceptance before/after native closure, one-time outstanding-response decrement, original content/transport preservation, atomic rejection of foreign or content-bearing evidence, retained uncertainty after prior transport failure and exact durable Worker-outbox replay after a lost acceptance acknowledgment. A matching native observation does not imply disk synchronization, general history recovery or process cleanup.
- Native macOS arm64 Codex `0.151.0` adapter tests passed with private scripted providers, including raw live acceptance, legacy/paginated investigation, thread persistence/resume, Plan/Steer/interruption and proxy isolation. Real Worker completion/command/plan/revocation/account-disconnect/Stop/Archive checks passed; raw cancellation output initially exposed a Stop regression, which was corrected to retain it as non-acceptance evidence. Native unanswered-question Stop and public inbox/read/respond through native acceptance/cleanup were rerun successfully. No user account or external inference was used; native readiness/public first dispatch remain seeded/pending.

- After live acceptance integration, `go test -race -p 1 ./cmds/delidev-cli/...` and package vet passed on macOS arm64. The native Linux arm64 disposable-container adapter/Connect acceptance fixtures passed with networking disabled. Windows amd64 CLI cross-compilation passed; this is not native Windows evidence. No generated protocol files or repository-owned `dist` output changed.
