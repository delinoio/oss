# async-commit-hook implementation evidence

The complete [issue requirements](cmds-async-commit-hook-requirements.md) remain intact. This matrix was created before runtime implementation and maps them to the delivered source and executable evidence. Verification below was performed on September 19, 2026.

The initial product version is `0.1.0` across the executable, app/client packages, installers and release metadata. Configuration, state, CLI JSON and API schema versions remain `1`. The `0.1.0` artifacts passed the full Go suite, frontend tests/build, release/workflow checks and all six target builds; native `ach version --json` reports `0.1.0`.

## Completion boundary

The owner approved exactly two changes to the original acceptance criteria: the real-machine campaign across all six targets is excluded, and public release/site deployment is prepared but not executed. Cross-builds, local automated integration and a nonpublishing release dry run remain required and passed. These amendments do not imply native Windows/Linux runtime qualification.

One requested validation remains pending: desktop Edge could not be launched because it is not installed/available through this environment's browser tools. The owner was asked whether to connect Edge or explicitly exclude that validation. No answer or additional exclusion is assumed. Chrome and automated accessibility tests passed. Accordingly, this document does not claim every original validation step is complete.

The third PR repair pass confirmed a Unix daemonization-before-sampling defect. The fourth pass replaces sampled ownership with kernel-backed per-check supervision and verifies detached descendants across normal exit, cancellation, replacement and recovery. The original requirement was retained; this was never an owner-approved exclusion. See the fourth-pass evidence and recovery limits below.

## Requirement-to-evidence matrix

Paths in the implementation column are relative to `cmds/async-commit-hook/internal/core` unless otherwise stated. Named Go tests are committed under that directory or `cmds/async-commit-hook/integration`.

| Requirement group | Implementation | Tests and completion evidence | Status |
| --- | --- | --- | --- |
| Runtime, ownership, six targets, English, sole executable | `model.go`, platform process/files adapters, command main, app and client packages | All six CGO-free archives built; only `ach`/`ach.exe` in each; local real-binary integration | Implemented; local/build checks passed |
| Strict v1 configuration and dependency graph | `config.go`, `service.go`, `runner.go` | `TestConfigurationRejectsInvalidGraphs`, no-applicable, optional, dependency and report tests; unknown fields/versions rejected | Passed |
| Trust registration, worktrees and clones | `store.go`, `git.go`, `service.go` | `TestWorktreeIdentityAndSeparateClone`; common-dir identity and per-worktree IDs, separate clones remain separate | Passed |
| Safe hooks and durable receipts | `hooks.go`, CLI submission, `Start` | `TestPostCommitDetachedModesAndWaitExpiry` proves Git returns before a barrier-controlled command in both modes; dedup/idempotency and atomic concurrent initialization tests; native/custom/Lefthook ownership tests | Passed |
| Source isolation, unsupported sources, recursive-hook prevention | `git.go` raw-blob materialization, managed hooks/flag | Committed source survives dirty worktree and branch switching; `TestSourceRefusesSubmoduleAndLFSBeforeCommands`, `TestRawCheckoutDoesNotInvokeSourceFilters`; workspace cleanup assertions | Passed |
| Group parallel/queue/replace and descendants | `runner.go`, process adapters, transactional check claims | FIFO, named groups, detached/reparented-child replacement, durable start barrier, kernel emptiness proof, lost-worker recovery and unrelated-check isolation | Passed on local macOS and Linux container; real six-target qualification excluded |
| Daemon/on-demand parity, startup, drain/force, viewer isolation | `lifecycle.go`, `api.go`, `Work` | Six concurrent starts converge to one daemon; `TestDrainAndForcedStop`, `TestTemporaryViewerExitLeavesWorkerAndHistory`; query-only MCP starts no daemon | Passed |
| Recovery, no replay, storage failures | `RunOne`, process identity snapshots, lifecycle locks | `TestRecoveryInterruptsWithoutReplayAndPIDReuse`, `TestStorageFailureReapsOwnedCommands`; pending requests recover through worker startup | Passed |
| SQLite records, evidence, retention and unavailable source | `store.go`, `query.go`, owned artifact files | `TestPruneCannotResurrectSuccess`, active inherited-evidence retention, dry-run and expired tombstones; source availability is exposed separately from history | Passed |
| Exact final gate and compatible latest acceptance | `service.go` common gate, accepted sequence/fingerprint | Latest queued result defeats prior success; changed declared public input produces no compatible gate; missing/tampered/pruned evidence rejected; no-applicable and optional-only cancellation tested | Passed |
| Required/optional checks and report semantics | `runner.go`, `reports.go` | Optional failure remains visible while required gate passes; required report absence/parse/failure defeats exit zero; JUnit and Go JSON passing/failing/incomplete/malformed fixtures | Passed |
| Failure identity, comparison and reruns | `query.go`, `service.go` rerun closure/inheritance | `TestComparisonAndEnvironmentCompatibility` checks new/continuing/resolved failures and incompatibility; rerun tests preserve predecessor and original successful-evidence provenance | Passed |
| Evidence collection and cleanup diagnostics | `reports.go`, `runner.go` finalization | Root-confined report reads, evidence digest checks, `TestCleanupFailureRemainsDurableAndFailsGate`; evidence remains available after cleanup failure | Passed |
| Inbox, explicit acknowledgement and context recovery | store/application service, guide | Idempotent acknowledgement and dedup test; status/inbox reads do not ack; real MCP ack shares SQLite state and leaves gate unchanged | Passed |
| Full CLI surface, JSON, stable errors and exit statuses | `internal/cli/cli.go` | Real CLI integration for init/run/status/wait/check/daemon/ui/pre-push/update ownership; strict parser and shared service tests; wait expiry returns 4 and execution continues; stdout parses as versioned JSON | Passed |
| Pre-push policies and actual multi-ref tips | `prepush.go`, explicit hook option | `TestPrePushAllTipsUsesExactSHA` covers non-HEAD tips, multiple branches, block/wait, tags/deletions; `TestPrePushRunAndWaitCreatesMissingAttempt` checks actual binary and final SHA gate | Passed |
| Official stdio MCP and three agent adapters | `mcp.go`, `agents.go`, embedded agent guide | All 13 tools discovered through official SDK; run/wait expiry/logs/failures/compare/ack/rerun/cancel workflow; actual Codex/Claude/OpenCode discover/connect in isolated settings; all adapters repeat/preserve conflict and comments | Passed |
| Skill workflow and client preservation | embedded guide, owned settings edits/backups | Receipt retention, independent work, context recovery, inert logs, explicit ack and final commit check included; client install/uninstall and conflict fixtures preserve unrelated content | Passed |
| Web navigation, views and controls | `apps/async-commit-hook/src`, generated query client | Typecheck, component tests, production route/CSP test; actual Chrome pairing/repository/branch/check detail, inert failure/report rendering, ack, cancellation, dialog Escape and focus restoration | Implemented; Chrome passed; Edge pending |
| Local API, pairing and revocation | `api.go`, protocol/client | Expiry/replay/revoke, exact Origin/Host and unauthenticated denial tests; IDs constrain artifact access; cursor filters apply before pagination; bounded reads and version errors | Passed |
| Secret references, redaction and owner-only files | `config.go`, `reports.go`, platform files | Every stream split boundary redaction test; declared-only environment and report redaction; traversal and symlink escape rejected; Unix modes and Windows ACL adapter | Passed locally; Windows adapter cross-built |
| Archives, installers and Homebrew | release builder, public installers, generated formula | Six target builds, archive-entry inspection, SHA256SUMS verification; installer bad signature/bad checksum/success fixtures; four-target Homebrew formula; package-owned self-update refusal | Passed locally/build; native six-target installer campaign excluded |
| Explicit authenticated update, backup and recovery | `update.go`, platform helper | Real Sigstore fixture with wrong identity rejected, forged bundle/tampered archive rejected; archive traversal/duplicates rejected; state backup reopens; atomic replacement and interrupted-journal recovery; active and Homebrew refusal | Passed locally; Windows helper cross-built |
| Manual release, static deployment and CI separation | release workflow, CI job, packaging contract | Actionlint and workflow tests pass; local dry run emits six archives, installers, formula, checksum and unsigned/unpublished compatibility record; no publication command was run | Passed within owner amendment |
| Internal/public docs and repository integration | project/domain contracts, AGENTS, public `/docs`, root dev entry | Docs-first commit; protocol freshness/lint/breaking; app build verifies docs/installers/security headers; root DevHud dev remains unchanged; fixed ach ports 46308/46309 | Passed |
| Actual six-target machine validation | Original acceptance criterion amended by owner | Explicitly not performed; local macOS results and six cross-builds reported separately | Excluded by owner |
| Public release and site deployment | Original delivery criterion amended by owner | No release tag, GitHub Release, tap push, signing credential creation or Pages deployment executed | Excluded by owner |

## Reproducible validation commands

The administrator embedded bundle was generated before the full Go suite. The following checks passed:

```sh
pnpm --filter devhud-admin build:embedded
go test ./...
go test -race ./cmds/async-commit-hook/...
go vet ./cmds/async-commit-hook/...
# From apps/async-commit-hook:
pnpm test
# From the repository root:
pnpm --filter @delinoio/async-commit-hook-api-client test
pnpm proto:check
pnpm ci:contracts
pnpm ci:workflows
node --test scripts/release/async-commit-hook.test.mjs
python3 scripts/release/build-async-commit-hook.py --output <empty-temporary-directory>
```

The local release dry run used `/tmp/ach-release-897-0.1.0`, outside the repository. It produced four `.tar.gz` and two `.zip` archives plus checksums, installers, formula and compatibility metadata. It does not contain newly published Sigstore signatures: signing is the explicitly unexecuted protected release path, while verification is covered with existing signed fixtures and tampering tests.

## Observed local compatibility

| Component | Observed version / verification |
| --- | --- |
| Host | macOS 26.6.2, arm64; does not qualify macOS 13 or the other five targets |
| Shell | `/bin/sh`, GNU bash 3.2.57(1)-release |
| Go | 1.25.8 |
| Node / pnpm | 24.11.0 / 10.26.2 |
| Chrome | 153.0.8010.48; real pairing, controls, inert content and accessibility tree/focus checks |
| Edge | Unavailable; user clarification pending, not an approved exclusion |
| Codex CLI | 0.145.0; owned isolated skill and MCP entry discovered as enabled |
| Claude Code | 2.1.126; isolated installation and MCP connection successful |
| OpenCode | 1.1.53; isolated installation and MCP connection successful |

Agent discovery used actual client executables with temporary HOME/configuration directories; no personal client configuration or credentials were edited. Full tool behavior was exercised through the official Go MCP client, without invoking paid agent-model sessions. Browser verification used a temporary registered repository and local state. No remote results, telemetry or diagnostic uploads were added.

## PR #901 review and CI repair evidence

The second repair pass merged `main` through `da21872e` and preserved both the Runmoor documentation and async-commit-hook CI plans. Product version remains `0.1.0`. Each review problem and independent CI root cause has its own commit.

| Review requirement | Repair and regression evidence |
| --- | --- |
| Renew terminal nonpassing pre-push attempts | `TestPrePushRunAndWaitRepairsTerminalEvidence` covers failed, cancelled, interrupted, expired and missing evidence; queued work is reused. The public pre-push guide matches the shared gate. |
| Idempotent pruning | `TestPruneCannotResurrectSuccess` repeats dry/live pruning without changing the expired tombstone and retries leftover artifact cleanup without restoring older success. |
| Arbitrary log/report bytes over protobuf | `TestEvidenceTextNormalizesInvalidUTF8WithoutChangingByteOffsets` marshals actual API responses containing invalid and split UTF-8 while preserving stored bytes and byte cursors. |
| Reports belong to the current check execution | `TestReportsMustBeProducedAfterTheirOwningCheckStarts`, `TestReportPathsCannotHaveCompetingOwners` and `TestReportPreparationCannotDeleteOutsideWorkspace` reject stale/competing evidence and protect paths outside the workspace. |
| Acknowledge completed results only | `TestAcknowledgementIdempotencyAndAutomaticDedup` rejects active-state acknowledgement and retains a later failure in the inbox; web tests verify disabled active/already-acknowledged controls. |
| Detached branch filtering | `TestDetachedRunFilterAndCursorScope` distinguishes detached, named and unfiltered queries through the API, including pagination and cursor scope. Web request tests cover detached checks and an unfiltered inbox. |
| Cancellation without an active worker | `TestUnstartedCancellationCompletesWithoutWorker` atomically finalizes unowned queued runs, including empty graphs; `TestCancellationDoesNotFinalizeRunOwnedByWorker` preserves worker ownership. |
| Project agent installation from a subdirectory | `TestProjectAgentsResolveNestedAndLinkedWorktreeRoots` covers all three adapters, nested paths, linked worktrees and repeated install/uninstall at the registered root. |
| Retry downstream release publication | CI workflow tests require separate release, Homebrew and Pages jobs. A real temporary Git remote verifies repeated Homebrew publication preserves the existing commit. Failed jobs can be retried within the same run's artifact-retention window without recreating its successful immutable GitHub release. |

CI repair evidence:

- Windows environment/redaction fixtures now use native PowerShell syntax and explicitly assert undeclared environment exclusion. The regression passes locally and its Windows test binary cross-compiles; Windows execution is left to hosted CI.
- Release fixtures use Node built-ins and run without workspace dependencies. YAML workflow checks moved to the dependency-installed CI contract suite. An isolated dependency-free fixture run passed, and all 154 release tests passed in a local Linux container with read-only source/Git mounts and networking disabled.
- The shared API/sweeper Docker builder now uses Go `1.25.8`, matching `go.mod`; a CI contract prevents future drift. Both prior OCI failures had rejected Go `1.25.7` before compilation. The local multi-architecture OCI build was stopped during slow base-image downloads, so a completed local OCI build is not claimed.

After the repairs, `go test -p 1 ./...`, async-commit-hook unit/integration race tests, the full Runmoor race suite, `go vet ./...`, frontend `pnpm test` (13 component tests plus typecheck/production/static checks), client tests, protocol lint/breaking/freshness, 30 CI contracts, workflow lint and six `0.1.0` archive cross-builds passed. The release dry run wrote only local artifacts under `/tmp/ach-pr901-review-repairs-release` and performed no publication.

Initial root Go runs exposed existing Runmoor shell-startup timing failures. Its guest-validation correctness fixture now allows 15 seconds while the dedicated transport timeout remains 50 ms; focused ordinary/race tests and the final serial root suite passed. The independent runner-startup fixture also failed intermittently before that successful root run; its production startup semantics were not changed.

The two owner-approved completion amendments and the pending Edge validation above remain unchanged. These local checks do not claim that the subsequent hosted CI run has completed.

## Third PR #901 repair pass

The incoming head `3d71383e` had no merge conflicts or failing hosted checks; the prior Windows, release-fixture and OCI failures passed in CI run `35425813089`. Twelve new bot findings were inspected. Eleven were repaired in separate commits; Unix process ownership was still unresolved at the end of that pass. The fourth pass below supersedes that status.

| Repaired finding | Regression evidence |
| --- | --- |
| UTF-8 evidence page boundaries | `TestEvidencePagesPreserveRunesAndIncompleteLiveTail` and the API serialization fixture cover small/default limits, Korean/emoji, invalid bytes and later completion of a live partial rune. |
| Git diff transport bytes | `TestChangesNormalizesRawGitBytesAfterTruncation` covers invalid content/path bytes and a valid rune split by the 2 MiB byte cap; Connect JSON/protobuf fields remain valid. |
| Automatic context deduplication | `TestAutomaticDeduplicationIncludesExecutionContext` verifies public input changes create a fresh compatible attempt, secret value changes do not, and explicit submissions remain distinct. |
| Current worktree branch | `TestRepositoryListingObservesCurrentWorktreeBranch` switches/deletes branches, detaches HEAD and preserves historical branch labels. |
| Raw source streaming | `TestWorkspaceStreamsManyAndLargeRawBlobsInOneBatch` instruments real Git, verifies exactly one batch for 256 files and a large binary blob, and checks exact content, symlinks and executable permissions. Existing filter/LFS/submodule tests still pass. |
| Unstarted preparation recovery | `TestRecoveryResumesOnlyEntirelyUnclaimedRuns` resumes preparing/running requests with untouched checks, discards partial workspaces and interrupts claimed checks without replay. |
| Structured failure bounds | `TestLargeJUnitSummariesStayReadableWithinRunBudget` collects a report larger than 8 MiB, caps summaries across two checks, serializes both detail/failure responses under the transport cap and retrieves full report-tail evidence. |
| Reused checkout paths | `TestExplicitRegistrationAfterCheckoutPathReusePreservesHistory` covers identical/different common-directory paths, explicit trust, stale API IDs and preserved history. `TestLegacyPathRegistryUpgradePreservesIDsAndRequiresExplicitTrust` verifies migration and foreign-key enforcement. |
| Persistence failure propagation | `TestCheckPersistenceFailureReleasesWorkerForReconciliation` injects SQLite failures for running/collecting/passed writes, checks worker lock release and confirms restart interruption. Permanent-storage-failure tests still pass. |
| Windows helper cleanup | `TestUpdateHelperCleanupSurvivesSuccessfulJournalRemoval` preserves live/changed helpers, retains retry metadata and removes authenticated exited helpers on a later open. Windows executable targets cross-compiled; this pass did not execute Windows locally. |
| Terminal detail polling | The frontend timer test observes running-to-passed polling stop and later explicit invalidation. A manual refresh action remains available. |

Passed: `go test ./cmds/async-commit-hook/...`, `go test -race ./cmds/async-commit-hook/...`, `go test -p 1 ./...`, `go vet ./...`, package-local frontend `pnpm test` (14 component tests plus typecheck/build/static checks), root `pnpm test` (all 12 selected Turbo tasks), protocol lint/breaking/freshness, 30 CI contracts, workflow lint and six unsigned `0.1.0` release archive builds under `/tmp/ach-pr901-repair3-release`. Generated repository-owned `dist` output is removed before delivery. No signing, release, tap update or deployment was performed.

The separate temporary Unix daemonization reproduction intentionally failed with `daemonized descendant survived successful ownership reconciliation`. It ran through a Go overlay without adding a passing or skipped test that would imply this defect is fixed, and explicitly killed the reproduced child on cleanup. Passing committed tests above therefore do not establish the missing Unix lifetime guarantee. The next required work is a supported process-ownership backend and proof of descendant termination across reparenting, cancellation, replacement and recovery, including macOS 13; the supported OS range and original requirement were not relaxed. Edge verification remains pending as before.


## Fourth PR #901 repair pass

The only incoming unresolved bot thread was `PRRT_kwDORRAKg86j9n3x`. The replacement runs each Unix check inside a same-binary supervisor whose ownership is established before the command can run. Linux uses subreaper adoption and an `ECHILD` emptiness proof. macOS uses a temporary background launchd job, a dedicated resource coalition, and kernel active-task accounting. The initial coalition experiment explicitly showed that `bootout` alone leaves a double-forked child alive; production cleanup therefore also drains and verifies the coalition.

Regression evidence:

- `TestScopeReapsDaemonizedDescendants`: same-binary fixtures create multiple process generations, new sessions, cleared environments and a changed working directory. No ownership sample is taken. Normal root exit, cancellation, worker socket loss and explicit recovery all remove the detached leaf.
- `TestReplaceReapsDaemonizedDescendantsBeforeNextStarts`: the next exclusive attempt cannot start while the prior detached leaf remains alive.
- `TestScopeCancellationDoesNotTouchAnotherCheck`: cancelling one scope preserves the other scope's detached leaf.
- `TestScopeStartBarrierAndOutput`: the start barrier, output transport and original nonzero command exit status remain observable.
- `TestScopeJournalNeverStoresResolvedEnvironment`: neither launchd plist nor ownership journal contains a resolved credential fixture.
- `TestScopeMissingJournalCannotConfirmCompletion` and `TestLegacyScopeCannotClaimUnknownDescendantsExited`: unavailable ownership proof cannot release a scheduling claim.
- `TestScopeRecoveryAfterSupervisorDeath` (macOS): recovery kills the supervisor's surviving descendants and verifies zero active coalition tasks.
- `TestScopeLostSubreaperFailsClosedUntilBootChanges` (Linux): a dead/reused supervisor identity without completion proof blocks recovery; a different kernel boot ID proves the old processes cannot remain.
- Existing FIFO, storage-failure cleanup and check-persistence-failure tests pass. Saving the preparing scope never returns a claimed check to queued state or replays it after a failed running-state write.

Executed successfully: `go test -p 1 ./...`, `go vet ./...`, the async-commit-hook Go suite and full race suite, focused lifecycle race tests, app-directory `pnpm test` (14 component tests, typecheck and production/static checks), and focused native Linux arm64 tests in a network-disabled `node:24-bookworm` container. The macOS supervisor-death regression passed on the local macOS 26.6.2 arm64 host. Six unsigned `0.1.0` target archives also built. These are local/container/build evidence, not macOS 13 or six-machine qualification. No elevated daemon, Endpoint Security entitlement, signing or publication was introduced. Edge validation remains pending as before.

A Linux supervisor that is itself forcibly killed before recording completion loses subreaper ownership. This case deliberately retains the group claim and requires a host reboot before recovery can independently prove the old tasks are gone; it never reports successful cancellation from an empty PID sample. Legacy unfinished sampled records similarly require explicit reconciliation. Normal worker death retains the independent supervisor, which drains descendants and allows interrupted recovery.


### Fourth-pass Windows CI repair

While local ownership repairs were running, incoming-head CI run `35427897838` finished with Windows Go failures in the collecting/passed variants of `TestCheckPersistenceFailureReleasesWorkerForReconciliation`. Native PowerShell startup exceeded that fixture's 15-second context budget; surrounding successful checks took up to about 25 seconds. The collecting case returned the intended injected SQLite error after its watchdog expired, while the passed case was cancelled before reaching the target write. No product timeout or storage logic caused this failure.

The separate CI repair gives this correctness fixture a two-minute deadlock watchdog and requires the returned error to contain the injected SQLite failure, preserving proof that the intended branch executed. It does not add a command runtime timeout or relax the storage, lock-release, process-cleanup and interruption assertions. Focused local ordinary/race execution and Windows test-binary cross-compilation validate the edit; native Windows execution remains hosted CI evidence, not a claimed local result.


### Independent supervisor lifecycle integration

Final integration inspection found that an independently surviving supervisor also needs an account lifecycle lease. The follow-up repair registers it before the start barrier under the existing account lock. Worker disappearance cannot make active commands invisible to mode/port/state changes or self-update. If the supervisor itself dies, backend or changed-boot proof remains required; a dead owner PID alone does not remove its lease. Retention clears proven inactive leases before deleting their ownership journals.

`TestSupervisorLeaseBlocksConfigurationWithoutWorker` covers rejection of configuration changes and updates with only a supervisor registered, later lease reclamation, and safe journal pruning. The macOS supervisor-death test now asserts that living coalition descendants retain that lease; the Linux lost-subreaper test asserts the same conservative behavior without a completion record. Focused race tests passed for these cases, detached replacement, storage failures and retention.


After lifecycle integration, the full async-commit-hook race suite, `go test -p 1 ./...`, `go vet ./...`, and focused Linux arm64 container lifecycle/retention tests passed again. The final six-target unsigned archive build uses `/tmp/ach-pr901-repair4-complete-release`; product version remains `0.1.0`. Public release, signing, Homebrew publication and deployment remain unexecuted. Generated `dist` directories are removed before delivery.

Four additional incoming-head reviews were first visible in the final inventory. They are handled without another status poll. `TestEnvironmentSecrecyConflictsRejectedBeforeAcceptance` rejects cross-check secret/public conflicts, including case variants, preserves consistent shared declarations and proves no run is accepted into SQLite.

`TestCommitSubjectsNormalizeDisplayWithoutChangingObjectIDs` writes a real Git commit with invalid UTF-8, verifies raw Git still returns those bytes, and marshals the actual ListCommits protobuf/JSON response while preserving both object IDs and valid Korean/emoji text.

`TestGoReportLargeRepeatedOutputUsesBoundedTailAllocations` parses 50,000 output events for one test, verifies its exact chronological tail, and rejects allocation growth above a generous 256 MiB budget (the old implementation copies multiple GiB). `TestReportOutputTailWrapAndOversizedEvents` covers oversized chunks, wraparound, Unicode bytes and the 64 KiB allocation bound.

`TestRetentionAgeOverflowRejectedWithoutDeletingEvidence` rejects negative, first-overflow, million-day and maximum-int inputs through both personal configuration and dry/live pruning. It verifies unchanged run records and retained bytes, and accepts zero/unlimited and the maximum safe day boundary.


### Final fourth-pass verification

All five inventoried review findings are repaired: Unix ownership (`PRRT_kwDORRAKg86j9n3x`), environment secrecy conflicts (`PRRT_kwDORRAKg86j9zOj`), commit-subject transport (`PRRT_kwDORRAKg86j9zOq`), Go report tail allocation (`PRRT_kwDORRAKg86j9zOs`), and retention overflow (`PRRT_kwDORRAKg86j9zOx`). The independent Windows fixture watchdog repair is also included. After the four late-arriving findings, the complete async-commit-hook race suite, root serial Go suite, root Go vet, app-directory frontend tests, and all six unsigned archives passed again. Final archives are under `/tmp/ach-pr901-repair4-all-findings-release`. The prior artifact directories record intermediate verification only. New hosted CI results are not claimed; the one-shot repair workflow does not poll after its final inventory. Version remains `0.1.0`, the PR stays non-draft, and no publication occurred.

## Fifth PR #901 repair pass

Incoming head `a6e5027b` has no merge conflict or failed CI checks; hosted run `35429903259` passed, including native Windows Go execution of the fourth-pass watchdog repair. This pass repairs six new review findings in separate commits:

| Review thread | Repair and regression evidence |
| --- | --- |
| `PRRT_kwDORRAKg86j-AMI` | The application recovery test starts with revoked stored authorization and a fresh fragment, clicks Pair again, retains/focuses the code, and successfully pairs with that exact code. Stale authorization and query data are cleared. |
| `PRRT_kwDORRAKg86j-AMK` | `TestBranchLabelsNormalizeDisplayWithoutChangingObjectIDs` retains a real invalid-byte packed ref, normalizes only the API display label, and serializes protobuf and JSON without changing commit IDs. The Unix fixture uses packed refs because APFS rejects equivalent loose-ref filenames; it does not claim Windows arbitrary-path-byte behavior. |
| `PRRT_kwDORRAKg86j-AML` | `TestCommittedConfigReadIsBoundedBeforeParsing` exercises the exact 1 MiB boundary, one-byte overflow and a 64 MiB committed blob. Oversized reads allocate less than 16 MiB, return `invalid-config`, and create no run. Missing objects retain `config-unavailable`. |
| `PRRT_kwDORRAKg86j-AMN` | `TestSourceLFSDeclarationsIgnoreCommentsAndPatternText` accepts comments, indentation/BOM, quoted patterns and other filter values, while rejecting actual LFS declarations. Existing raw-pointer/submodule rejection and checkout-filter isolation tests pass. |
| `PRRT_kwDORRAKg86j-AMR` | Isolated shell-installer download fixtures verify explicit option precedence over environment/default versions and reject missing, malformed, unknown and positional arguments before downloads. Existing signature/checksum refusal fixtures still pass. |
| `PRRT_kwDORRAKg86j-AMS` | Development script fixtures start a package-manager stand-in with a server grandchild that does not forward signals. SIGINT/SIGTERM return only after port 46308 can be rebound; occupied ports and address overrides fail. The shared helpers are present in actual Turbo hash inputs and CI selection tests. Native execution here is macOS; Windows taskkill execution remains hosted validation. |

The final root Go suite (`go test -p 1 ./...`), async-commit-hook race suite, root `go vet ./...`, frontend-directory `pnpm test` (15 component tests, typecheck, production build and four script tests), five release fixtures, 30 CI contracts, 11 CI selection tests and workflow syntax validation passed. Administrator embedded assets were generated before Go compilation. Product version remains `0.1.0`; the PR remains non-draft. New hosted CI results are not inferred from these local checks. The two owner-approved exclusions and previously pending Edge validation remain unchanged; no signing, public release, tap mutation or deployment occurred.

Six unsigned target archives were built and their checksum manifest verified under `/tmp/ach-repair5-archives-verified`. The copied shell installer matches the repaired source exactly, and compatibility metadata records version `0.1.0`, unsigned and unpublished status. Generated repository-owned `dist` output is removed before delivery.

## Seventh PR #901 repair invocation

The intervening sixth invocation found no conflicts, unresolved bot threads or CI failures and made no changes. This invocation started from `ed2f2384`, with ten newly posted findings and passing incoming-head CI (`35432465822`). Each finding is repaired in its own commit:

| Review thread | Repair and regression evidence |
| --- | --- |
| `PRRT_kwDORRAKg86j-TPf` | `TestAttributeBlobsAreBoundedBeforeAcceptance` checks the exact 1 MiB limit, overflow and a 64 MiB committed attribute file. Overflow stays below a 16 MiB allocation budget, returns a typed usage error and produces no run. |
| `PRRT_kwDORRAKg86j-TPh` | `TestHookOwnershipFailureRollsBackNewHookAndAllowsRetry` injects SQLite ownership failures for both hooks and verifies removal, repeated reinstall and uninstall. `TestHookRollbackPreservesConcurrentEdits` preserves changed contents. |
| `PRRT_kwDORRAKg86j-TPk` | `TestDelayedUpdateHelperCannotUndoRecovery` holds an already-read prepared journal behind a synchronization barrier, completes recovery, then releases the helper. Missing and replaced journals both prevent executable changes or journal resurrection. The production helper rereads under the lifecycle lock through replacement. |
| `PRRT_kwDORRAKg86j-TPm` | `TestConfigValidateUsesSelectedWorktreeRoot` invokes the real CLI from nested primary/linked worktree paths, ignores misleading nested configuration and detects an uncommitted invalid linked-root file without changing primary validation. |
| `PRRT_kwDORRAKg86j-TPo` | `TestWorkerRetryBackoffHasBoundedExplicitDeadlines` verifies each retry boundary through the one-minute cap with a logical clock. `TestWorkerSchedulesBackoffWithoutReleasingPendingWork` uses an injected SQLite failure and a log barrier to verify the initial structured delay, shutdown and retained pending state. |
| `PRRT_kwDORRAKg86j-TPp` | Repository display tests marshal protobuf/JSON for invalid-byte names, paths and branches without modifying raw database strings. The Linux fixture registers and accesses an actual invalid-byte directory through its worktree ID. |
| `PRRT_kwDORRAKg86j-TPq` | Explicit state-precedence tests cover both orders of every outcome pair, optional failures/lifecycle outcomes and empty applicability. `TestFailedPrerequisiteWinsRegardlessOfCheckNames` executes both name orderings of the same failed/blocked dependency graph. |
| `PRRT_kwDORRAKg86j-TPr` | `TestJUnitDuplicateFailuresHaveStableDistinctIdentities` covers nested/repeated suites, repeated testcase names, multiple failure/error elements and diagnostic-only edits. Stored comparisons retain all continuing failures and classify exactly one added/removed duplicate. |
| `PRRT_kwDORRAKg86j-TPs` | `TestReportSyntaxOverlappingSecretsDoNotChangeValidation` runs passing/failing JUnit and Go JSON reports with `a` or a quotation mark as a secret. Syntax remains valid for adjudication while persisted evidence and extracted text are redacted. |
| `PRRT_kwDORRAKg86j-TPu` | `TestConcurrentPushSelectionReusesOneLatestAttempt` synchronizes independently opened SQLite clients before selection. Empty history, pending reuse, failed renewal, passing reuse and missing-evidence renewal produce one shared attempt per round. Existing full pre-push policy/wait tests also pass. |

Passed after all repairs: `go test -p 1 ./...`, `go test -race ./cmds/async-commit-hook/...`, `go vet ./...`, and frontend-directory `pnpm test` (15 component tests plus typecheck, production build and four script tests). The administrator embed was generated before Go compilation. A network-disabled local Linux arm64 container passed all new core regressions, including native invalid-byte path access. The complete Windows amd64 core test binary cross-compiled; this is not a claim of local Windows execution.

All six unsigned `0.1.0` archives and their checksum manifest were verified under `/tmp/ach-repair7-release`. No signing, public release, tap mutation or deployment was performed. The PR remains non-draft. New hosted CI results are not claimed before the final push. The original two owner-approved exclusions and pending Edge validation remain unchanged, and generated repository-owned `dist` directories are removed before delivery.

## Eighth PR #901 repair invocation

Incoming head `59613713` had six unresolved bot findings, no merge conflict and no failing checks. Hosted CI run `35434105587` passed for that incoming head. This one-shot invocation repairs each finding in a separate commit:

| Review thread | Repair and regression evidence |
| --- | --- |
| `PRRT_kwDORRAKg86j-efW` | `TestExpandedReportStreamsToCompleteEvidence` stores an 8 MiB one-byte-secret input as 80 MiB of complete redacted evidence while allocating less than 4 MiB during storage; it verifies the digest, tail and temporary-file cleanup. `TestExpandedFailureTextRetainsBoundedRedactedPrefix` covers extracted-field expansion and a secret crossing the retained prefix. Existing all-boundary masking and syntax-overlapping-secret tests pass. |
| `PRRT_kwDORRAKg86j-efY` | `TestSynthesizedDiagnosticsShareFailureSummaryBounds` combines a 100 KiB command, 400 missing-report diagnostics and legacy Unicode failure text. Synthesized and stored summaries stay within 4 KiB per field and 1 MiB per response, disclose omission, and serialize below the Connect limit. |
| `PRRT_kwDORRAKg86j-efb` | `TestRepeatedDiagnosticIdentitiesCompareIndependently` covers distinct missing reports, identical repeated diagnostics, reordered diagnostics, one resolved/new occurrence and distinct full identities whose displayed prefixes are identical. |
| `PRRT_kwDORRAKg86j-efc` | `TestGoReportAcceptsLargeSingleEvents` accepts 5 MiB output events for passing and failing tests, preserves the bounded failure tail, supports CRLF and a final event without a newline, and still rejects truncated JSON. Existing repeated-output allocation and incomplete-report tests pass. |
| `PRRT_kwDORRAKg86j-efe` | `TestWaitTimeoutRejectsDurationOverflow` covers the default, zero, ordinary values, 9223372036 seconds, the first duration overflow, maximum int64, integer parse overflow and negative values, retaining usage exit 2 for invalid input. |
| `PRRT_kwDORRAKg86j-eff` | `TestAPIRerunPreservesAcceptedReceiptOnStartupFailure` exercises authenticated Connect requests for successful startup and an injected runner-log startup failure, proving HTTP success retains exactly one new queued attempt and its ID/diagnostic. Invalid pre-acceptance requests remain errors and create no run. Web tests cover both rerun buttons, receipt display, duplicate-submission prevention and direct navigation; generated-client tests retain optional diagnostic wire compatibility. |

Passed after all six repairs: `go test -p 1 ./...`, `go test -race ./cmds/async-commit-hook/...`, `go vet ./...`, frontend-directory `pnpm test` (18 component tests, typecheck, production build and four script tests), generated-client tests (two), and protocol lint, breaking and freshness checks. Administrator embedded assets were generated before Go compilation. The Windows amd64 core test binary cross-compiled; native Windows execution is not claimed for this pass.

Six unsigned `0.1.0` archives and their complete checksum manifest were verified under `/tmp/ach-repair8-release`; compatibility metadata remains explicitly unsigned and unpublished. The final pre-push inventory contains only the six handled findings and no failing checks or merge conflict. New hosted CI results are not claimed: the one-shot workflow stops after pushing and resolving these threads. The PR remains non-draft, public release/signing/tap changes/site deployment were not executed, and generated repository-owned `dist` directories were removed. The two owner-approved exclusions and previously pending Edge validation remain unchanged.

## Ninth PR #901 repair invocation

Incoming head `208518bf` had three unresolved bot findings, no merge conflict and no failing checks (34 passing, three skipped; main CI run `35435945734`). Each finding is repaired in its own commit:

| Review thread | Repair and regression evidence |
| --- | --- |
| `PRRT_kwDORRAKg86j-raa` | `TestTerminalCheckPublicationHonorsCommittedCancellation` uses a worker ownership lock and a second SQLite connection to commit cancellation after a stale worker read but before completion. Both cancellation reasons override passed, failed, blocked and interrupted proposals, preserve timestamps, log the committed state and prevent a passing gate even with valid evidence. `TestCancellationAfterTerminalPublicationDoesNotRewriteEvidence` covers the reverse ordering. Focused ordinary/race tests and the existing injected-persistence-failure regression pass. |
| `PRRT_kwDORRAKg86j-rai` | `TestRepeatedGoTestFailuresHaveStableDistinctIdentities` parses repeated test failures plus a package-level failure, verifies unique stable IDs despite diagnostic edits, and preserves later IDs when the first iteration passes or skips. Stored comparisons classify exactly one resolved/new occurrence while retaining two continuing failures. Existing large-event, bounded-tail, report syntax and incomplete-report checks pass. |
| `PRRT_kwDORRAKg86j-ram` | `TestCodexEquivalentMCPKeysRejectConflictsBeforeMutation` covers bare, quoted, literal, whitespace, escaped, dotted, parent, inline and array declarations, plus incompatible parents and invalid TOML. Conflicts leave exact settings bytes, skills, ownership and backups untouched. `TestCodexTOMLSemanticInspectionPreservesUnrelatedText` accepts misleading comments/string values, preserves unrelated MCP settings and formatting, and verifies repeated install plus removal produce valid TOML. Existing all-client installation and linked-worktree tests pass. |

Passed after all three repairs: `go test -p 1 ./...`, `go test -race ./cmds/async-commit-hook/...` and `go vet ./...`. Administrator embedded assets were generated before Go compilation. The complete Windows amd64 core test binary also cross-compiled; this is not native Windows execution evidence. No frontend or protocol sources changed in this invocation.

All six unsigned `0.1.0` archives and the full checksum manifest were verified under `/tmp/ach-repair9-release`. The final pre-push inventory contains only the three handled findings, no failing checks and no merge conflict. The PR remains non-draft. New hosted CI results are not claimed after the one-shot push. No signing, public release, tap change or deployment occurred; generated repository-owned `dist` output was removed. The original two owner-approved exclusions and pending Edge validation remain unchanged.

## Tenth PR #901 repair invocation

Incoming head `be3b765a` had seven unresolved bot findings, no merge conflict and no failing checks (34 passing, three skipped; main CI run `35437557384`). Each finding is repaired in a separate commit:

| Review thread | Repair and regression evidence |
| --- | --- |
| `PRRT_kwDORRAKg86j-3QA` | `TestPushEvidenceVerificationReleasesWriterAndRevalidatesSelection` pauses evidence verification through a synchronization barrier while a separate SQLite connection successfully persists unrelated worker state. Unchanged evidence is reused; a newer accepted attempt or concurrent pruning forces fresh selection. Existing concurrent pre-push acceptance and all policy tests pass. |
| `PRRT_kwDORRAKg86j-3QE` | `TestPreparationFailureCannotFinalizeAfterCheckSaveFailure` makes the source unavailable and injects a SQLite check-write failure. The worker returns the persistence error without publishing a terminal run; removing the injection permits recovery and persists the failed check diagnostic. |
| `PRRT_kwDORRAKg86j-3QI` | `TestTreeListingStreamsWithoutRetainingAllPaths` consumes 100,000 synthetic records (about 100 MiB) incrementally with an 8 MiB retained-heap allowance. `TestTreeListingBoundsRecordsAndStopsOnVisitorFailure` covers the exact 1 MiB record limit, overflow, incomplete records, unsafe paths and early visitor failure, including real Git reader cleanup. Existing raw blob streaming, source-filter isolation and unsupported-source checks pass. |
| `PRRT_kwDORRAKg86j-3QL` | `TestDependencyApplicabilityCoversEveryDependentOS` checks all 64 prerequisite/dependent OS mask combinations, including empty-as-all declarations and optional prerequisites. `TestInapplicablePrerequisiteRejectsAcceptanceOnEveryHost` rejects an invalid cross-platform graph before any durable receipt exists. |
| `PRRT_kwDORRAKg86j-3QR` | `TestLFSPointerRequiresCompleteBoundedSyntax` covers supported version aliases, OID/size, extensions, CRLF, missing final newline, malformed fields and the upstream 1024-byte cutoff. `TestLFSHeaderExamplesRemainByteExactSource` prepares ordinary committed documentation containing only the header, incomplete examples or a large body without changing bytes. Valid raw pointers remain unsupported before commands execute. |
| `PRRT_kwDORRAKg86j-3QV` | `TestHookReinstallationRefreshesOwnedConfiguration` updates both hooks from a previous executable/configuration to the current paths, preserves an exact backup and verifies ownership, idempotence and uninstall. `TestHookRefreshRecoversOwnershipFailures` injects intent/completion persistence failures and interrupted pre-publication state; repeated installation recovers. User edits and replaced files are preserved by `TestHookRefreshPreservesUserEditsAndReplacements`. |
| `PRRT_kwDORRAKg86j-3Qb` | `TestFingerprintIncludesOnlyExecutionContext` excludes every push policy/diff-base combination, retains default v1 digest compatibility and detects changes in execution fields/public values. `TestComparisonSurvivesPushPolicyAndDiffBaseChanges` executes successive real commits and retains both default/explicit comparisons and continuing failure identity while preserving full configuration snapshots. Existing secret-value exclusion and automatic-context deduplication tests pass. |

Passed after the repairs: `go test -p 1 ./...`, `go test -race ./cmds/async-commit-hook/...`, `go vet ./...`, and frontend-directory `pnpm test` (18 component tests, typecheck, production build and four script tests). Administrator embedded assets were generated before Go compilation. Windows test compilation identified a new fixture's Unix-only helper dependency; that fixture now invokes portable Git directly, its focused tests pass, and the complete Windows amd64 core test binary cross-compiles. No native Windows runtime validation is claimed.

All six unsigned `0.1.0` archives and the complete checksum manifest were verified under `/tmp/ach-repair10-release`. The final pre-push inventory contains only the seven handled findings, no failing checks and no merge conflict; the PR remains non-draft. New hosted CI results are not claimed after the one-shot push. No signing, public release, tap change or deployment occurred. Generated repository-owned `dist` output was removed, and the two owner-approved exclusions plus pending Edge validation remain unchanged.

## Eleventh PR #901 repair invocation

Incoming head `329c6672` had seven unresolved bot findings, no merge conflict and no failing checks (34 passing, three skipped; main CI run `35444669568`). Each finding is repaired in its own commit:

| Review thread | Repair and regression evidence |
| --- | --- |
| `PRRT_kwDORRAKg86j_ytO` | `TestRunLoadsRawSourceAndBranchOutsideJSONSnapshot` round-trips invalid UTF-8 source/branch bytes through SQLite, store reopening and repeated state writes while retaining safe JSON display. `TestRunAndRerunFromInvalidUTF8WorktreePath` executes and reruns an actual invalid-byte Linux path, verifies both gates and retains history after the source moves. |
| `PRRT_kwDORRAKg86j_ytQ` | `TestJUnitLineNumbersRemainWithinWireRange` checks positive int32 boundaries, negatives, zero, oversized integers, malformed and missing locations through parsing, persistence and API conversion. Failures and filenames remain; invalid locations become unknown. Injected legacy records also lose invalid locations before display rather than wrapping. |
| `PRRT_kwDORRAKg86j_ytT` | Local Git tag fixtures cover absent, matching and mismatched lightweight/annotated/nested version tags and an unavailable remote. Verification never mutates a tag. Workflow tests require the remote commit guard before signing and again before release creation, rather than relying on `gh release create --target` to validate an existing tag. |
| `PRRT_kwDORRAKg86j_ytV` | `TestPrePushRecordsPushedBranchContextForEveryNewTip` pushes two non-checkout tips, including a renamed remote destination and an explicit object refspec, while the checkout stays on main. Branch-filtered history retains both new runs, and the release comparison selects its earlier branch execution instead of a newer unrelated main execution. |
| `PRRT_kwDORRAKg86j_ytW` | `TestOrdinaryHookUninstallRemovesEveryOwnedHook` installs both hooks, removes them without the opt-in flag, clears ownership even for an already-missing file and repeats safely. `TestOrdinaryHookUninstallPreservesUnrelatedAndModifiedHooks` retains unrelated hooks, diagnoses modified owned hooks and verifies pre-push installation still requires opt-in. CLI help and public cleanup guidance match. |
| `PRRT_kwDORRAKg86j_ytX` | Archive fixtures inject two different wall-clock times and require identical Unix/Windows bytes and SHA-256 values for identical payloads, different bytes for changed payloads, fixed gzip/tar metadata and byte-exact extraction. The six-target build also verifies zero gzip timestamps in every Unix archive. |
| `PRRT_kwDORRAKg86j_ytY` | `TestUnstartedChecksPublishVerifiableEmptyLogs` covers startup failure and pre-launch cancellation, verifies the empty-file digest/size and preserves continuing startup diagnostics in comparison. Unix tests obstruct ownership setup; the Windows fixture uses a missing executable and is cross-compiled here. `TestLogFinalizationPreservesWriteFailureDiagnostic` confirms failed sync cannot become valid evidence. |

Passed after the repairs: `go test -p 1 ./...`, `go test -race ./cmds/async-commit-hook/...`, `go vet ./...`, frontend-directory `pnpm test` (18 component tests, typecheck, production build and four script tests), seven dependency-free release fixtures, 31 CI contract tests and workflow syntax validation. Administrator embedded assets were generated before Go compilation. A network-disabled Linux arm64 container passed the eight new core regression tests, including real invalid-byte path execution and rerun. The complete Windows amd64 core test binary cross-compiled; native Windows execution is not claimed.

All six unsigned `0.1.0` archives, fixed Unix gzip timestamps and the complete checksum manifest were verified under `/tmp/ach-repair11-release`. The final pre-push inventory contains only the seven handled findings, no failing checks and no merge conflict; the PR remains non-draft. New hosted CI results are not claimed after the one-shot push. No signing, public release, tap change or deployment occurred. Generated repository-owned `dist` output was removed. The two owner-approved exclusions and pending Edge validation remain unchanged.

## Twelfth PR #901 repair invocation

Incoming head `2b9d843b` had four unresolved bot findings, no merge conflict and no failing checks (34 passing, three skipped; main CI run `35449764622`). Three actionable findings are repaired in separate commits; the fourth is disproved without changing correct production code:

| Review thread | Disposition and regression evidence |
| --- | --- |
| `PRRT_kwDORRAKg86kAZnX` | `TestWorkspacePreservesSourceObjectFormat` initializes real SHA-1 and SHA-256 repositories, verifies the managed storage format, exact commit and committed bytes, executes a passing check and preserves uncommitted original files. Managed initialization now explicitly selects the source storage hash algorithm. |
| `PRRT_kwDORRAKg86kAZna` | No production change: `defer in.Close()` is scoped to the `WalkDir` callback and runs after each individual file, as specified by the [Go defer contract](https://go.dev/ref/spec#Defer_statements). A temporary test overlay exercised two complete backups of 512 evidence files each with the process soft `RLIMIT_NOFILE` set to 128, verifying every copied payload. Both backups passed against the unchanged implementation. The inline review receives this explanation before resolution. |
| `PRRT_kwDORRAKg86kAZnd` | `TestEnvironmentDuplicateNamesAreRejectedCaseInsensitively` covers exact and case-variant duplicate names, public and secret declarations, different credential references, and distinct valid names. Invalid committed configurations return `invalid-environment` before credential lookup or durable acceptance. Existing cross-check secrecy tests retain compatible shared names. |
| `PRRT_kwDORRAKg86kAZne` | Seven offline publication fixtures cover initial publication, partial upload recovery, lost draft-creation responses, newly generated signature bytes on retry, immutable completed releases, wrong run/commit/ownership, unexpected assets, lookup/tag errors, and missing/duplicate/mismatched uploads. Only a matching workflow-run-owned draft can resume. Complete remote names, sizes, digests and uploaded states are checked before publication. Every GitHub mutation and tag lookup in these fixtures is intercepted. |

Passed after the repairs: `go test -p 1 ./...`, `go test -race ./cmds/async-commit-hook/...`, `go vet ./...`, eight dependency-free release tests (including the seven Python publication fixtures), 31 CI contract tests and workflow syntax validation. Administrator embedded assets were generated before Go compilation. A network-disabled Linux arm64 container passed the new object-format/environment regressions plus existing environment secrecy checks. The complete Windows amd64 core test binary cross-compiled; native Windows execution is not claimed. No frontend or protocol sources changed in this invocation.

All six unsigned `0.1.0` archives and their complete checksum manifest passed verification. The initial build lost its output directory under `/tmp` while running; rebuilding into a dedicated external temporary directory completed all six targets successfully. This invocation performs no signing, public release, tap mutation or site deployment. The PR remains non-draft, and new hosted CI results are not inferred from local verification. Generated repository-owned `dist` output is removed before delivery. The two owner-approved exclusions and previously pending Edge validation remain unchanged.

## Fourteenth PR #901 repair invocation

The thirteenth invocation found no actionable findings and made no changes. This invocation starts from `ce56348d` with four new bot findings, no merge conflict and no failing checks (34 passing, three skipped; main CI run `35452302470`). Each finding is repaired in its own commit:

| Review thread | Repair and regression evidence |
| --- | --- |
| `PRRT_kwDORRAKg86kAwi4` | `TestInitCannotEscapeWorktreeThroughConfigSymlink` checks external `.config` targets with both missing and existing files, retaining exact external contents and refusing trust registration. `TestOwnedConfigurationCreationIsAtomicAndPreservesExistingFiles` races eight creators through a root-confined handle, keeps one complete configuration, preserves it on retry and verifies staging cleanup. |
| `PRRT_kwDORRAKg86kAwjC` | `TestExpiredWaitContextTakesPrecedenceOverEveryRunState` covers all 12 states, immediate timeout, a cancelled query against a closed store, unchanged acknowledgement/state and live terminal reads. `TestWaitCancellationDuringReadCannotReturnTerminalSuccess` holds the sole database connection and observes the connection-wait barrier before cancellation, proving expiration is checked again after the read. Existing CLI timeout-boundary tests pass. |
| `PRRT_kwDORRAKg86kAwjK` | `TestRunListPagesOmitLargeCheckGraphsAndPreserveDetail` persists 51 attempts with 4,000 checks each from a valid configuration. Authenticated Connect Checks/Inbox pages return 50 summaries in approximately 21 KiB, retain the final cursor page and open complete 4,000-check details with commands/diagnostics. Client tests retain absent, zero and 4,000 optional counts; web tests display those counts, preserve older-response fallback and select the correct execution. |
| `PRRT_kwDORRAKg86kAwjN` | `TestUpdateRecoveryAuthenticatesBackupBeforeRestoring` covers a valid original plus changed, missing, directory, symlink, missing-digest and oversized backups. Invalid inputs cannot publish an executable or discard the journal/candidate. Valid recovery atomically publishes authenticated bytes and retains the verified backup. Existing interrupted-update, active-refusal and delayed-helper recovery tests pass. |

Passed after all four repairs: `go test -p 1 ./...`, `go test -race ./cmds/async-commit-hook/...`, `go vet ./...`, frontend-directory `pnpm test` (20 component tests, typecheck, production build and four script tests), three generated-client wire tests, and protocol formatting/lint/breaking/freshness checks. Administrator embedded assets were generated before Go compilation. A network-disabled Linux arm64 container passed all six new core regression tests, including real symlink rejection, 4,000-check run pagination and backup restoration. The complete Windows amd64 core test binary cross-compiled; this is not native Windows execution evidence.

All six unsigned `0.1.0` archives and their complete checksum manifest passed verification in the dedicated external repair artifact directory. The PR remains non-draft. No signing, public release, tap mutation or site deployment occurred. New hosted CI results are not inferred from local checks. Generated repository-owned `dist` directories were removed before delivery, and the two owner-approved exclusions plus previously pending Edge validation remain unchanged.

## Fifteenth PR #901 repair invocation

Incoming head `82bfc027` had five unresolved bot findings, no merge conflict and two failed checks: native Windows Go tests and their aggregate CI Result (`35455486852`). The Windows log reports the package-wide 600-second watchdog while `TestRerunInheritedProvenanceAndActiveRetention` had been running for 12 seconds; it does not report an assertion failure. Each review repair and the independent test-fixture cost reduction has its own commit:

| Review thread | Repair and regression evidence |
| --- | --- |
| `PRRT_kwDORRAKg86kBTg0` | Lifecycle identity now includes credential reference mappings without resolving values. `TestLifecycleRejectsChangedCredentialReferences` rejects changed environment references, file references and removed entries through both owner entry and daemon reuse. Equivalent maps from another configuration path remain compatible despite map order and secret-value changes; new references work once the original owner exits. |
| `PRRT_kwDORRAKg86kBTg6` | Expanded logs poll only while their check is active, fetch final bytes once when it completes, and retain manual refresh and pagination. Eight terminal-state UI cases verify polling stops even while the containing run remains active, final bytes arrive, and explicit reads still work. |
| `PRRT_kwDORRAKg86kBTg9` | Invalid UTF-8 public environment values return `invalid-environment` before acceptance rather than being changed by JSON serialization. Unix tests supply distinct invalid byte sequences and prove no attempt is stored and values do not leak in diagnostics. Valid Unicode, empty and absent values retain distinct fingerprints and exact execution inputs after SQLite reload. |
| `PRRT_kwDORRAKg86kBThA` | Completion delivery can unblock after worker ownership cancellation, after command reconciliation returns. `TestWorkerStorageFailureJoinsMoreRunsThanCompletionBuffer` accepts 2,050 real SQLite requests, holds their run callbacks at a context barrier, closes the store, and verifies error shutdown joins every callback without a concurrency cap or a blocked completion sender. |
| `PRRT_kwDORRAKg86kBThG` | Credential files require a regular nonsymlink file, matching opened identity, and a 64 KiB stream bound plus one overflow byte. Tests cover missing files, directories, empty/exact-boundary values, oversized files, symlinks, FIFO/device inputs, and a nonblocking FIFO open after possible path replacement. Failures retain a typed redacted diagnostic; valid values remain available to stream redaction. |

The CI repair retains 51 attempts, authenticated Checks/Inbox pagination, a complete 4,000-check detail graph, and an expanded first-page response above 4 MiB. Fifty attempts now use 256-check committed graphs with longer commands; the final attempt uses the separate committed 4,000-check graph. This reduces fixture check inserts from 204,000 to 16,800. The test explicitly measures the expanded payload (5,486,922 bytes in this run), so its transport-budget regression coverage cannot silently disappear. On the local macOS host the focused test fell from 8.75 seconds to 1.20 seconds; summary pages remained approximately 21 KiB. The native Windows rerun remains hosted-CI evidence, not something established by local timing or cross-compilation.

Passed after all repairs: `go test -p 1 ./...` (core 102.553 seconds), `go test -race ./cmds/async-commit-hook/...` (core 147.946 seconds), `go vet ./...`, frontend-directory `pnpm test` (28 component tests, typecheck, production build and four script tests), and three API client wire tests. Administrator embedded assets were generated before Go compilation. A network-disabled Linux arm64 container passed all seven new or revised core regression tests. The complete Windows amd64 core test binary cross-compiled; no native Windows execution is claimed for this local pass. Generated repository-owned `dist` directories were removed before delivery.

All six unsigned `0.1.0` archives and the complete checksum manifest passed verification in the dedicated external repair artifact directory. No signing, public release, tap change or site deployment occurred. The PR remains non-draft. The two owner-approved exclusions and previously pending Edge validation remain unchanged. New hosted CI results are not inferred from local checks.

## Sixteenth PR #901 repair invocation

Incoming head `87cc2497` had three unresolved bot findings, no merge conflict and no failed checks (34 passed, three skipped). Each finding is repaired in a separate commit:

| Review thread | Repair and regression evidence |
| --- | --- |
| `PRRT_kwDORRAKg86kFctK` | `ListRepositories` now uses bounded keyset pages across repository/worktree IDs, including within one repository, and probes only the selected page. Display labels are valid UTF-8 capped at 4 KiB while raw paths and IDs remain unchanged. `TestRepositoryPagesBoundLargeRegistryAndPreserveEveryWorktree` reads 2,051 worktrees across 42 authenticated Connect pages, with long paths and JSON-escaped labels, checks every ID exactly once and keeps each response below 8 MiB. Cursor/size validation occurs before database access. Web tests cover explicit next-page loading, split-repository merging, failed-page retry, retained selection, keyboard focus during a blocked read, and suppressed repeated activation; client tests preserve cursor fields and legacy last-page responses. |
| `PRRT_kwDORRAKg86kFctO` | Go report parsing accepts standard interleaved `build-output` and `build-fail` events, with bounded output tails and stable identities keyed by `ImportPath` and occurrence. `TestGoReportAcceptsActualToolchainBuildFailures` runs the installed Go toolchain against an isolated module with a broken dependency, retains the compiler diagnostic and confirms message changes preserve failure IDs. Interleaving tests retain separate build and test output, distinct repeated build failures, nonfailing warnings, and rejection of missing import paths or unknown actions. |
| `PRRT_kwDORRAKg86kFctU` | Agent install/reinstall/uninstall rejects symlinked/nonregular configuration or skill files before backups or ownership changes. Fixtures cover Codex TOML, Claude JSON and both OpenCode JSONC/JSON paths, dangling links, previously owned configurations replaced by links, and linked skills. Each conflict preserves the link, target bytes, existing skill/configuration and ownership/backup counts. A dangling OpenCode JSONC link never selects the JSON fallback. A separate positive test installs/removes through a symlinked parent directory while retaining the directory link and unrelated target content. |

Passed after the repairs: `go test -p 1 ./...` (core 96.412 seconds), `go test -race ./cmds/async-commit-hook/...` (core 158.832 seconds), `go vet ./...`, frontend-directory `pnpm test` (29 component tests, typecheck, production build and four script tests), four API client wire tests, and complete protocol formatting/lint/breaking/freshness checks. Administrator embedded assets were generated before Go compilation. A network-disabled Linux arm64 container passed six new regression tests covering registry pages, bounded cursors/labels, interleaved build events and agent links. The real Go toolchain build-failure test ran on the local macOS host; the container does not have Go and did not run that test. The complete Windows amd64 core test binary cross-compiled; this is not native Windows execution evidence. Generated repository-owned `dist` directories were removed before delivery.

All six unsigned `0.1.0` archives and their complete checksum manifest passed verification in the dedicated external repair artifact directory. The PR remains non-draft. No signing, public release, tap change or site deployment occurred. The two owner-approved exclusions and previously pending Edge validation remain unchanged. New hosted CI results are not inferred from local verification.

## Seventeenth PR #901 repair invocation

Incoming head `64b3ca1f` had six unresolved bot findings, no merge conflict and two failed checks: native Windows Go tests and their aggregate CI Result (run `35481952422`). Each finding and the independent CI budget repair has its own commit:

| Review thread | Repair and regression evidence |
| --- | --- |
| `PRRT_kwDORRAKg86kF0y3` | Queue claims retain every earlier uncancelled queued member's accepted position while prerequisites run. `TestQueueRetainsOrderWhileEarlierDependencyRuns` uses two SQLite connections, makes the newer prerequisite pass first, verifies no overtaking/overlap, then checks release after completion, blocking or queued cancellation. |
| `PRRT_kwDORRAKg86kF0y7` | Branch discovery streams count- and byte-bounded lexical pages into Connect and the web selector. A packed-ref fixture exceeds 8 MiB before pagination; responses stay below 1 MiB and stop at the byte budget. Tests verify complete page coverage without duplicates, worktree-scoped/malformed cursors, oversized records, raw invalid-byte refs and valid transport labels. Web tests retain an unloaded current branch, loaded options and selection across page errors/retry; generated wire tests cover additive cursors and legacy omission. |
| `PRRT_kwDORRAKg86kF0y_` | Every managed Git invocation disables replacement objects. A real local commit replacement changes ordinary Git's source/configuration and introduces unsupported LFS attributes, while ach's exact original SHA still produces the original configuration and prepared source. The replacement ref remains intact; direct raw blob reads also ignore blob replacements. |
| `PRRT_kwDORRAKg86kF0zC` | Configuration validation reserves ACH_MANAGED case-insensitively before acceptance. Tests cover public/secret/credential-backed, required/optional and all platform declarations; no invalid attempt is persisted, and distinct names remain allowed. |
| `PRRT_kwDORRAKg86kF0zF` | Agent merge inputs retain exact bytes and file identity. Settings/skill publication revalidates immediately before replacement; absent destinations use no-replace creation. Tests deterministically interpose in-place edits, identical-byte inode replacement, creation, deletion and symlinks, preserving the concurrent file. Rollback tests cover new and existing skill files and refuse to overwrite concurrent edits; all client install/remove and link-preservation tests still pass. Uncertain post-rename I/O errors retain intended ownership for recovery. |
| `PRRT_kwDORRAKg86kF0zH` | Automatic hooks are restricted to the canonical native common-directory hooks folder. Shared/custom paths, including missing hooks and symlink redirection, receive worktree-scoped manual examples without new files or ownership. A two-repository fixture preserves an existing manager, leaves pre-push absent and verifies uninstall skips unrelated files. Executing the example passes stdin/exit status in the selected worktree and skips an unrelated repository. |

The Windows log reaches Go's 600-second package watchdog while `TestWorkerStorageFailureJoinsMoreRunsThanCompletionBuffer` has run for 15 seconds and is still flushing individual SQLite fixture inserts, before the worker scenario starts. Its 2,050 accepted requests now use one durable preparation transaction while retaining the real queue, cancellation barrier and complete callback joining assertion. The three-OS CI matrix uses an explicit 20-minute per-package watchdog for the growing native integration suite; this is not a product command timeout. The focused shutdown regression passes with race detection, and all 32 CI contract tests plus workflow syntax validation pass.

Passed after all repairs: `go test -p 1 ./...` (core 116.178 seconds), `go test -race ./cmds/async-commit-hook/...` (core 173.408 seconds), `go vet ./...`, frontend-directory `pnpm test` (30 component tests, typecheck, production build and four script tests), five generated-client wire tests, complete protocol formatting/lint/breaking/freshness checks, eight offline release fixtures, 32 CI contract tests and workflow syntax validation. Administrator embedded assets were generated before Go compilation. A network-disabled Linux arm64 container passed ten related core regression tests. The complete Windows amd64 core test binary cross-compiled; native Windows success is not inferred from that compilation or the local suite.

All six unsigned `0.1.0` archives, their executable contents and all ten checksum-manifest entries passed verification in the dedicated external repair artifact directory. No signing, public release, tap change or site deployment occurred. The PR remains non-draft. Generated repository-owned `dist` directories were removed before delivery. The two owner-approved exclusions and previously pending Edge validation remain unchanged. Hosted CI for the new commits is not monitored by this one-shot repair invocation; the prior Windows/aggregate failures remain historical pre-push results.

## Nineteenth PR #901 repair invocation

The eighteenth invocation found no actionable findings and made no changes. This invocation starts from `04a5cf62` with three new bot findings, no merge conflict and no failing checks (34 passed, three skipped). Each finding has its own repair commit:

| Review thread | Repair and regression evidence |
| --- | --- |
| `PRRT_kwDORRAKg86kGQzJ` | Commit history now streams through a fixed buffer instead of retaining Git stdout. Subjects retain a valid UTF-8 prefix of at most 4 KiB with an ellipsis; parent IDs remain complete within a bounded header. Tests import a real 100-commit page with a 12 MiB subject and many 96 KiB subjects, verify the response remains below 5 MiB, preserve every commit/parent ID and retrieve the next offset page. Additional stream cases cover tabs, invalid bytes, Unicode boundary cuts, entirely invalid long subjects, malformed/oversized parent records and page overflow. |
| `PRRT_kwDORRAKg86kGQzP` | Windows uses a deterministic Git-compatible regular link-text representation with managed core.symlinks=false, requiring no symlink privilege or Developer Mode. Tests create committed link entries through the index without OS links, verify normal/dangling/external/Unicode targets, exercise the Windows materializer on every host and successfully execute the resulting source. Unix keeps native links. A platform-specific fingerprint discriminator prevents old Windows native-link evidence from satisfying the new context; preparation and rerun reject that legacy context with fresh-run guidance. |
| `PRRT_kwDORRAKg86kGQzT` | Pre-push exposes its selected durable gate before post-acceptance startup/read/wait work. Both modes retain the queued run ID, SHA, state and startup-failed recovery diagnostic when runner.log cannot be opened. Tests preserve the preceding successful tip, verify retry reuses the same saved request, and ensure pre-acceptance errors produce no receipt. A portable CLI test verifies one versioned JSON response on stdout, diagnostics on stderr, exit 3 and an inspectable queued run in SQLite. |

Passed after all three repairs: `go test -p 1 ./...` (core 113.702 seconds), `go test -race ./cmds/async-commit-hook/...` (core 170.011 seconds), `go vet ./...`, frontend-directory `pnpm test` (30 component tests, typecheck, production build and four script tests), five generated-client wire tests and protocol generation/freshness verification. Administrator embedded assets were generated before Go compilation. A network-disabled Linux arm64 container passed six related core regressions, including the explicit Windows link-text materialization path. Complete Windows amd64 core and CLI test binaries cross-compiled; this is not native Windows execution evidence.

All six unsigned `0.1.0` archives, their executable contents and ten checksum-manifest entries passed verification in the dedicated external repair artifact directory. The PR remains non-draft. Generated repository-owned `dist` directories were removed before delivery. No signing, public release, tap mutation or site deployment occurred. The two owner-approved exclusions and previously pending Edge validation remain unchanged. New hosted CI results are not inferred from local tests or monitored after the one-shot push.

## Twentieth PR #901 repair invocation

Incoming head `43930077` had a merge conflict, no unresolved bot findings and no failing checks. The explicit CI inventory contained 33 passed checks, three skipped checks and one running Windows Go check. This invocation merges the five upstream commits through `8040b312`, including the Runmoor registration-inspection repair and stable release workflow, the project-selected release coordinator and its dispatch behavior. Merge commit `1f4a7597` resolves the only content conflict in `docs/repository-workflow-contract.md`: it retains the current Runmoor compatibility and release contracts and places the existing three-OS Go 20-minute test watchdog explanation under continuous integration. The ach command retains version `0.1.0`; Runmoor's upstream version is preserved independently.

Focused validation passed before the merge commit: 35 CI contract tests, workflow syntax validation and 38 Runmoor/project release fixtures. Final local verification passed with `go test -p 1 ./...`, `go test -race ./cmds/runmoor/...` (24.714 seconds), and `go vet ./...`. The changed Derun MCP and Runmoor packages executed successfully; unchanged ach tests reused valid Go cache results. Administrator embedded assets were generated before Go compilation. Both affected documentation apps passed their directory-local `pnpm test`: Runmoor built its production site, passed 124 content tests and validated all seven clean routes; public-docs built its production site and passed clean URL validation.

No new ach runtime, protocol, client or frontend behavior changed in this merge repair, and the prior six-target archive evidence is not presented as a new build. Generated repository-owned `dist` directories were removed. No signing, public release, tap mutation or site deployment occurred. The PR remains non-draft; hosted results for the outgoing merge are not inferred from local checks or monitored after the one-shot push. The two owner-approved exclusions and previously pending Edge validation remain unchanged.

## Twenty-first PR #901 repair invocation

Incoming head `ea382b21` had merge conflicts, six unresolved bot findings, and failures in Windows Go tests plus the aggregate CI Result (run `35486202620`). Merge commit `43fbba5e` incorporates upstream `0a386f6f`, preserving both clibox and ach CI jobs, change-planner entries, package ownership rules and lockfile importers. Forty CI contract tests, workflow syntax validation, frozen lockfile verification, clibox's 15 launcher/distribution tests, project release fixtures and local npm/pnpm consumer smoke tests passed. Root `cargo test` passed with `TMPDIR=/private/tmp RUSTC_WRAPPER=`: the host's unavailable sccache and `/var` versus `/private/var` temporary-path alias caused the earlier environment-only attempts to fail. No Rust implementation change was needed beyond the upstream merge.

Each review finding has its own repair commit:

| Review thread | Repair and regression evidence |
| --- | --- |
| `PRRT_kwDORRAKg86kGmRA` | All exact-commit gates share the same latest-attempt selector. Evidence hashing happens outside SQLite transactions, followed by complete metadata revalidation in a short transaction. A barrier pauses verification while a second connection writes unrelated state, accepts a newer attempt or prunes the candidate; read-only gates never reuse stale success or submit a replacement, while run-and-wait retains its replacement behavior. |
| `PRRT_kwDORRAKg86kGmRB` | Working-tree configuration uses a reader capped at 1 MiB plus one overflow byte before parsing. An unbounded synthetic input proves the exact read budget; boundary, overflow and I/O error tests pass. CLI integration rejects a sparse 1 GiB working file with exit 2 and retains nested/linked-worktree discovery behavior. |
| `PRRT_kwDORRAKg86kGmRC` | Additive opaque branch identities separate raw ref identity from normalized labels. Real packed Git refs with distinct invalid UTF-8 bytes share the same display label but select different commits, diffs and retained run histories. Current-worktree identity survives before its page is loaded; history remains accessible after source removal. Malformed, oversized, cross-worktree and conflicting selectors are rejected. Web tests retain both options and send identities to Changes, Commits and Checks while leaving Inbox branch-unfiltered; wire tests preserve legacy omissions. |
| `PRRT_kwDORRAKg86kGmRE` | SQLite uses an escaped absolute file URI with fixed connection parameters. Tests open, write and reopen paths containing URI punctuation, spaces and Unicode while retaining WAL, FULL synchronization, foreign keys and busy timeout. Linux additionally executes the invalid-byte filename case. Exact parent-directory contents rule out a redirected database. |
| `PRRT_kwDORRAKg86kGmRI` | Checks and Inbox require both selected repository/worktree IDs before querying or rendering rows. Controlled pending discovery and selected/empty/error outcomes verify no unfiltered request occurs; successful discovery sends only the intended scope. |
| `PRRT_kwDORRAKg86kGmRM` | Go report completeness tracking retains only unfinished identities and deletes pass/skip/fail entries immediately. A 100,000-test report passes, reopening a completed identity remains incomplete until its terminal event, and a repeated failure retains its second-occurrence ID. Separate output-tail and build-event regressions also pass. |

The independent Windows CI repair addresses the persistence fixture's dependency on native-shell completion before it can reach an injected write. The hosted collecting case stayed before that write until its two-minute watchdog expired; the precise external-shell stall is not inferred from the log. The fixture now re-executes a deterministic test subprocess through real process ownership, requires its completed output for collecting/passed cases, and retains assertions for the exact SQLite error, worker lock release, process termination and interrupted recovery. Focused race tests and Linux execution pass. Other runner integration tests retain actual PowerShell coverage; native Windows success requires the outgoing hosted run and is not inferred from local results.

Passed after all repairs: `go test -p 1 ./...` (ach core 117.383 seconds), `go test -race ./cmds/async-commit-hook/...` (core 182.339 seconds), `go vet ./...`, frontend-directory `pnpm test` (34 component tests, typecheck, production build and four script tests), six client wire tests plus typecheck, and complete protocol formatting/lint/breaking/freshness checks. Administrator embedded assets were generated before Go compilation. A network-disabled Linux arm64 container passed seven related core regressions, including raw filename bytes and the deterministic persistence helper. Complete Windows amd64 core and CLI test binaries cross-compiled; no local native Windows runtime verification is claimed.

All six unsigned `0.1.0` archives, their executable contents and ten checksum-manifest entries passed verification in the dedicated external repair artifact directory; eight offline ach release fixtures also passed. Generated repository-owned `dist` directories were removed. No signing, public release, tap mutation or site deployment occurred. The PR remains non-draft. The two owner-approved exclusions and previously pending Edge validation remain unchanged. The historical Windows/aggregate CI failures refer to the incoming head; new hosted results are not inferred from local validation or monitored after the one-shot push.


## Twenty-second PR #901 repair invocation

Incoming head `f272f013` had no merge conflicts, three unresolved bot findings and no failing CI. The explicit CI inventory contained 38 passed checks and three skipped checks. The original worktree directory had been removed externally; repair work resumed from the unchanged PR branch in a new isolated worktree without modifying the main checkout.

| Review thread | Repair and regression evidence |
| --- | --- |
| `PRRT_kwDORRAKg86kG6T_` | State startup creates missing directories privately and validates existing directories without chmod or DACL replacement. Unix regressions preserve shared root/child permissions and unrelated contents, while existing private state reopens successfully. The Windows-specific test verifies a shared DACL remains byte-equivalent after rejection; it is cross-compiled locally, not claimed as executed on Windows. A trailing-separator regression ensures the new leaf receives its private Windows ACL at creation. Public configuration guidance explains explicit recovery. |
| `PRRT_kwDORRAKg86kG6UC` | Hook uninstall uses the existing no-follow regular-file snapshot primitive, checks owned content, then revalidates exact identity and bytes immediately before removal. Deterministic intervening edits, same-byte replacements, creation, removal and symlinks all preserve the concurrent state and installation record with `hook-conflict`. Existing ordinary uninstall, refresh, fallback and rollback tests pass. |
| `PRRT_kwDORRAKg86kG6UD` | Lifecycle discovery reclaims dead CLI, viewer, MCP, worker and daemon records from SQLite and account control, retaining live birth identities. Tests cover previous-state rows, intentionally removed state and retry after injected database failure. Supervisor backend/boot proof remains required. The first full-suite run exposed accidental treatment of the update recovery journal as a component; a follow-up fix limits discovery to UUID-v7 filenames with matching embedded IDs. Journal-preservation, mismatched-ID and existing authenticated-update recovery tests pass. |

Passed on the final implementation: `go test -p 1 ./...` (ach core 130.329 seconds), `go test -race ./cmds/async-commit-hook/...` (core 195.567 seconds), and `go vet ./...`. Administrator embedded assets were generated before repository-wide Go compilation. Frontend-directory `pnpm test` passed 34 component tests, typecheck, production build and four script tests; the API client passed six wire tests. Protocol formatting/lint/breaking/freshness, workflow lint and 48 CI/release fixture tests passed. A network-disabled Linux arm64 container passed 15 focused regressions, including supervisor ownership and authenticated update recovery. The complete Windows amd64 core test binary cross-compiled; native Windows test execution is not claimed.

All six unsigned `0.1.0` archives, executable entries and ten checksum entries were rebuilt and verified after the last code change. Generated repository-owned `dist` directories were removed before delivery. No signing, public release, tap mutation or deployment occurred. The PR remains non-draft. The two approved exclusions remain unchanged; previously pending Edge validation is still outstanding and is not an approved exclusion. Outgoing hosted CI is not inferred from local tests or monitored after the one-shot push.


## Twenty-third PR #901 repair invocation

Incoming head `4d9fb006` had a merge conflict, no unresolved bot findings and no failing checks. Merged upstream through `fecd5bf1`, including native Linux CLI package distribution, Clibox release compatibility and the upstream binpm `0.2.1` release. The ach source version remains `0.1.0`.

Resolved adjacent documentation additions in root/app/command `AGENTS.md` and the docs catalog by preserving both complete contracts. The async-commit-hook ownership, local-only state and nonpublishing release restrictions remain separate from the upstream six-project APT/DNF publication contract. Automatic CI, package and lockfile merges retain both projects' jobs and dependencies.

Before the merge commit, all four changed documentation apps passed their package-local `pnpm test`, the focused CI/release contract suite passed 66 tests, workflow lint passed, and the Derun Go suite passed. The broader CI/release run passed 253 of 255 tests on macOS; its two failures required absent Debian tooling or GNU tar. Both affected test files then passed all four tests in a network-disabled Debian-based Node container without changing implementation or weakening assertions.


While validation was running, upstream advanced to `f038fd39` with Clibox utility implementation. A second merge includes that exact revision and its native Rust-test CI step without changing ach. The updated root Rust suite, all 43 CI contracts, workflow lint, 15 Clibox package tests and real npm/pnpm host consumer smoke tests passed before committing this integration. The DevHud frontend dependency was regenerated for the root Rust validation boundary.


Merge validation passed before the late review repairs: root `go test -p 1 ./...`, `go vet ./...`, root `TMPDIR=/private/tmp RUSTC_WRAPPER= cargo test` after generating the required frontend dependency, and `cargo fmt --all --check`. The Clibox standalone `cargo publish -p clibox --dry-run` packaged and verified successfully and explicitly aborted before upload. Documentation-app, CI/release fixture, workflow and npm/pnpm consumer results are recorded above. The incoming hosted CI inventory had 38 passed checks and three skipped checks; outgoing CI and the new native Linux package installation matrix are not inferred from these local tests.

The single final remote inventory contained four new bot findings that arrived during merge validation. Each actionable finding has a separate repair commit; the invocation does not poll for additional reviews or hosted results after that inventory.

| Review thread | Repair and regression evidence |
| --- | --- |
| `PRRT_kwDORRAKg86kHN7T` | Workspace preparation rediscovers and validates the accepted repository/worktree identities before object-format lookup, object fetching or owned-workspace creation. A real replacement clone at the same path cannot execute the old queued request, whether unregistered or separately initialized. Tests verify that no workspace is created, the old attempt fails and a fresh initialized submission succeeds. |
| `PRRT_kwDORRAKg86kHN7U` | JUnit parsing retains only the aggregate 1 MiB failure-summary prefix, then consumes and validates the remaining XML without accumulating additional failures. Fields are redacted before truncation. A 100,000-failure fixture checks the budget; malformed tails, negative counts and text outside the root still fail after exhaustion. A real check with 20,000 failures preserves its failed state, truncation diagnostic and complete paginated report evidence. A secret crossing the display boundary remains masked. |
| `PRRT_kwDORRAKg86kHN7V` | Native hook creation and rollback operate through a common-directory `os.Root` handle. Tests replace the validated hooks parent with an external symlink before and after opening the root and verify no external hook or ownership record is created. Rollback cannot follow a replaced parent into an unrelated target. All existing hook ownership, install, refresh, uninstall and rollback tests pass. |
| `PRRT_kwDORRAKg86kHN7W` | Source RPCs pass their request context through worktree lookup, SQLite reads and Git discovery, preserving canceled and deadline-exceeded Connect statuses. For branches, commits and changes, a portable re-executed test binary stalls as Git, publishes its process identity and is confirmed reaped when the request is canceled. Expired requests retain their deadline status. |

After the four repairs, root `go test -p 1 ./...` passed (ach core 206.096 seconds), `go vet ./...` passed and complete protocol formatting/lint/breaking/freshness checks passed. Administrator embedded assets were regenerated before repository-wide Go compilation. A network-disabled Linux arm64 container passed 11 related core regressions, including raw worktree-path bytes, bounded JUnit summaries, source identity, hook confinement and source-RPC cancellation. The complete Windows amd64 core test binary cross-compiled; native Windows execution is not inferred from compilation. All six unsigned `0.1.0` executable archives and all ten checksum entries were rebuilt and verified after the final code change.

The first complete race run, concurrent with repository tests and cross-builds, failed two existing orphan-process fixtures at their five-second readiness watchdog; it reported no data race. Both fixtures then passed unchanged for three consecutive race-enabled repetitions. The complete `go test -race ./cmds/async-commit-hook/...` passed when run separately (core 176.556 seconds). The exact cause of the initial readiness delays is not inferred from the timeout alone. No assertions or product timeouts were changed to obtain these results.

Generated repository-owned `dist` output was removed after validation. No release, signing credential, package repository, Homebrew tap or website was published or modified. The ach version stays `0.1.0`, and PR #901 remains non-draft. The two approved exclusions remain unchanged; pending Edge validation is still outstanding and is not an approved exclusion. The four handled threads are resolved only after the single final push succeeds, without subsequent CI polling.

## Twenty-fourth PR #901 repair invocation

Incoming head `9d9dc667` already included the upstream PR package-matrix policy merge. The repair inventory reported no merge conflict, six unresolved bot findings and no failed checks. The explicit CI inventory contained 38 passed checks and four skipped checks. The prior repair checkout had been removed externally; work resumed in a fresh isolated worktree, fast-forwarded to the current PR head without changing the main checkout. Each actionable review finding has a separate commit.

| Review thread | Repair and regression evidence |
| --- | --- |
| `PRRT_kwDORRAKg86kHfai` | Go report parsing bounds redacted failure summaries to 1 MiB as events arrive. A second scan counts completed iterations only for retained identities, avoiding a map of every completed test while preserving original build/test IDs and pass/skip history. Separate 100,000-event build/test failure fixtures check the prefix budget and identity values; malformed, unknown-action, missing-import and unfinished tails still fail. Sparse repeated identities retain their third occurrence after 100,000 unrelated passes. Secret-boundary and real execution tests retain redaction, truncation diagnostics and complete paginated evidence. |
| `PRRT_kwDORRAKg86kHfak` | Native hook refresh snapshots, staging, revalidation, rename and cleanup use the same opened Git common-directory root. Tests redirect the validated parent before/after the snapshot to an external directory holding identical stale hook bytes, and separately replace the original inode with identical bytes. All preserve the concurrent file and ownership without external staging. Existing refresh, recovery, creation, removal and rollback tests pass. |
| `PRRT_kwDORRAKg86kHfao` | Caller cancellation/deadline errors are preserved after Git process/pipe cleanup across discovery, commit/config/base resolution and streamed branches/history/diffs. Eight post-discovery stages and the three original discovery RPCs stop at a deterministic test-process barrier, then confirm Connect Canceled and process reaping. Barrier-controlled deadline contexts cover history and merge-base; actual already-expired request coverage remains. Partial-page cleanup keeps its caller active. |
| `PRRT_kwDORRAKg86kHfat` | Self-update compares authenticated release executable bytes before backups or replacement, still validating its version. Three identical update preparations with a closed state database preserve executable identity/mtime, leave no candidate, backup or journal and return unchanged. An identical binary reporting the wrong release version is rejected. A different local binary with the same version follows state backup and replacement, preserving the exact original bytes. Existing signature, archive, interrupted replacement and recovery tests pass. Public update guidance documents up-to-date behavior. |
| `PRRT_kwDORRAKg86kHfav` | Uninstall skips settings publication/backup when no owned MCP entry is removed, revalidates the snapshot and completes skill/ownership cleanup. Sixteen fixtures cover Codex, Claude Code and both OpenCode JSONC/JSON paths with absent settings, both files absent, an already removed entry and an empty file. Repeated removal preserves absence or exact file identity/content, creates no no-op backup, and clears the remaining skill and record. Missing OpenCode JSON fallback destinations retain their recorded ownership path. Existing install/conflict/symlink/rollback tests pass. |
| `PRRT_kwDORRAKg86kHfaw` | Execution deep links bind the heading, path, branch selector and sidebar selection to the run's exact repository/worktree pair using the shared detail query cache. Five component scenarios cover a non-first worktree, later registry pages with failure/retry, unavailable workspace metadata, missing execution and failed execution reads. No unrelated worktree/branch/history request or acknowledgement occurs while resolving; complete evidence and focus remain available, GetRun is fetched once, and returning to history uses the target worktree. Pagination remains explicit. |

Final local validation passed: root `go test -p 1 ./...` (ach core 122.422 seconds), `go test -race ./cmds/async-commit-hook/...` (core 262.138 seconds), `go vet ./...`, frontend-directory `pnpm test` (39 component tests, typecheck, production build and four script tests), six client wire tests, complete protocol formatting/lint/breaking/freshness, 53 CI/release fixture tests and workflow validation. Administrator embedded assets were regenerated before repository-wide Go compilation. No Rust source was changed by this repair invocation.

A network-disabled Linux arm64 container passed 13 related core regressions, including bounded Go summaries, hook confinement, post-discovery source cancellation, unchanged updates and absent agent settings. The complete Windows amd64 core test binary cross-compiled; native Windows execution is not inferred from compilation. All six unsigned `0.1.0` executable archives and all ten checksum entries were rebuilt and verified after the final code change.

Generated repository-owned `dist` output was removed after validation. No release, signing credential, package repository, Homebrew tap or website was published or modified. The ach version remains `0.1.0`, and PR #901 remains non-draft. The two approved exclusions remain unchanged; pending Edge validation is still outstanding and is not an approved exclusion. The six handled threads are resolved only after the single final push succeeds, without subsequent CI polling.

The single final remote inventory still contained exactly the same six handled findings and no failing checks across 42 checks. GitHub reported merge state `UNKNOWN`, so this evidence does not assert final mergeability. No further remote status polling is performed in this invocation.

## Twenty-sixth PR #901 repair invocation

The preceding twenty-fifth invocation made no changes: no actionable findings or failed checks were available and three checks were still running. This invocation starts from `71d32987` with no merge conflict, three new bot findings and a failed Windows Go job plus its aggregate CI Result. The working tree was clean. Each review repair and the independent CI root cause receives its own commit.

- `PRRT_kwDORRAKg86kHuDn`: byte retention counts evidence and leftover owned workspaces, including workspace-only cleanup retries. Four terminal/expired and evidence-present/absent scenarios verify exact dry-run bytes, no preview mutation, quota selection, tombstones and repeat cleanup; a separate scenario protects pending workspaces and inherited evidence. The focused prune, retention-bound and inherited-provenance suite passed. Scans do not follow symlinks and surface I/O errors instead of silently reporting a partial byte total.

- `PRRT_kwDORRAKg86kHuDp`: implicit self-update scans the complete paginated inventory and compares canonical stable ach semantic versions. A three-page fixture puts a newer release behind a recently published backport; drafts, prereleases, foreign and malformed tags cannot win. Tests cover numeric ordering, equal versions, large components, implicit downgrade rejection, explicit rollback and late-page failure without partial selection. Focused version/authenticity/unchanged-update tests passed, and the frontend-directory `pnpm test` passed after updating public guidance (39 component tests, typecheck, production build and four script fixtures). The existing x/mod version is reused as a direct dependency.

- `PRRT_kwDORRAKg86kHuDs`: failed update preparation removes its exclusively created partial state backup, plus a completed backup/candidate if later pre-journal work fails. Three repeated failures each cover a closed database, missing evidence root, partial evidence copy followed by a rejected symlink, and journal destination conflict after candidate/version/backup preparation. Previous backups, the installed executable and a pre-existing journal path remain unchanged. Focused preparation, authenticated replacement and recovery tests passed. A journal already published before a directory-sync error retains referenced files and requires recovery; cleanup errors are surfaced and logged.

The independent CI repair addresses Windows job `106040257292` in run `35496466391`: eight source cancellation/deadline subtests failed opening the concurrently published readiness file with a Windows sharing violation. The fixture now receives the stalled test process PID over an isolated loopback connection, removing that file publication/read race while preserving real process identity, canceled/deadline Connect status and child-reaping assertions. Two complete focused race-enabled repetitions passed. The explicit CI inventory contained 36 passed checks, four skipped checks and the Windows/aggregate failures; outgoing native Windows success is not inferred from local validation.

After all four repairs, root `go test -p 1 ./...` passed (ach core 153.990 seconds), as did `go vet ./...`, complete protocol formatting/lint/breaking/freshness, six client tests, 53 CI/release fixtures and workflow validation. Administrator embedded assets were generated before repository-wide Go compilation. The changed public guidance passed the frontend-directory test/typecheck/build described above. No Rust source changed.

A network-disabled Linux arm64 container passed 15 related regressions covering retention, backup cleanup, release selection and source cancellation. The complete Windows amd64 core test binary cross-compiled; native Windows execution is not inferred from compilation. All six unsigned `0.1.0` executable archives and all ten checksum entries were rebuilt and verified after the final code change.

The final complete `go test -race ./cmds/async-commit-hook/...` passed (core 319.673 seconds, integration 23.373 seconds and CLI 3.428 seconds). No data races or test failures were reported. Generated repository-owned `dist` output was removed after validation. No signing, release, package repository, Homebrew tap or website publication was performed. Version `0.1.0` and the non-draft PR are preserved. The two approved exclusions remain unchanged; previously pending Edge validation remains outstanding and is not an approved exclusion. New hosted CI is not inferred from local validation or monitored after the single push.

The single final remote inventory contained only the same three handled findings and the same incoming Windows/aggregate failures across 42 checks; it reported no merge conflict. These findings are resolved only after the single final push succeeds.
