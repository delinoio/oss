### Instructions for `servers/`

- Follow root `AGENTS.md`, project index docs, and relevant `docs/servers-*.md` contracts before implementation.
- Keep repository and domain rules in the appropriate `AGENTS.md` files.
- Write all source and comments in English.
- Prefer enums or typed constants over free-form strings for API contracts.

### Scope in This Domain

- `servers/thenv`: Backend for secure environment sharing.
- `servers/delibase`: Go/PostgreSQL/sqlc organization, billing, and usage service owned by project `delibase`.
- `servers/devhud-deck`: bounded authentication/persistence foundation for the
  client-initiated GitHub.com pull-request service owned by project `devhud`;
  connection, refresh/live billing, mutation, snapshots, and notification
  resolution are implemented while push delivery and deployment remain
  unimplemented.
- `servers/devhud-realqa`: implemented inactive Go/PostgreSQL/sqlc preset/tracker/auth/deletion, internal GitHub.com issue-provider, screenshot submission, live-transfer/initial-storage-authorization, and public-image foundation plus planned recurring storage/rebind/terminal-cleanup slices owned by project `devhud`.
- `servers/internal`: Repository-shared Go package boundary currently consumed by delibase; planned DevHud feature servers require explicit compatibility review before consuming provider-agnostic subsets, and no consuming project owns it.

### Server Language and Data Rules

- Servers in this domain must be implemented in Go.
- SQL queries and type-safe data access must use `sqlc`.
- Protobuf definitions should live at `proto/<service_name>/v1/*.proto` unless a project contract explicitly uses a shared cross-runtime proto root.
- Each server project must provide a local protobuf generation script and a `go generate` entrypoint.
- Keep API boundaries explicit and versionable.
- Prioritize Connect RPC-based communication for business flows over Tauri-specific bindings.
- Keep authorization and audit behavior documented and testable.
- Never expose secret values in logs or default API responses.

### Fixed Server Project Structure

Stateful server projects under `servers/<service_name>/` should follow this minimum structure:

- `cmd/<service_name>/main.go`
- `internal/service/`
- `internal/contracts/`
- `internal/logging/`
- `db/query/`
- `db/migrations/`
- `db/sqlc.yaml`
- `proto/<service_name>/v1/*.proto`
- `buf.yaml`
- `buf.gen.yaml`
- `scripts/generate-go-proto.sh`
- `generate.go` (with `go:generate` directive)

Scaffold-only service projects may start with a smaller structure (`main.go` + `internal/service`) when documented in the project index and matching server-domain contract docs, but must adopt explicit contract/data/logging subdirectories before persistence and public API rollout.

### Integration Rules

- Changes to server interfaces must be synchronized with related CLI and app contracts.
- Update `docs/project-thenv.md` and `docs/servers-thenv-server-foundation.md` for every thenv interface or trust model update.

### Delibase Rules

- Follow `docs/project-delibase.md`, `docs/servers-delibase-server-foundation.md`, `docs/protos-delibase-api-contract.md`, and `docs/servers-internal-foundation.md` before implementation.
- The canonical future API origin is `https://delibase.deli.dev`; do not activate or deploy a runtime for issue #722.
- Use PostgreSQL and sqlc for persistence, UUID v7 for persisted IDs, signed int64 USD micro-units for money, signed int64 units for usage, non-overlapping effective price/billing-period windows, organization-scoped billing references, and transactional/locked append-only ledger, audit, hierarchy, and reservation invariants. Every committed organization must retain at least one active Owner and exactly one Polar customer. Mutations against an existing organization acquire its row lock before locking or creating an account; a completed idempotency replay may return before those business locks. Organization/team-membership identity, Polar subscription identifiers, and Logto service-client identifiers are immutable; organizations may retain historical subscription rows but have at most one active subscription, newer Polar events may reactivate scheduled-canceled subscriptions, and revoked subscriptions remain terminal. Reservation prices, team snapshots, active memberships/accounts and effective team access, storage-derived creation times, expiries, credit availability, and active-subscription current-period overage capacity must match authoritative storage state at reservation and commit; reservation insert triggers acquire the organization lock before billing-period or subscription snapshot locks; reservation costs must equal their overflow-safe unit-price product; commits must originate from immutable usage records, consume only the held credit/overage split, require and snapshot the containing billing period for overage-funded usage settlement, allow credit-only commits without a billing-period snapshot when no period contains the commit, and never exceed the reserved maximum. Release and expiry must restore the held credit/overage amounts through matching ledger rows. Automatic expiry reports ledger-inconsistent held rows while continuing later releasable expired rows in the same organization, and explicit release and automatic expiry use distinct typed audit events. Polar usage delivery must omit credit-funded settlement and report only nonzero overage as chargeable USD micro-units; a deferred database invariant requires exactly one matching durable Polar outbox event for every nonzero-overage usage record, with organization, record, event-name snapshot, commit timestamp, and overage micro-units equal to the immutable usage row. Reservation rows, webhook inbox events, integration-outbox events, and deletion jobs cannot be deleted; organization deletion markers and retained slug aliases are one-way/immutable; integration-outbox event bodies and deletion-job targets are immutable; and append-only audit rows accept only pseudonymous actors and allowlisted metadata that passes credential-shape rejection. Deleting organizations cannot accept new reservations. Revoked or expired invitations cannot be accepted, and active reservations retain their service allowlist and Polar meter mapping. Active references block operational deletion while finalized financial and usage snapshots do not; blocker-gated deletion and membership removal fully expire relevant TTL-ended reservations before evaluating active references.
- Held reservations block organization-member removal and self-leave with `ERROR_REASON_MEMBER_HAS_ACTIVE_RESERVATIONS`.
- Persisted Polar meter identifiers must satisfy the provider request-shape boundary before usage snapshot backfill. Automatic expiry must keyset-paginate beyond full failed-organization candidate pages so later healthy organizations remain eligible in the same batch.
- Authenticated invitation preview and acceptance must not require a pre-existing local account, organization membership, or team access. Acceptance locks the organization before creating or locking the active local account with a privacy-safe non-empty placeholder display name when absent, then locks the invitation, revalidates invitation state, and records the account's first acceptance before creating memberships. A later request with a different idempotency key must not let the same account consume the invitation again or restore removed access.
- Team reads and mutations must establish organization membership before resolving requested team identities so non-members receive the same membership-required failure for existing and absent team IDs.
- Keep Logto identity validation separate from delibase authorization; validate the canonical `https://delibase.deli.dev` audience, require confirmation for non-`General` subtree deletion, and exclude organization deletion from Admin permissions. Polar owns payment settlement/invoices, while delibase owns local users keyed immutably by their original Logto `sub`, organization, team, membership, ledger, reservation, and audit state.
- The six Connect services are `AccountService`, `OrganizationService`, `TeamService`, `CatalogService`, `BillingService`, and `UsageService`. Human calls use user tokens except for anonymous `CatalogService` reads; live-user usage mutations validate M2M and the `x-delibase-forwarded-user-token` forwarded end-user context, whose value is always redacted. Live-user usage idempotency remains service-scoped and binds the forwarded user subject into the digest.
- Bounded background usage extends only `BillingService` and `UsageService`. Human authorization create/revoke and M2M authorized reserve/commit/release/resource-deletion notification use distinct idempotency operations. Authorized usage uses `ModeM2M` and never requires or persists a forwarded user bearer. Reserve/commit/release digest-bind authenticated service plus authorization/purpose/resource/UTC-period/units, with commit/release additionally binding the reservation ID; resource deletion binds authorization/service/purpose/resource/expected revision. Public catalog meters expose stable service authorization targets without Logto client IDs or credentials. Reserve accepts only the current or immediately preceding UTC day according to server time, while commit/release use the reservation's stored period. Grants use UUID v7, bind the authorizer, personal/organization owner, organization/team, exact service/meter, `REALQA_STORAGE`, feature resource, `UTC_DAY`, and a per-period maximum, and fail closed on substitution, lost access, non-active status, limit overflow, or replay conflict. Resource deletion transitions `ACTIVE` to `RESOURCE_DELETED` only at the current expected revision and returns an existing `REVOKED`, `ACCESS_LOST`, `RESOURCE_DELETED`, or `OWNER_DELETED` status/current revision without transition to the exact bound service whose expected revision predates closure; future/unknown revisions and substitutions fail closed. Deck must have no purpose value.
- Shared reusable auth/JWKS, Connect interceptors, redaction, request/trace IDs, HTTP defaults, structured logging, and UUID v7 code belongs under `servers/internal`; business policy remains in delibase.
- Shared Logto validation must require the exact configured issuer, canonical `https://delibase.deli.dev` audience, expiry, allowed asymmetric signature/header type, exact ES algorithm/curve pairing, `kid`, route scopes, and the expected user or client-credentials token shape. Unknown JWKS key IDs force a serialized refresh without blocking fresh cached-key validation; stale-key use is opt-in and bounded; final JWKS response URLs after redirects must satisfy the configured HTTPS/no-credentials/no-query/no-fragment boundary.
- Authentication route policies must select an explicit public, user, M2M, or M2M-plus-forwarded-user mode. The zero-value mode is invalid so missing policy entries fail closed.
- Authentication middleware must strip authorization and forwarded-user credential headers after validation and attach typed claims to context. Shared code must never infer local organization, team, billing, meter-allowlist, or authorization state from Logto claims/scopes.
- Shared safe-error mapping must attach vetted bearer or forwarded-user authentication reasons, map only explicit signing-key provider unavailability to retryable dependency failure, classify unknown keys and key/algorithm/type/curve mismatches as invalid credentials, and may otherwise preserve an intentional Connect status code and recognized `delibase.v1.ErrorDetail.reason` only. It must discard arbitrary response metadata, unrecognized detail types, and free-form detail fields; validated request and trace IDs are added to unary and streaming failures by the outer request metadata interceptor.
- Server root loggers must use the shared redacting handler, and routine events must use allowlisted safe fields plus keyed actor pseudonyms in the exact `actor:v1:<32-lowercase-hex>` shape. Configure pseudonymization with a distinct secret of at least 32 bytes; never log the key or raw Logto subject.
- Delibase inbox, outbox, and deletion-job enqueueing must use the typed `internal/reliability` contracts with transaction-bound sqlc queries. Accepting account or organization deletion atomically enqueues separate immutable Deck and RealQA lifecycle calls, keyed by the delibase deletion-job UUID and delivered through feature-scoped M2M identities only to each API's typed `DeleteFeatureData` lifecycle mode; delibase owns each exact outbound origin/audience and distinct Logto client ID/secret, and startup fails before deletion acceptance or lifecycle claims if implemented dispatch lacks either complete validated identity set. Absent feature data is an idempotent success. Worker dispatch identifiers are allowlisted typed values. Claims use skip-locked leases and claim-token compare-and-set transitions; normal attempts are capped at exactly 12, expired twelfth claims recover into retained dead-letter state, and dead letters retry every 24 hours until terminal success. Retry clocks, jitter, and claim-token generation remain injectable for deterministic tests.
- Delibase Polar startup requires the single provider product ID, fetches it from the configured production/sandbox API, verifies one active fixed monthly USD `$10` base price, and only then pins it together with the provider environment to the database-enforced `10,000,000`-micro-unit paid-cycle grant. Production is the default provider environment; sandbox checkout requires the explicit `DELIBASE_POLAR_ENVIRONMENT=sandbox` opt-in, and startup must fail rather than reuse provider state persisted for the other environment. An optional compatible provider API override must be an HTTPS URL without credentials, query, or fragment; official production/sandbox endpoints remain the default.
- Polar subscription checkout creation must durably claim one provider-idempotency attempt per organization before provider creation. The same request digest may recover an ambiguous or post-provider-success failure, distinct keys remain blocked, and successful active-checkout, audit, and idempotency finalization removes the attempt atomically under the organization billing lock. A definitive provider rejection clears the attempt; a successful checkout blocks distinct idempotency keys until provider expiry. Billing summaries prefer an active subscription, otherwise report an unexpired checkout as checkout-pending, and select inactive subscription history by provider-event recency. Retained refunds preserve their organization-scoped provider-order binding and monotonic provider-event/reversal/chargeback state, and refund reversal increases must transactionally match retained paid-cycle totals. Retained paid cycles immutably snapshot their provider-order, subscription, billing-period, and original period-bound identities, increase reversals only to match retained refunds, and block deletion of their referenced subscriptions and billing periods. Any outstanding refund shortfall after a renewed paid grant carries into the renewed current billing period; append-only reconciliation adjustments remove the carried amount when a reordered paid grant clears the organization-level debt.
- Polar refunds and chargebacks must reconcile against retained financial history after organization deletion. Organization deletion must cancel every nonterminal Polar subscription, including subscriptions materialized by delayed webhooks after deletion. Hosted billing-portal sessions and subscription checkouts use distinct typed audit events.
- New usage client references are canonical, credential-safe, and unique per service. Migration preserves benign legacy syntax and duplicate correlation values as immutable grandfathered snapshots, while credential-shaped values fail closed.
- Required checks once code exists include `gofmt`, `go vet ./...`, `go test ./servers/delibase/...`, sqlc/migration checks, Protobuf generation/compatibility, PostgreSQL concurrency tests, and Docker validation.
- Issue #722 artifact scope excludes public activation/deployment, production SLO/RPM controls, dashboards/alerts, kill switches, feature flags, operator RPCs, and manual replay tooling. GHCR release scope is pushed core `delibase@vX.Y.Z` tags whose derived OCI version is at most 128 characters only: validate the exact pushed architecture digests, generate an SPDX JSON inventory for each platform, assemble a run-scoped candidate index, complete its signature and GitHub provenance/per-platform SBOM attestations, and only then publish the public multi-architecture `vX.Y.Z` tag. Release artifacts must be rerun-safe, and an existing version may be reused only when it resolves to the same validated digest; it must never be retargeted. A serialized newest-core-SemVer gate alone may advance `latest`, so an older concurrent release cannot move it backward. Run-scoped candidate references are not releases; manual, branch, `edge`, and SHA publication paths are forbidden.

### Multi-Component Contract Sync

- `servers/thenv` changes must keep CLI contracts synchronized.

### DevHud Feature Server Rules

- A GitHub-accepted Deck mutation whose immediate result reload fails returns success with `refresh_required` and no stale pull-request detail; clients refresh and never retry that provider side effect.
- Deck device writes accept request-only shortcut configurations with closed modifier/key enums and widget configuration only; the server alone authors effective shortcut conflict state/revisions plus widget snapshots/revisions. List results expose the synchronized PR revision required by mutations, and `UpdateView` may change notification preferences while owner/kind remain immutable.
- Read `docs/project-devhud.md`, the corresponding
  `docs/servers-devhud-*-foundation.md`, proto contract, and app contract before
  implementation. The private `devhud.deck.v1` and `devhud.realqa.v1`
  source/generated packages exist. Deck's bounded authentication/persistence
  foundation plus its GitHub.com connection/callback/lifecycle and
  permission-filtered PR-mutation adapter exist at `servers/devhud-deck`;
  `servers/devhud-realqa` contains the inactive preset/tracker/auth/deletion,
  internal GitHub.com provider, R2-backed image-transfer/public-delivery, and
  replay-safe online-submission/live-transfer/initial-storage-authorization
  foundation. Deck's client-owned billed provider refresh, snapshot, and
  notification-resolution surfaces are implemented; push delivery and RealQA's
  recurring storage, rebind, and terminal-cleanup surfaces remain planned. No
  origin, published image, production secret, provider registration, catalog
  activation, deployment, or operational rollout is claimed.
- Both services use Go, PostgreSQL, migrations, sqlc, UUID v7, Connect RPC for business mutations, redacted structured operations/audit data, and narrowly scoped HTTP callbacks/webhooks. RealQA's implemented provider HTTP surface is limited to the exact signed OAuth/App callbacks and signed GitHub lifecycle webhook path in its server contract; it does not authorize browser RPC access. DevHud calls Connect through its private native transport, so feature services must not add `http://tauri.localhost` to CORS; Deck may allow the exact HTTPS DeliDev origin only for `DeckIntegrationService`, while RealQA has no browser client. Before reusing any `servers/internal` package, complete the compatibility review required by its contract (including feature audiences and error types); keep feature business policy in its owning service.
- Validate feature-audience Logto user tokens plus memory-only delibase-audience forwarded user tokens with matching subjects and required scopes. Only `DeleteFeatureData` may alternatively accept the feature-specific delibase M2M identity and typed account/organization-deletion trigger. Each feature requires a receiver-side expected delibase lifecycle client ID, verifies M2M `sub == client_id == configured ID` in addition to issuer/audience/scope, rejects peer M2M clients, and fails startup once lifecycle mode exists if that pin is missing or malformed. Strip credentials before handlers and never store/log tokens or raw authorization/provider metadata.
- Deck's future origin is exactly `https://deck.deli.dev`. Its GitHub App is separate from RealQA and GitHub.com-only. Never add a scheduler, durable refresh job or polling worker, webhook-driven PR refresh, detached post-client continuation, or any provider request not traceable to an active desktop/mobile/background/widget/manual client request. The client targets five-minute polling while permitted and automatically refreshes only views attached to a widget, notification, or shortcut or opened in the prior 30 days. Coalesce automatic/widget requests for the same viewer/view across devices for five minutes, make cache hits free, and make manual refresh bypass the cache after a billed-provider-call warning. Non-dispatching `GetRefreshPreflight` validates the authoritative catalog and returns the server-derived price plus an opaque short-lived token bound to the prospective UUID v7 `RefreshView` identity, origin, and client kind; it performs no reservation, provider call, cache refresh, or charge, and only manual UI displays the price. Every `RefreshView` first looks up an authenticated exact identity-and-digest attempt; an existing attempt returns or, only under a fresh active request carrying the current forwarded user bearer, resumes accounting after preflight expiry or catalog change, while every new attempt requires and revalidates the unexpired token before reservation/provider dispatch. Every logical `RefreshView` uses a client-generated UUID v7 identity whose durable attempt serializes reservation, provider dispatch, snapshot persistence, and billing accounting without becoming executable background work; exact replay returns the first result/disposition without another provider request or charge and changed-input reuse conflicts. The disabled delibase meter is `(devhud, deck_github_pull_request_refresh)`, unit `provider_refresh`, precision zero, exactly 50 USD micros/unit, and allowlisted only to Deck. Every reserve, release, and commit uses Deck's required outbound exact delibase origin/audience/service UUID/client ID/secret set, a least-privilege memory-cached client-credentials token, and the current memory-only forwarded user bearer from that same active request. Reserve one unit before provider dispatch, never call GitHub after reservation failure, release if no provider request was sent, and commit after dispatch including provider errors, rate limits, and timeouts. Never store the forwarded bearer, use `REALQA_STORAGE` or another background authorization, or add a billing worker/finalization grant. Startup fails before billed refresh handling when exact configuration or scoped token acquisition is unavailable. Enforce 12 manual refreshes/minute/user, 30 mutations/minute/user, four provider requests/installation, GitHub retry/rate-limit guidance, current-snapshot-only retention, 30-day notification history, typed freshness/transitions, and redacted outcome/latency metrics without SLOs.
- Deck disconnect immediately deletes provider tokens, pending callbacks, provider identity metadata, cached PR results, notification state, and widget snapshots while retaining disconnected view definitions. Reconnect clears the same cached provider scope when its installation/account/permission binding changes. Organization Owners/Admins install, replace, or reconfigure an owner-scoped GitHub installation; ordinary organization members may authorize only their own user token against the already-connected installation without changing connection metadata or state. Retain current PR snapshots only, notification history 30 days, and views until deletion. PR result rows expose individual/team reviewer identities, current assignees and labels for removal actions, explicit open/closed/merged lifecycle state independently from draft state, supported mutations, available merge methods, and the synchronized mutation revision; bounded `ListPullRequestMutationCandidates` exposes permission-filtered user/team/label operands only for advertised add actions. Connected view-definition reads use a versioned keyed repository-qualifier index outside the encrypted query to authorize before decryption; exact snapshot action lookups use an authorization-safe keyed repository-and-PR index and never scan or decrypt unrelated repository references. Merge/native auto-merge additionally require the App and viewer intersection to grant contents write. Installation rows include stable GitHub account ID/login/kind for settings display. `CreateView` requires a client-generated UUID v7 idempotency key; exact replays return the original view/revision without consuming another limit slot and changed-input reuse conflicts. Application-level envelope-encrypt retained GitHub credentials with per-record data keys wrapped by versioned managed environment-scoped keys, provider-adapter-only decrypt authority, and rotation/rewrap; rotate an expired user access token through its valid refresh token and persist the encrypted replacement pair before continuing, while GitHub's mandatory authorization-revocation webhook deletes credentials for that GitHub user. Keyed lifecycle tombstones serialize installation deletion and user revocation against first-connect, callback, and refresh upserts; only a callback created after a recorded user revocation may supersede it, and a rejected refresh token requires reauthentication. Plaintext must never reach PostgreSQL, backups, or observability. Encrypt retained raw queries, identity-bearing typed builder clauses, and PR repository/title/author/reviewer/assignee/label fields at rest with managed environment-scoped keys; never persist URLs or emit retained identity-bearing fields to logs, telemetry, traces, audits, notification history, or uncontracted caches. Opaque events resolve only through authenticated `ResolveNotificationEvent` for the matching active registration after current authorization/detail opt-in checks and never cause provider refresh. Authenticated `GetDevice` reloads the current registration and server-authored state by stable device ID after restart/stale conflict. Each `RegisterDevice` creation/renewal requires a client-generated UUID v7 replay identity; initial registration omits an expected revision, renewal requires the current device revision, exact replay returns the identical bounded lease, registration ID, and single-registration `UnregisterDevice` revocation grant, stale renewal fails without replacing mutable configuration, and changed-input reuse conflicts. The registration row keeps only a grant verifier; separately encrypted replay material exists only through the lease and is deleted on unregister/expiry. The client retains the grant only in the documented OS-vault cleanup tombstone so logout/reset cleanup can finish after account switches before later registration. Reset is device-local and does not delete server views/connections. Idempotent `DeckViewService.DeleteFeatureData` accepts either an authorized owner request or the matching durable delibase lifecycle trigger; the latter is the only account/org deletion path. Only required pseudonymized financial/security records survive.
- Deck callback URLs carry only an authenticated opaque random handle; account, owner, and current DeliDev GitHub-login bindings remain in the encrypted one-use server record, and the returned GitHub user must match that login. Signed installation-target rename events update the stable account binding, purge stale provider state, and invalidate repository indexes for repair. Authorized personal owners and organization Owners/Admins may bypass repository visibility only for `UpdateView`/`DeleteView` repair when every inaccessible qualifier has exact keyed evidence from a signed repository-removal lifecycle event; repository addition or installation replacement clears that evidence, and an update must reauthorize every repository in the resulting definition before persistence.
- RealQA's future origins are exactly `https://realqa.deli.dev` and `https://assets.realqa.deli.dev`. Its server must consume generated Go types from `protos/devhud-realqa` and must not redefine the wire contract. Repository issue-schema mapping must preserve additive issue type, Issue Form input/textarea prefill, textarea render language, and single- versus multi-select dropdown metadata; selected repository owner/name snapshots must follow signed rename webhooks by stable provider repository ID. Its GitHub App is separate from Deck and GitHub.com-only. Application-level envelope-encrypt retained GitHub credentials under the same per-record/versioned-managed-key/rotation boundary as Deck; the active key seals new records, configured previous versions remain decryptable, and authorized use transactionally rewraps an old version before operators remove it. Organization members use caller-scoped GitHub user credentials only and cannot share or manage the Owner/Admin connector credential or installation binding. Register only the internal GitHub adapter; no public tracker SDK. HTTP is limited to provider callbacks/lifecycle webhooks, exact-origin signed image PUT, and public image GET.
- RealQA's implemented inactive image boundary stages privately through five-minute-or-deadline RealQA-signed same-origin PUTs backed by R2's S3-compatible object PUT/GET/DELETE surface, never an exposed R2 endpoint; it stores upload tokens only as digests, independently re-decodes/re-encodes PNG/WebP, strips metadata, verifies declared and sanitized representations, and promotes only through opaque 128-bit public IDs. Apply 25 MiB/image, 250 MiB/session, 100 MP/image, 60,000-byte body, three concurrent upload sessions/user, 30 submissions/hour/user, and an artifact-only Cloudflare WAF/rate-limit fixture of 300 public GETs/minute/IP. Public tombstones survive scoped row deletion and return `Image removed`; no R2 list/provisioning surface, object key, sequential ID, or signed GET is exposed. Never retain reconciled title/body/URL/DOM content beyond minimum provider IDs/URLs, asset state, and idempotency digest.
- RealQA preset creation requires a client-generated UUID v7 idempotency key; exact replay returns the original preset/revision without consuming another limit slot and changed-input reuse conflicts. Its planned disabled `(devhud, realqa_image_transfer)` meter uses precision-zero `encoded_mib` units at 500 USD micros/unit and a minimum 86,400-second reservation TTL, reserving `ceil(accepted_declared_encoded_bytes / 1_048_576)` and committing `ceil(verified_uploaded_encoded_bytes / 1_048_576)` once per submission. `verified_uploaded_encoded_bytes` sums the accepted declared lengths of successfully verified PUTs, not their server-sanitized output lengths, so committed units cannot exceed reserved units when re-encoding grows an object. Uploads stop by reservation creation plus 23 hours or expiry minus one hour, whichever is earlier; no signed PUT outlives that deadline. `SubmitIssue` must start by the deadline with a fresh forwarded bearer, claim a durable transfer-finalization attempt with stable downstream keys, and use the final hour for bounded verification and live commit/release. Deadline/cleanup work never calls live usage without the bearer; an unfinalized reservation expires, staging is deleted, and nothing is promoted. Its planned disabled `(devhud, realqa_image_storage)` meter uses precision-zero `mib_day` units at 2 USD micros/unit; the authorization maximum is `ceil(verified_retained_bytes / 1_048_576)` per UTC day and settlement is `ceil(retained_image_byte_seconds / (1_048_576 * 86_400))` once per billable authorization/day. Before the first positive `CreateBackgroundUsageAuthorization`, persist a per-submission creation attempt with exact inputs and a stable downstream key; a lost response is replayed by a fresh authenticated `SubmitIssue` request, and cleanup retains unresolved attempt/tombstone state until it can recover and close the exact grant. Durably checkpoint every completed day, process the oldest unresolved checkpoint while it is the immediately preceding reservable day, and finish any accepted reservation later through its stored period. If no reservation is established before the next rollover, enter failed-billing grace at that missed period's start, block submissions, and classify it and later grace days as non-billable and never back-billed rather than silently skipping them. A non-deletion transition that closes/replaces an `ACTIVE` grant while retaining images persists one serialized cutoff, settles billable accrual through it, and starts replacement accrual at the same cutoff, while already closed grants are not back-billed. Deletion persists its cutoff, tombstones public identifiers, stops accrual, and removes objects independently of delibase availability; retain only the minimum mapping/billing retry tombstone until pre-cutoff settlement and exact closure finish. Both meters are allowlisted only to RealQA; zero numerators skip delibase reservation/commit/authorization mutations, checked wide-integer overflow fails closed, and authoritative mapping or transfer-TTL mismatch blocks submission/settlement before upload or reservation. Issue #756's bounded background authorization and authorized-usage handlers are implemented, while the RealQA server and catalog/client activation remain planned and disabled. Live transfer and recurring settlement use RealQA's required outbound exact delibase origin/audience/service UUID/client ID/secret set with procedure-specific least-privilege client-credentials tokens, memory-only bounded token caching, the forwarded user bearer only for live usage, and startup failure before submissions/work claims when configuration or either scoped token acquisition fails; this identity is distinct from the inbound lifecycle client pin. Never retain forwarded tokens or substitute the live usage RPCs for recurring work. Before GitHub create/reconciliation, persist a durable submission attempt that remains background-claimable without a client retry until the exact issue is reconciled and linked assets are idempotently promoted or, by the 24-hour staging boundary, converted to removed placeholders with bounded staging/issue-reference/storage-grant cleanup. Failed storage billing preserves public images for at most 30 days. At the unrecovered deadline, tombstone public identifiers and remove objects even during a delibase outage, then retry the retained billing teardown without restoring an image or extending public readability. Owner-authorized `RebindSubmissionStorageAuthorization` serializes a UUID v7-idempotent attempt, uses the current forwarded bearer to revoke an exact `ACTIVE` grant or accept an exact already `REVOKED`/`ACCESS_LOST` grant as closed, creates/validates the exact replacement, then compare-and-swaps the mapping; replay, stale mapping, substitution, invalid terminal state, and concurrency fail closed without retaining a bearer or installing duplicate grants. Unlinked private uploads expire by the 24-hour boundary after authenticated transfer finalization or natural reservation expiry; abandoned-submission cleanup durably closes any recovered storage grant and retains its creation attempt, mapping, and retry tombstone until an exact terminal response so no active grant is lost or orphaned. Authenticated opaque-cursor `ListSubmissions` exposes only caller-authorized retained submission/asset references needed for later deletion, never submitted content or a public asset index. Deleted public URLs return `Image removed`; image deletion does not depend on GitHub, and best-effort issue cleanup is limited to removing/replacing the same submission's deleted-image references on the exact issue RealQA created. Idempotent `DeleteFeatureData` accepts either an authorized owner request or the matching durable delibase lifecycle trigger and removes all scoped presets/submissions/assets plus tracker connection/installation bindings, encrypted credentials, and callback/webhook state. Owner-request cleanup retains each authorization mapping until exact-bound `MarkBackgroundUsageResourceDeleted` returns `RESOURCE_DELETED`, `REVOKED`, or `ACCESS_LOST`; lifecycle cleanup also accepts already-terminal `OWNER_DELETED` because delibase closes owner grants before dispatch.
- RealQA `SubmitIssue` requires its memory-only forwarded delibase bearer to carry both `delibase:usage:execute` for live transfer finalization and `delibase:billing:write` for initial storage-authorization creation.
- Production-facing Deck/RealQA catalog records remain disabled. Tests may build non-root multi-architecture fixture images with SBOM/signature/attestation validation and fixture GitHub Apps/R2/extension IDs, but must not publish GHCR images, deploy APIs, configure DNS/R2, inject production secrets, register either GitHub App, publish an extension/widget/store artifact, activate catalog entries, or roll out operations.
- Once code exists, run `go vet ./servers/devhud-deck/...`, `go test ./servers/devhud-deck/...`, `go vet ./servers/devhud-realqa/...`, and `go test ./servers/devhud-realqa/...`, plus format, sqlc, migration, PostgreSQL concurrency, provider-fixture, billing/deletion, and image validation from the corresponding server contracts.

### Testing and Validation

- Run relevant Go tests (`go test`) when code in this domain changes.
- Keep operational logging sufficient for incident debugging and audit reconstruction.
