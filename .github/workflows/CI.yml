name: CI

on:
  pull_request:
    types: [opened, synchronize, reopened]
  push:
    branches: [main]
  workflow_dispatch:

permissions:
  contents: read

concurrency:
  group: ci-${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  go-quality:
    name: Go Quality
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Filter changed paths
        id: filter
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: dorny/paths-filter@v4
        with:
          filters: |
            go:
              - .github/workflows/CI.yml
              - go.mod
              - go.sum
              - scripts/generate-go-proto.sh
              - scripts/lib/go-proto-tools.sh
              - cmds/**
              - servers/**

      - name: Determine whether to run
        id: gate
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          GO_CHANGED: ${{ steps.filter.outputs.go }}
        run: |
          set -euo pipefail

          run=false
          if [ "${EVENT_NAME}" = "workflow_dispatch" ] || [ "${GO_CHANGED}" = "true" ]; then
            run=true
          fi

          echo "run=${run}" >> "${GITHUB_OUTPUT}"

      - name: Skip (no Go-related changes)
        if: ${{ steps.gate.outputs.run != 'true' }}
        run: |
          echo "Skipping Go Quality: no relevant changes"

      - name: Setup Go
        if: ${{ steps.gate.outputs.run == 'true' }}
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache-dependency-path: go.sum

      - name: Generate Go protobuf artifacts
        if: ${{ steps.gate.outputs.run == 'true' }}
        shell: bash
        run: ./scripts/generate-go-proto.sh

      - name: Check go fmt
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: |
          set -euo pipefail

          formatted_output="$(go fmt ./...)"
          if [ -n "$formatted_output" ]; then
            echo "::error::go fmt reformatted files. Run 'go fmt ./...' locally and commit the result."
            printf '%s\n' "$formatted_output"
            exit 1
          fi

      - name: Run go vet
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: go vet ./...

  go-test:
    name: Go Test (${{ matrix.os }})
    runs-on: ${{ matrix.os }}
    strategy:
      fail-fast: false
      matrix:
        os:
          - ubuntu-latest
          - macos-latest
          - windows-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Filter changed paths
        id: filter
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: dorny/paths-filter@v4
        with:
          filters: |
            go:
              - .github/workflows/CI.yml
              - go.mod
              - go.sum
              - scripts/generate-go-proto.sh
              - scripts/lib/go-proto-tools.sh
              - cmds/**
              - servers/**

      - name: Determine whether to run
        id: gate
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          GO_CHANGED: ${{ steps.filter.outputs.go }}
        run: |
          set -euo pipefail

          run=false
          if [ "${EVENT_NAME}" = "workflow_dispatch" ] || [ "${GO_CHANGED}" = "true" ]; then
            run=true
          fi

          echo "run=${run}" >> "${GITHUB_OUTPUT}"

      - name: Skip (no Go-related changes)
        if: ${{ steps.gate.outputs.run != 'true' }}
        run: |
          echo "Skipping Go Test on ${{ matrix.os }}: no relevant changes"

      - name: Setup Go
        if: ${{ steps.gate.outputs.run == 'true' }}
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache-dependency-path: go.sum

      - name: Generate Go protobuf artifacts
        if: ${{ steps.gate.outputs.run == 'true' }}
        shell: bash
        run: ./scripts/generate-go-proto.sh

      - name: Run go test
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: go test ./...

  proto-delibase:
    name: Delibase Protobuf
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Filter changed paths
        id: filter
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: dorny/paths-filter@v4
        with:
          filters: |
            proto:
              - .github/workflows/CI.yml
              - go.mod
              - go.sum
              - package.json
              - pnpm-lock.yaml
              - pnpm-workspace.yaml
              - protos/buf.gen.yaml
              - protos/buf.yaml
              - protos/delibase/**
              - scripts/check-proto.sh
              - scripts/generate-delibase-proto.sh
              - scripts/generate-proto.sh
              - scripts/lib/go-proto-tools.sh

      - name: Determine whether to run
        id: gate
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          PROTO_CHANGED: ${{ steps.filter.outputs.proto }}
        run: |
          set -euo pipefail

          run=false
          if [ "${EVENT_NAME}" = "workflow_dispatch" ] || [ "${PROTO_CHANGED}" = "true" ]; then
            run=true
          fi

          echo "run=${run}" >> "${GITHUB_OUTPUT}"

      - name: Skip (no delibase Protobuf changes)
        if: ${{ steps.gate.outputs.run != 'true' }}
        run: |
          echo "Skipping delibase Protobuf checks: no relevant changes"

      - name: Resolve delibase Protobuf compatibility baseline
        if: ${{ steps.gate.outputs.run == 'true' }}
        shell: bash
        env:
          BASELINE_COMMIT: ${{ github.event.pull_request.base.sha || github.event.before || '' }}
        run: |
          set -euo pipefail

          baseline_path="protos/delibase/delibase.v1.binpb"
          if [ -z "${BASELINE_COMMIT}" ] || [[ "${BASELINE_COMMIT}" =~ ^0+$ ]]; then
            echo "No prior event commit is available; using the checked-in initial baseline"
          elif ! git cat-file -e "${BASELINE_COMMIT}^{commit}"; then
            echo "The prior event commit is not available: ${BASELINE_COMMIT}" >&2
            exit 1
          elif git cat-file -e "${BASELINE_COMMIT}:${baseline_path}"; then
            immutable_baseline="${RUNNER_TEMP}/delibase.v1.binpb"
            git show "${BASELINE_COMMIT}:${baseline_path}" > "${immutable_baseline}"
            echo "DELIBASE_PROTO_BASELINE=${immutable_baseline}" >> "${GITHUB_ENV}"
            echo "Using the delibase v1 baseline from ${BASELINE_COMMIT}"
          else
            echo "The prior event commit has no delibase v1 baseline; using the checked-in initial baseline"
          fi

      - name: Setup pnpm
        if: ${{ steps.gate.outputs.run == 'true' }}
        uses: pnpm/action-setup@v5
        with:
          version: 10.26.2

      - name: Setup Node.js
        if: ${{ steps.gate.outputs.run == 'true' }}
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Setup Go
        if: ${{ steps.gate.outputs.run == 'true' }}
        uses: actions/setup-go@v6
        with:
          go-version-file: go.mod
          cache-dependency-path: go.sum

      - name: Install dependencies
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm install --frozen-lockfile

      - name: Check Protobuf compatibility and generated output
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm check:proto

      - name: Run delibase Protobuf tests
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: go test ./protos/delibase/...

      - name: Run delibase Protobuf vet
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: go vet ./protos/delibase/...

      - name: Typecheck delibase TypeScript artifacts
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm --filter @delinoio/delibase-connect typecheck

  rust-fmt:
    name: Rust fmt
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Filter changed paths
        id: filter
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: dorny/paths-filter@v4
        with:
          filters: |
            rust:
              - .github/workflows/CI.yml
              - Cargo.toml
              - Cargo.lock
              - '**/Cargo.toml'
              - '**/Cargo.lock'
              - rust-toolchain
              - rust-toolchain.toml
              - '**/rust-toolchain'
              - '**/rust-toolchain.toml'
              - '**/*.rs'
              - apps/devhud/src-tauri/capabilities/**
              - apps/devhud/src-tauri/tauri.conf.json
              - crates/**

      - name: Determine whether to run
        id: gate
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          RUST_CHANGED: ${{ steps.filter.outputs.rust }}
        run: |
          set -euo pipefail

          run=false
          if [ "${EVENT_NAME}" = "workflow_dispatch" ] || [ "${RUST_CHANGED}" = "true" ]; then
            run=true
          fi

          echo "run=${run}" >> "${GITHUB_OUTPUT}"

      - name: Skip (no Rust-related changes)
        if: ${{ steps.gate.outputs.run != 'true' }}
        run: |
          echo "Skipping Rust fmt: no relevant changes"

      - name: Install Rust toolchain
        if: ${{ steps.gate.outputs.run == 'true' }}
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: nightly-2026-01-01
          components: rustfmt

      - name: Cache Rust dependencies
        if: ${{ steps.gate.outputs.run == 'true' }}
        uses: Swatinem/rust-cache@v2

      - name: Run rustfmt
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: cargo fmt --all --check

  rust-clippy:
    name: Rust clippy
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Filter changed paths
        id: filter
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: dorny/paths-filter@v4
        with:
          filters: |
            rust:
              - .github/workflows/CI.yml
              - Cargo.toml
              - Cargo.lock
              - '**/Cargo.toml'
              - '**/Cargo.lock'
              - rust-toolchain
              - rust-toolchain.toml
              - '**/rust-toolchain'
              - '**/rust-toolchain.toml'
              - '**/*.rs'
              - apps/devhud/src-tauri/capabilities/**
              - apps/devhud/src-tauri/tauri.conf.json
              - crates/**

      - name: Determine whether to run
        id: gate
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          RUST_CHANGED: ${{ steps.filter.outputs.rust }}
        run: |
          set -euo pipefail

          run=false
          if [ "${EVENT_NAME}" = "workflow_dispatch" ] || [ "${RUST_CHANGED}" = "true" ]; then
            run=true
          fi

          echo "run=${run}" >> "${GITHUB_OUTPUT}"

      - name: Skip (no Rust-related changes)
        if: ${{ steps.gate.outputs.run != 'true' }}
        run: |
          echo "Skipping Rust clippy: no relevant changes"

      - name: Install Rust toolchain
        if: ${{ steps.gate.outputs.run == 'true' }}
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: nightly-2026-01-01
          components: clippy

      - name: Cache Rust dependencies
        if: ${{ steps.gate.outputs.run == 'true' }}
        uses: Swatinem/rust-cache@v2

      - name: Setup pnpm
        if: ${{ steps.gate.outputs.run == 'true' }}
        uses: pnpm/action-setup@v5
        with:
          version: 10.26.2

      - name: Setup Node.js
        if: ${{ steps.gate.outputs.run == 'true' }}
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install frontend dependencies
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm install --frozen-lockfile

      - name: Build DevHud frontend
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm --filter devhud build

      - name: Install DevHud Linux dependencies
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: |
          sudo apt-get update
          sudo apt-get install --yes \
            libayatana-appindicator3-dev \
            librsvg2-dev \
            libssl-dev \
            libwebkit2gtk-4.1-dev \
            libxdo-dev

      - name: Run clippy
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: |
          cargo clippy --workspace --all-targets --all-features --exclude devhud-probe -- -D warnings
          cargo clippy -p devhud-probe --all-targets --features desktop-cef --locked -- -D warnings

  rust-test:
    name: Rust test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Filter changed paths
        id: filter
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: dorny/paths-filter@v4
        with:
          filters: |
            rust:
              - .github/workflows/CI.yml
              - Cargo.toml
              - Cargo.lock
              - '**/Cargo.toml'
              - '**/Cargo.lock'
              - rust-toolchain
              - rust-toolchain.toml
              - '**/rust-toolchain'
              - '**/rust-toolchain.toml'
              - '**/*.rs'
              - apps/devhud/src-tauri/capabilities/**
              - apps/devhud/src-tauri/tauri.conf.json
              - crates/**

      - name: Determine whether to run
        id: gate
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          RUST_CHANGED: ${{ steps.filter.outputs.rust }}
        run: |
          set -euo pipefail

          run=false
          if [ "${EVENT_NAME}" = "workflow_dispatch" ] || [ "${RUST_CHANGED}" = "true" ]; then
            run=true
          fi

          echo "run=${run}" >> "${GITHUB_OUTPUT}"

      - name: Skip (no Rust-related changes)
        if: ${{ steps.gate.outputs.run != 'true' }}
        run: |
          echo "Skipping Rust test: no relevant changes"

      - name: Install Rust toolchain
        if: ${{ steps.gate.outputs.run == 'true' }}
        uses: dtolnay/rust-toolchain@v1
        with:
          toolchain: nightly-2026-01-01

      - name: Cache Rust dependencies
        if: ${{ steps.gate.outputs.run == 'true' }}
        uses: Swatinem/rust-cache@v2

      - name: Run cargo test
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: cargo test --workspace --all-targets

  node-mpapp-test:
    name: Node mpapp test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Filter changed paths
        id: filter
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: dorny/paths-filter@v4
        with:
          filters: |
            ci_workflow:
              - .github/workflows/CI.yml

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10.26.2

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Determine whether to run
        id: gate
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          CI_WORKFLOW_CHANGED: ${{ steps.filter.outputs.ci_workflow }}
          WORKSPACE: mpapp
        run: |
          set -euo pipefail

          run=false
          if [ "${EVENT_NAME}" = "workflow_dispatch" ] || [ "${CI_WORKFLOW_CHANGED}" = "true" ]; then
            run=true
          else
            affected_json="$(pnpm dlx turbo@2.9.14 query affected --packages "${WORKSPACE}")"
            printf '%s\n' "${affected_json}"
            run="$(printf '%s' "${affected_json}" | jq -r '.data.affectedPackages.items | any(.name == env.WORKSPACE)')"
          fi

          echo "run=${run}" >> "${GITHUB_OUTPUT}"

      - name: Skip (mpapp unaffected)
        if: ${{ steps.gate.outputs.run != 'true' }}
        run: |
          echo "Skipping Node mpapp test: workspace unaffected"

      - name: Install dependencies
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm install --frozen-lockfile

      - name: Run mpapp tests
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm --filter mpapp test

  node-mpapp-lint:
    name: Node mpapp lint
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Filter changed paths
        id: filter
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: dorny/paths-filter@v4
        with:
          filters: |
            ci_workflow:
              - .github/workflows/CI.yml

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10.26.2

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Determine whether to run
        id: gate
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          CI_WORKFLOW_CHANGED: ${{ steps.filter.outputs.ci_workflow }}
          WORKSPACE: mpapp
        run: |
          set -euo pipefail

          run=false
          if [ "${EVENT_NAME}" = "workflow_dispatch" ] || [ "${CI_WORKFLOW_CHANGED}" = "true" ]; then
            run=true
          else
            affected_json="$(pnpm dlx turbo@2.9.14 query affected --packages "${WORKSPACE}")"
            printf '%s\n' "${affected_json}"
            run="$(printf '%s' "${affected_json}" | jq -r '.data.affectedPackages.items | any(.name == env.WORKSPACE)')"
          fi

          echo "run=${run}" >> "${GITHUB_OUTPUT}"

      - name: Skip (mpapp unaffected)
        if: ${{ steps.gate.outputs.run != 'true' }}
        run: |
          echo "Skipping Node mpapp lint: workspace unaffected"

      - name: Install dependencies
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm install --frozen-lockfile

      - name: Run mpapp lint
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm --filter mpapp lint

  node-binpm-docs-test:
    name: Node binpm-docs test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Filter changed paths
        id: filter
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: dorny/paths-filter@v4
        with:
          filters: |
            ci_workflow:
              - .github/workflows/CI.yml

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10.26.2

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Determine whether to run
        id: gate
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          CI_WORKFLOW_CHANGED: ${{ steps.filter.outputs.ci_workflow }}
          WORKSPACE: binpm-docs
        run: |
          set -euo pipefail

          run=false
          if [ "${EVENT_NAME}" = "workflow_dispatch" ] || [ "${CI_WORKFLOW_CHANGED}" = "true" ]; then
            run=true
          else
            affected_json="$(pnpm dlx turbo@2.9.14 query affected --packages "${WORKSPACE}")"
            printf '%s\n' "${affected_json}"
            run="$(printf '%s' "${affected_json}" | jq -r '.data.affectedPackages.items | any(.name == env.WORKSPACE)')"
          fi

          echo "run=${run}" >> "${GITHUB_OUTPUT}"

      - name: Skip (binpm-docs unaffected)
        if: ${{ steps.gate.outputs.run != 'true' }}
        run: |
          echo "Skipping Node binpm-docs test: workspace unaffected"

      - name: Install dependencies
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm install --frozen-lockfile

      - name: Run binpm-docs tests
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm --filter binpm-docs test

  node-nodeup-docs-test:
    name: Node nodeup-docs test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Filter changed paths
        id: filter
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: dorny/paths-filter@v4
        with:
          filters: |
            ci_workflow:
              - .github/workflows/CI.yml

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10.26.2

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Determine whether to run
        id: gate
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          CI_WORKFLOW_CHANGED: ${{ steps.filter.outputs.ci_workflow }}
          WORKSPACE: nodeup-docs
        run: |
          set -euo pipefail

          run=false
          if [ "${EVENT_NAME}" = "workflow_dispatch" ] || [ "${CI_WORKFLOW_CHANGED}" = "true" ]; then
            run=true
          else
            affected_json="$(pnpm dlx turbo@2.9.14 query affected --packages "${WORKSPACE}")"
            printf '%s\n' "${affected_json}"
            run="$(printf '%s' "${affected_json}" | jq -r '.data.affectedPackages.items | any(.name == env.WORKSPACE)')"
          fi

          echo "run=${run}" >> "${GITHUB_OUTPUT}"

      - name: Skip (nodeup-docs unaffected)
        if: ${{ steps.gate.outputs.run != 'true' }}
        run: |
          echo "Skipping Node nodeup-docs test: workspace unaffected"

      - name: Install dependencies
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm install --frozen-lockfile

      - name: Run nodeup-docs tests
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm --filter nodeup-docs test

  node-public-docs-test:
    name: Node public-docs test
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Filter changed paths
        id: filter
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: dorny/paths-filter@v4
        with:
          filters: |
            ci_workflow:
              - .github/workflows/CI.yml

      - name: Setup pnpm
        uses: pnpm/action-setup@v5
        with:
          version: 10.26.2

      - name: Setup Node.js
        uses: actions/setup-node@v6
        with:
          node-version: lts/*
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Determine whether to run
        id: gate
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          CI_WORKFLOW_CHANGED: ${{ steps.filter.outputs.ci_workflow }}
          WORKSPACE: public-docs
        run: |
          set -euo pipefail

          run=false
          if [ "${EVENT_NAME}" = "workflow_dispatch" ] || [ "${CI_WORKFLOW_CHANGED}" = "true" ]; then
            run=true
          else
            affected_json="$(pnpm dlx turbo@2.9.14 query affected --packages "${WORKSPACE}")"
            printf '%s\n' "${affected_json}"
            run="$(printf '%s' "${affected_json}" | jq -r '.data.affectedPackages.items | any(.name == env.WORKSPACE)')"
          fi

          echo "run=${run}" >> "${GITHUB_OUTPUT}"

      - name: Skip (public-docs unaffected)
        if: ${{ steps.gate.outputs.run != 'true' }}
        run: |
          echo "Skipping Node public-docs test: workspace unaffected"

      - name: Install dependencies
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm install --frozen-lockfile

      - name: Run public-docs tests
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm --filter public-docs test

  node-delidev-app:
    name: Node DeliDev app
    runs-on: ubuntu-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v6
        with:
          fetch-depth: 0

      - name: Filter changed paths
        id: filter
        if: ${{ github.event_name != 'workflow_dispatch' }}
        uses: dorny/paths-filter@v4
        with:
          filters: |
            delidev:
              - .github/workflows/CI.yml
              - .nvmrc
              - package.json
              - pnpm-lock.yaml
              - pnpm-workspace.yaml
              - turbo.json
              - apps/AGENTS.md
              - apps/delidev-app/**
              - protos/buf.gen.yaml
              - protos/buf.yaml
              - protos/delibase/**
              - scripts/generate-delibase-proto.sh
              - scripts/lib/go-proto-tools.sh

      - name: Determine whether to run
        id: gate
        shell: bash
        env:
          EVENT_NAME: ${{ github.event_name }}
          DELIDEV_CHANGED: ${{ steps.filter.outputs.delidev }}
        run: |
          set -euo pipefail
          run=false
          if [ "${EVENT_NAME}" = "workflow_dispatch" ] || [ "${DELIDEV_CHANGED}" = "true" ]; then
            run=true
          fi
          echo "run=${run}" >> "${GITHUB_OUTPUT}"

      - name: Skip (DeliDev unaffected)
        if: ${{ steps.gate.outputs.run != 'true' }}
        run: |
          echo "Skipping DeliDev app checks: no relevant changes"

      - name: Setup pnpm
        if: ${{ steps.gate.outputs.run == 'true' }}
        uses: pnpm/action-setup@v5
        with:
          version: 10.26.2

      - name: Setup Node.js
        if: ${{ steps.gate.outputs.run == 'true' }}
        uses: actions/setup-node@v6
        with:
          node-version-file: .nvmrc
          cache: pnpm
          cache-dependency-path: pnpm-lock.yaml

      - name: Install dependencies
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm install --frozen-lockfile

      - name: Build generated delibase client
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm --filter @delinoio/delibase-connect build

      - name: Validate types, lint, and components
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: |
          pnpm --filter delidev-app typecheck
          pnpm --filter delidev-app lint
          pnpm --filter delidev-app test

      - name: Build and validate PWA artifact
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: |
          pnpm --filter delidev-app build
          pnpm --filter delidev-app test:pwa
          git diff --exit-code -- apps/delidev-app/dist

      - name: Install browser engines
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm --filter delidev-app exec playwright install --with-deps chromium firefox webkit msedge

      - name: Run responsive browser smoke tests
        if: ${{ steps.gate.outputs.run == 'true' }}
        run: pnpm --filter delidev-app test:browser

  ci-result:
    name: CI Result
    runs-on: ubuntu-latest
    if: always()
    needs:
      - go-quality
      - go-test
      - proto-delibase
      - rust-fmt
      - rust-clippy
      - rust-test
      - node-mpapp-test
      - node-mpapp-lint
      - node-binpm-docs-test
      - node-nodeup-docs-test
      - node-public-docs-test
      - node-delidev-app
    steps:
      - run: exit 1
        if: ${{ always() && (contains(needs.*.result, 'failure') || contains(needs.*.result, 'cancelled')) }}
