# Docs site switcher

Private shared Rspress theme component for switching between Delino public documentation sites.

The package intentionally exposes source TypeScript and CSS to the documentation apps. It is not published and has no generated `dist` output.
