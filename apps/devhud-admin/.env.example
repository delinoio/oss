# OSS uses this safe loopback default. Override it only for a contributor-owned
# local Logto instance, and use the exact same value in servers/devhud-api/.env.
# Team development receives this name from its service path.
DEVHUD_LOGTO_ISSUER=http://localhost:3001/oidc
