{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "main",
  "description": "Minimal permissions for the bundled DevHUD desktop shell.",
  "windows": ["main"],
  "permissions": ["core:default"]
}
