{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "desktop-main",
  "description": "Allows only DevHud's desktop HUD window to use its HUD lifecycle commands.",
  "local": true,
  "platforms": ["linux", "macOS", "windows"],
  "windows": ["main"],
  "permissions": [
    "allow-hide-hud",
    "allow-show-settings"
  ]
}
