# clibox-transform

Internal transform command implementation for clibox. This crate is not published
and does not provide a supported external Rust library API.

The executable is distributed through native packages and `@delino/clibox` on npm.
