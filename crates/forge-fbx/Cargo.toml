[package]
name = "forge-fbx"
version = "0.0.0"
edition = "2024"
license = "Apache-2.0"
publish = false

[dependencies]
forge-scene = { path = "../forge-scene" }
forge-tree-doc = { path = "../forge-tree-doc" }
fbxcel = { version = "=0.9.0", features = ["writer"] }
glam = "=0.30.10"

[dev-dependencies]
ufbx = "=0.11.4"
serde_json = "1.0.149"
