# typia-macros

> **Rust version of [typia.io](https://typia.io/).**

`typia-macros` is the proc-macro companion crate for `typia`, the Rust version
of `typia.io`.

It provides the derive and field-tag attribute support used by `typia`, such as
`#[derive(LLMData)]` and `#[typia(tags(...))]`.
