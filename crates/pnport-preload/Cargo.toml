[package]
name = "pnport-preload"
version = "0.0.0"
edition = "2021"
license = "MIT"
publish = false

[lib]
crate-type = ["cdylib"]
test = false
doctest = false

[dependencies]
pnport-core = { path = "../pnport-core" }
libc = "0.2.182"
serde_json = "1"
