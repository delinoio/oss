# clibox-config

Internal dotenv and YAML configuration commands for the `clibox` executable.
This crate is not published and has no supported public Rust API.

See [the clibox README](../clibox/README.md) for command usage.
